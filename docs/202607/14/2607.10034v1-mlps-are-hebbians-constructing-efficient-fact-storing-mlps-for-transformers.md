---
title: "MLPs are Hebbians: Constructing Efficient Fact-Storing MLPs for Transformers"
title_zh: MLP是Hebbian学习器：为Transformer构建高效的事实存储MLP
authors: "Roberto Garcia, Jerry Liu, Ronny Junkins, Sabri Eyuboglu, Atri Rudra, Christopher Ré"
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.10034v1"
tags: ["query:ns-xai"]
score: 8.0
evidence: LLM中MLP事实存储的可解释性
tldr: 大型语言模型以信息论最优速率在MLP层存储事实知识，但现有构造无法解释这一现象。本文提出首个与Transformer兼容的事实存储MLP封闭形式构造，满足最优存储缩放、任意输入输出几何及Transformer内工作。在等向嵌入下，该构造达到信息论最优存储容量，参数需求比先前构造少10-104倍；对任意键值嵌入，同样达到最优缩放。在Transformer块内用于事实检索时，参数需求少15-63倍。作为概念验证，交换Transformer的MLP可实现模块化事实编辑。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10034v1/fig-001.webp\", \"caption\": \"\", \"page\": 23, \"index\": 1, \"width\": 1027, \"height\": 693}, {\"url\": \"assets/figures/arxiv/2607.10034v1/fig-002.webp\", \"caption\": \"\", \"page\": 28, \"index\": 2, \"width\": 2117, \"height\": 1754}, {\"url\": \"assets/figures/arxiv/2607.10034v1/fig-003.webp\", \"caption\": \"\", \"page\": 29, \"index\": 3, \"width\": 2237, \"height\": 1844}, {\"url\": \"assets/figures/arxiv/2607.10034v1/fig-004.webp\", \"caption\": \"\", \"page\": 30, \"index\": 4, \"width\": 2237, \"height\": 1852}, {\"url\": \"assets/figures/arxiv/2607.10034v1/fig-005.webp\", \"caption\": \"\", \"page\": 31, \"index\": 5, \"width\": 2476, \"height\": 1852}]"
motivation: 现有MLP事实存储构造无法解释LLM中事实存储的信息论最优缩放现象。
method: 提出首个Transformer兼容的封闭式MLP构造，分析解码间隔，实现最优存储容量缩放。
result: 等向嵌入下参数减少10-104倍，任意嵌入下达到最优缩放，Transformer内参数减少15-63倍。
conclusion: 该构造为模块化事实编辑提供新途径，验证了MLP作为Hebbian存储的理论可行性。
---

## 摘要
大型语言模型（LLM）在其参数中存储事实知识。尽管近期研究表明这些知识存在于MLP层中，但现有的关于LLM中事实存储的构造性和机械性可解释性模型未能解释一个令人惊讶的经验现象：它们以信息论最优的速率存储事实。在这项工作中，我们发展了关于这一现象的理论解释。我们首次构造了与Transformer兼容的事实存储MLP闭式构造，该构造满足LLM中经验观察到的三个特性：（i）达到最优的事实存储规模，（ii）处理任意输入/输出几何结构，（iii）在Transformer内部工作。我们工作的关键在于分析MLP的解码边界，而以往研究仅研究MLP的事实存储。在等向嵌入下，我们的构造实现了信息论最优的存储容量规模，并且在匹配事实数量时，所需参数比以往构造少10到104倍。对于任意键和值嵌入，我们展示了我们的构造达到相同的存储容量规模，惩罚因子取决于嵌入几何结构。此外，我们证明了所构造的MLP可以在Transformer块内用于事实回忆任务，并在最优容量规模下实现，在匹配事实数量时所需参数比以往构造少15到63倍。最后，作为概念验证，我们展示了通过将Transformer的MLP替换为新的MLP，事实存储MLP能够实现模块化事实编辑。

## Abstract
Large language models (LLMs) store factual knowledge in their parameters. While recent work has shown that this knowledge resides in MLP layers, existing constructive and mechanistic interpretability models of fact-storage in LLMs fail to explain the surprising empirical phenomenon that they store facts at an information-theoretically optimal rate. In this work, we develop a theoretical account of this phenomenon. We develop the first Transformer-compatible fact-storing MLP closed-form construction that satisfies the following three properties empirically observed in LLMs: it (i) attains optimal fact storage scaling, (ii) handles arbitrary input/output geometries, and (iii) works inside Transformers. Key to our work is to analyze the decoding margin of MLPs, whereas prior work only studies MLP fact storage. Under isotropic embeddings, our construction achieves information-theoretically optimal storage capacity scaling and requires $10$-$104\times$ fewer parameters at matched fact count than prior constructions. For arbitrary key and value embeddings, we show that our construction attains the same storage capacity scaling, up to penalization factors depending on the embedding geometries. Moreover, we demonstrate that our constructed MLPs can be used within Transformer blocks for factual recall tasks at optimal capacity scaling, requiring $15$-$63\times$ fewer parameters at matched fact count than prior constructions. Finally, as a proof-of-concept, we show that fact-storing MLPs enable modular fact editing by swapping a Transformer's MLP with a new one.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：大型语言模型（LLM）能够以信息论最优的速率在其 MLP 层中存储事实知识，但现有的构造性和可解释性模型无法解释这一现象。现有构造要么理论容量次优，要么无法处理真实 LLM 中的非等向嵌入，要么不能在 Transformer 块内部实际用于事实回忆。
- **研究背景**：事实知识在 Transformer 中主要存储于 MLP 层（Geva et al. 2021; Dai et al. 2022），而 Nichani et al. (2024) 提出的 NTK MLP 构造虽有一定容量保证，但仍存在理论-经验差距，且局限于等向嵌入和独立 MLP 场景。
- **整体意义**：本文首次给出了满足 LLM 事实存储三个关键经验性质的闭式 MLP 构造，并为 Transformer 块实现信息论最优存储容量提供了理论解释，同时展示了模块化事实编辑的应用潜力。

## 2. 方法论

### 核心思想
- 将 MLP 重新解读为核空间中的 Hebbian 记忆（Theorem 3.1）：经过特征白化后，任何 MLP 等价于一个核 Hebbian 记忆，其核由特征映射定义。
- 关键洞察是研究 MLP 的 **解码边界（decoding margin）**，而不仅仅是正可分性（separability）。较大的解码边界保证了在嘈杂查询（如注意力输出）下仍能正确解码，这是 Transformer 可用性的必要条件。

### 关键技术细节
- **Hebbian MLP 构造**：使用双线性随机特征映射（sketched quadratic kernel），即 gating MLP 形式：`MLP(x) = B[(Ax) ⊙ (Gx)]`，其中 A、G 为随机高斯矩阵，B 为 Hebbian 读出矩阵。该映射近似二次核 `K₂(x,z) = ⟨x,z⟩²`。
- **解码边界分解**：`γ_{i,j} = signal - crosstalk`，信号项依赖自相似核值和值分离度，串扰项依赖核列能量、值干扰能量及其对齐度。
- **等向嵌入下的边界缩放**：`γ_min ≥ 1 - C √(F log F)/(md)`，达到信息论最优下界。
- **任意嵌入下的推广**：引入四个几何统计量（P_key, P_val, P_align, S_sig）惩罚因子，边界缩放仍保持最优，仅乘性惩罚。
- **白化与数据依赖构造**：
  - **核白化**：使用经验协方差对特征进行白化，降低串扰量。
  - **数据依赖构造**：通过交替最小二乘优化随机特征矩阵 A 和 G，进一步提升容量。
- **Transformer 集成**：证明在有界注意力噪声下，Transformer 块使用该 MLP 仍能实现最优容量 (Theorem 5.2)。

### 算法流程（文字描述）
1. 输入键集合 `K`、值集合 `V`、事实映射 `f`。
2. 随机采样两组高斯矩阵 `A, G ∈ R^{m×d}`。
3. 计算双线性特征 `ϕ(x)_r = (A_r^T x)(G_r^T x)`（归一化）。
4. 构建 Hebbian 读出 `B = (1/F) C_f^T Φ`，其中 `Φ` 为特征矩阵，`C_f` 为值编码矩阵。
5. 可选步骤：白化（乘以协方差逆）或数据依赖（交替最小二乘更新 A、G）。
6. 输出 MLP：`ŷ = B ϕ(x)`。

## 3. 实验设计

### 数据集 / 场景
- **合成任务**：Synthetic Sequential Factual Recall (SSFR) – 模拟 Transformer 从带干扰的上下文中回忆键-值事实。
- **真实风格任务**：基于 Goodreads 图书数据集的 (书籍，作者) 语言建模任务，使用单层 Transformer。
- **真实 LM 嵌入**：从 Qwen3-0.6B 中间层（第14层）捕获的 (输入，输出) 嵌入对。

### Benchmark 与对比方法
- **MLP 容量对比**：
  - 梯度下降训练的双线性 MLP (GD)
  - NTK 构造 (Nichani et al. 2024)
  - 本文三种变体：无白化、白化、数据依赖
- **事实编辑对比**：
  - MEMIT (Meng et al. 2023c)
  - ROME (Meng et al. 2023b)
  - AlphaEdit (Fang et al. 2025)
  - 本文方法：MLP Swapping（替换整个 MLP）

### 评价指标
- **边界/容量**：最小解码边界、存储参数-事实曲线。
- **Transformer 精度**：SSFR 端到端准确率、训练精度、事实自适应精度。
- **事实编辑**：有效性 (efficacy)、泛化 (paraphrase)、特异性 (specificity)、和谐平均得分 (score) 以及非事实困惑度比 (r_NF)。

## 4. 资源与算力

论文正文及附录均未明确说明使用的 GPU 型号、数量或训练时长。仅提及训练参数（学习率 2e-4，batch size 1280 等）和训练步数（4000 迭代），未报告硬件信息。

## 5. 实验数量与充分性

- **实验规模**：覆盖多个维度 (d=64,90,128)、多种事实数 (F 从 2^6 到 2^13)、隐藏宽度扫描、随机种子（多数 4-5 个种子）。
- **消融与验证**：
  - 边界缩放理论拟合 (R² ≥ 0.95)
  - 不同几何正则（β-sweep）下的边界验证
  - 注意力噪声与 Transformer 可用性关系
  - 容量曲线对比（与 GD、NTK）
  - 事实编辑实验在三个编辑百分比 (2%, 5%, 10%) 进行
- **充分性**：实验设计较全面，既验证了理论预测，又展示了实际应用。但主要基于合成数据和单层 Transformer，未在完整大规模 LLM 上进行编辑验证，存在外部有效性局限。对比方法均为最新先进方法，比较公平。

## 6. 主要结论与发现

1. **MLP 是核 Hebbian 记忆**：经过特征白化等价于核 Hebbian 记忆。
2. **解码边界决定 Transformer 可用性**：仅正边界不够，需远离零边界以应对注意力噪声。
3. **等向嵌入下信息论最优容量**：构造实现 `W = Θ(F log F)`，参数需求比 NTK 少 10–104 倍。
4. **任意嵌入下保持最优缩放**：仅受四个几何惩罚因子影响，经验边界拟合 R²≥0.95。
5. **Transformer 块达到最优容量**：使用本文构造的 MLP，在 SSFR 任务上参数需求比 NTK 少 15–63 倍。
6. **模块化事实编辑**：MLP Swapping 在最多 10% 事实编辑时精确得分为 0.999，比最先进基线高 44.9 个百分点，且几乎不损害非事实困惑度。

## 7. 优点

- **理论贡献**：首次给出满足 LLM 事实存储三大性质的闭式 MLP 构造，并提供信息论最优边界和容量保证。
- **方法创新**：引入解码边界概念并识别其关键作用，建立 Hebbian 记忆与 MLP 的联系，提出白化和数据依赖变体。
- **实验稳健**：理论预测与实证高度吻合（R²≥0.95），覆盖多种几何设置和 LLM 真实嵌入。
- **应用潜力**：展示模块化事实编辑的有效性，为知识操作提供了新范式。
- **开放科学**：代码已开源。

## 8. 不足与局限

- **架构限制**：理论和实验主要限于单层 Transformer 块；多层或多跳回忆未被覆盖。
- **构造 vs. 训练**：构造的 MLP 与真实训练后的 MLP 仍有性能差距（6–10 倍参数），表明训练可能进一步优化。
- **通用性**：事实编辑实验基于合成任务和特殊设计架构（无残差、固定输出投影），可能不直接推广到标准 LLM。
- **算力未报告**：缺乏硬件和运行时间细节，影响可复现性。
- **注意力噪声假设**：注意力噪声上限的估计依赖于特定任务，在实际 LLM 中可能更复杂。
- **事实集假设**：假设事实集为双射（键-值一一对应），实际 LLM 中存在重叠知识。

（完）
