---
title: Differentiable Clone-Structured Causal Graphs for End-to-End Cognitive Map Learning from Image Sequences
title_zh: 可微分的克隆结构因果图：从图像序列中进行端到端认知地图学习
authors: "Arash Nikzad, Sasan Sarbishegi, Ali Dasmeh, Muhammad Asif, Parsa Gharavi, Erik Husom, Sagar Sen, Andrew B. Lehr, Olivier Penacchio, Ana Clemente, Tristan M. Stöber"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12382v1"
tags: ["query:ns-xai"]
score: 8.0
evidence: 可微克隆结构因果图用于可解释认知地图学习
tldr: 针对CSCG需要预定义离散字母表且无法端到端处理原始图像序列的问题，提出全可微分的gradCSCG模块，并将其与VQ-VAE感知前端耦合。通过软发射前向传播使地图学习目标反向传播至感知模块，并引入损失平衡机制防止模块崩溃。在符号网格世界和MNIST图像序列上，该方法能从原始视觉输入中高精度恢复底层邻接图。本工作证明CSCG可作为深度学习架构中可组合的构建块。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12382v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1569, \"height\": 413, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12382v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1775, \"height\": 429, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12382v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1792, \"height\": 1130, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12382v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1786, \"height\": 352, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12382v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1744, \"height\": 1185, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12382v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1800, \"height\": 600, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.12382v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 900, \"height\": 1253, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12382v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1335, \"height\": 261, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12382v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1977, \"height\": 261, \"label\": \"Table\"}]"
motivation: 克隆结构因果图(CSCG)虽能学习可解释地图，但依赖离散字母表且不可微，难以与神经网络结合实现端到端原始图像序列处理。
method: 将CSCG重写为全可微分模块gradCSCG，耦合向量量化变分自编码器(VQ-VAE)感知前端，通过软发射前向传播和损失平衡机制联合训练。
result: 在符号网格世界和MNIST图像序列上，gradCSCG恢复房间拓扑及邻接图的边缘精确度和召回率均较高。
conclusion: 该工作表明CSCG可成为深度学习架构中的可组合构建块，为端到端认知地图学习提供新途径。
---

## 摘要
一个智能体如何仅从持续不断的原始感官输入及其自身运动来构建其世界的结构化地图，尤其是当自然变化意味着精确的感官模式很少重复时？克隆结构因果图算法（CSCG）是一种规范的海马体模型，展示了如何从混淆观测中学习可解释的地图。然而，CSCG需要预定义的离散字母表，其期望最大化公式不易与现有神经网络模块结合，阻碍了原始图像序列的端到端处理。我们通过将CSCG重新表述为单一、完全可微的模块gradCSCG，并将其与学习的向量量化变分自编码器（VQ-VAE）感知前端耦合，消除了这一障碍。软发射前向传播允许地图学习目标回流到感知中，同时一组损失平衡机制在联合训练期间缓解模块崩溃。我们首先证明，梯度训练通过从严重混淆的观测中恢复房间拓扑，重现了CSCG在原始符号网格世界上的结果。其次，我们表明在MNIST图像序列上地图恢复仍然稳健，其中每次访问一个位置都会生成其分配数字的新采样图像。在四个严重混淆的环境中，端到端流水线成功地从视觉输入中发现了具有高边精确度和召回率的底层邻接图。这项工作提供了一个原理证明，即CSCG可以作为深度学习架构中可组合的构建模块。

## Abstract
How can an agent build a structured map of its world from nothing but an ongoing sequence of raw sensory input and its own movements, especially when natural variation means exact sensory patterns rarely repeat? The Clone-Structured Causal Graph algorithm (CSCG), a normative hippocampus model, shows how an interpretable map can be learned from aliased observations. However, CSCG requires a predefined discrete alphabet, and its expectation-maximization formulation is not easily combined with existing neural network modules, preventing the end-to-end processing of raw image sequences. We remove this barrier by reformulating CSCG as a single, fully differentiable module, gradCSCG, and coupling it to a learned vector-quantized variational autoencoder (VQ-VAE) perceptual front-end. A soft emission forward pass allows the map-learning objective to flow back into perception, while a set of loss-balancing mechanisms mitigates module collapse during joint training. We demonstrate, first, that gradient training reproduces CSCG's results on original symbolic grid worlds by recovering room topology from heavily aliased observations. Second, we show that map recovery remains robust on MNIST image sequences, where each visit to a location yields a newly sampled image of its assigned digit. Across four heavily aliased environments, the end-to-end pipeline successfully uncovers the underlying adjacency graph with high edge precision and recall, directly from visual input. This work provides a proof of principle that CSCG can serve as a composable building block in a deep learning architecture.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：如何让智能体仅从持续的原始感官输入（如图像序列）和自身运动中构建出结构化的、可解释的认知地图？传统的克隆结构因果图（CSCG）算法虽然能够从混淆观测中学习可解释的地图，但它要求预定义的离散符号字母表，且采用期望最大化（EM）训练，无法与神经网络模块组合，因此不能端到端处理原始图像序列。
- **研究动机**：神经科学中的海马体模型（如CSCG）能解释认知地图的形成，但现实世界中的感知输入是连续高维的。将CSCG与深度学习结合，使其能从像素级输入中学习地图，是连接神经科学与人工智能的重要一步。
- **整体含义**：本文提出gradCSCG，将CSCG重写为完全可微模块，并与VQ-VAE感知前端联合训练，实现从原始图像序列端到端恢复环境拓扑图。这为CSCG成为深度学习架构中可组合的构建模块提供了原理证明。

## 2. 论文提出的方法论

### 核心思想
将CSCG的HMM前向算法重新实现为可微分的log-space计算图，通过梯度下降优化参数；同时耦合一个VQ-VAE作为感知前端，将图像离散化为符号令牌；通过软发射前向传播，让序列模型的似然梯度回流到编码器，从而联合优化感知和地图学习。

### 关键技术细节

- **VQ-VAE感知前端**：卷积编码器 \(E_\phi\) 将图像映射到潜在向量 \(z_t\)；通过最近邻查找量化到代码本 \(\{e_k\}\)，使用straight-through估计器传递梯度；同时输出温度控制的软后验 \(\rho_t(k) = \text{softmax}(-\|z_t - e_k\|^2/\tau)\)，用于可微耦合。
- **gradCSCG（可微克隆HMM）**：
  - 状态空间：每个令牌 \(k\) 分配 \(C_k\) 个克隆状态（隐状态）和一个吸收态 \(\bot\)。
  - 发射矩阵是确定性的：\(B_{s,o} = 1\) 若状态 \(s\) 发射令牌 \(o\)，否则0。
  - 可学习参数：初始logits \(\pi\) 和动作条件转移logits \(\Theta \in \mathbb{R}^{A \times N \times N}\)。
  - 前向算法（log-space）：使用logsumexp实现数值稳定。
  - 软发射：用后验 \(\log\rho_t(\omega(j))\) 替换硬发射 \(\log B_{j,o_t}\)，使损失可微。
- **损失函数与联合训练**：
  - 联合损失：\(L_{\text{joint}} = L_{\text{rec}} + \beta L_{\text{commit}} + \lambda_t \tilde{L}_{\text{gradCSCG}} + \alpha_{\text{div}} L_{\text{div}}\)。
  - 长度归一化：\(\tilde{L}_{\text{gradCSCG}}\) 除以序列长度 \(T\)，防止长序列主导。
  - 权重退火：\(\lambda_t\) 从0线性增加到 \(\lambda\)，让重建先稳定。
  - 多样性惩罚：\(L_{\text{div}} = \log K - H(\bar{\rho})\)，鼓励均匀使用代码本。
- **三阶段训练**：
  1. 独立预训练VQ-VAE。
  2. 联合训练VQ-VAE + gradCSCG（软发射）。
  3. 冻结编码器，用硬发射微调gradCSCG（可加转移熵正则化）。
- **地图读取**：
  - Viterbi解码得到最可能状态路径 \(s^*_{1:T}\)。
  - 将每个克隆状态映射到多数物理位置 \(\chi(s)\)。
  - 通过Viterbi路径上高频出现的边（>0.2%步数）提取邻接图。

## 3. 实验设计

### 数据集/场景
- 四个基于网格世界的导航环境，每个单元格分配一个MNIST数字类别（0-9）：
  - **aliased**：4×4网格，4个数字类别，每类重复4次，强混淆。
  - **corridors**：5×5网格，墙壁形成走廊，6个数字，瓶颈结构。
  - **room**：6×6网格，10个数字，内部4×4区域全是同一数字（高度混淆）。
  - **two_rooms**：13×9网格，87个可走位置，10个数字，两个房间共享一个3×3区域（最复杂）。
- 每个环境收集4-10个episode，每episode 10,000步，随机游走。
- 观测模型分为两种：
  - **符号型**（控制实验）：直接使用数字标签作为观测。
  - **图像型**：每次访问一个位置，从MNIST中采样该数字的新图像。

### 基准（Benchmark）
- 主要对比原始CSCG（EM训练）在符号输入上的表现，验证梯度训练能否复现。
- 没有与其它深度学习认知地图方法（如Dedieu等人的Transformer、de Tinguy的动态扩展地图）进行定量对比，但在Related Work中进行了定性讨论。

### 对比方法
- 与原始CSCG进行行为对比如图3（符号实验）。
- 与自身变体（如不使用损失平衡）进行消融，但文中未详细报告消融数值，仅定性描述“naive训练会导致代码本崩溃”。

## 4. 资源与算力

- **文中未明确说明使用的GPU型号、数量、训练时长**。
- 提到训练在TensorFlow中进行，使用Adam优化器，VQ-VAE/联合学习率3×10⁻⁴，最终阶段学习率10⁻²，批次大小4（联合阶段）或8（最终阶段），联合迭代2000-5000步，最终阶段1000-5000步。
- 未提供具体硬件信息（如NVIDIA V100/A100等）和实际时间消耗。

## 5. 实验数量与充分性

### 实验数量
- 四个环境 × 两种观测模型（符号/图像），共至少8组主要实验结果。
- 每个环境重复多次运行（报告均值和标准差，标准差≤0.03）。
- 消融：训练阶段分析、损失平衡的作用（图6显示训练曲线）、Viterbi路径阈值分析（表4）。
- 额外实验：手动设置每个观测的克隆预算（room环境）。

### 充分性与公平性
- **充分性**：实验覆盖了不同规模（16-87个位置）、不同混淆程度（从4类到10类数字）的环境，且包含符号控制实验，验证了从图像恢复地图的能力。评估指标全面（准确率、召回率、F1、纯度、动作预测准确率）。
- **客观性**：使用标准MNIST数据集，环境设计透明，随机游走确保探索充分。
- **公平性**：与原始CSCG进行定性对比，但未与近期端到端认知地图方法（如Dedieu 2024, de Tinguy 2024）进行定量比较，这是一个不足。此外，没有在真实机器人或3D场景中测试。

## 6. 论文的主要结论与发现

1. **梯度训练复现原始CSCG**：gradCSCG通过梯度下降在符号网格世界上恢复了与EM训练相同的拓扑图（房间、走廊、重叠房间），克隆状态正确拆分了混淆观测。
2. **端到端图像地图恢复**：VQ-VAE + gradCSCG联合训练后，能从MNIST图像序列中恢复所有环境的邻接图，召回率达1.00，F1最高达1.00（表3、表4）。
3. **克隆纯度与动作预测**：状态-位置纯度≥0.95，动作预测准确率≥0.97。
4. **Viterbi路径读取优于投影图**：对投影图进行简单阈值会引入弱假边，而Viterbi路径高频边读取可获得高精度（0.95-1.00）且不牺牲召回率。
5. **损失平衡机制是关键**：长度归一化、权重退火、多样性惩罚共同防止了代码本崩溃，使联合训练稳定。

## 7. 优点（方法或实验设计上的亮点）

- **完全可微性**：将CSCG改造为可微模块，允许与神经网络端到端联合训练，解决了传统CSCG无法处理原始感知的瓶颈。
- **软发射耦合**：通过温度控制的后验实现从序列似然到编码器的梯度回流，巧妙连接离散序列模型与连续感知。
- **三阶段训练策略**：预热→联合→冻结微调，平衡重建与序列学习，实用有效。
- **损失平衡机制**：长度归一化、退火、多样性惩罚、抗崩溃措施，是联合训练稳定的关键工程贡献。
- **清晰的地图评估**：不仅报告F1，还区分投影图和Viterbi路径两种读取方式，并给出阈值分析，评估更严谨。
- **开放的代码与数据**：提供完整实现、环境和评估套件，可复现性强。

## 8. 不足与局限

- **实验范围有限**：仅限于2D网格世界和MNIST图像，未在3D、部分可观测、非平稳或视觉更丰富的场景中验证。
- **与SOTA方法缺乏定量比较**：未与Dedieu等人（Transformer认知地图）或de Tinguy等人（动态扩展地图）进行公平对比，仅定性讨论，削弱了竞争力证明。
- **阈值读取依赖人工设定**：Viterbi路径的高精度依赖于0.2%步数阈值，虽然作者论证其自适应，但并非完全无参数。
- **静态克隆预算**：每个令牌的克隆数量是固定超参数（手动设置），不能动态增加或减少，限制了在未知环境中的适应性。
- **未明确计算资源**：缺少GPU型号和训练时间的报告，影响实用性评估。
- **感知前端限制**：VQ-VAE代码本大小较小（4-10），仅适用于简单MNIST，扩展到高分辨率复杂图像可能面临挑战。

（完）
