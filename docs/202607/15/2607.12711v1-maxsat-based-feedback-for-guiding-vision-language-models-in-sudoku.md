---
title: MaxSAT-Based Feedback for Guiding Vision-Language Models in Sudoku
title_zh: 基于MaxSAT的反馈引导视觉-语言模型解决数独问题
authors: "Pedro Orvalho, Guillem Alenyà, Felip Manyà"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12711v1"
tags: ["query:ns-xai"]
score: 9.0
evidence: 整合MaxSAT的神经符号方法，在视觉语言模型中进行约束推理
tldr: 视觉语言模型在数独等网格推理任务中常产生违反逻辑约束的答案。本文提出神经符号方法，引入最大可满足性求解器作为一致性检验与优化模块：将模型生成的候选赋值编码为软子句，数独规则作为硬子句，求解最大一致子集，进而生成文本与视觉反馈引导模型迭代修正。在多个开源与闭源VLM上实验表明，该方法显著提升逻辑一致性和求解成功数。该工作展示了符号优化增强视觉语言模型推理可靠性的潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12711v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1062, \"height\": 442, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.12711v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1271, \"height\": 516, \"label\": \"Table\"}]"
motivation: 解决VLM缺乏显式逻辑一致性机制，导致结构化推理任务中频繁违反约束的问题。
method: 将VLM候选赋值编码为MaxSAT软子句，数独规则为硬子句，求解最大一致子集并转化为反馈。
result: 在多个VLM上实验，MaxSAT反馈提升了逻辑一致性和成功求解数，尤其在全盘修正模式下。
conclusion: 符号优化可有效增强视觉语言模型在结构化推理中的可靠性与一致性。
---

## 摘要
视觉-语言模型（VLM）最近在结构化视觉推理任务（包括基于网格的谜题）中展现出有前景的性能。然而，尽管具有强大的感知能力，这些模型缺乏强制执行逻辑一致性的显式机制，并且经常生成违反底层约束的赋值。在本文中，我们提出了一种神经符号方法，通过最大可满足性（MaxSAT）预言机将形式化约束推理集成到VLM求解过程中。符号组件并非直接计算解，而是充当一致性验证器和细化引擎。VLM生成的候选放置被编码为部分MaxSAT公式中的软子句，而数独约束保持为硬子句。当出现不一致时，MaxSAT求解器识别出最大的相互一致的赋值子集，然后将其转化为结构化的文本和视觉反馈，以指导后续的细化。我们在一个数独数据集上，针对多个开源和闭源VLM评估了我们的方法。结果表明，基于MaxSAT的反馈提高了逻辑一致性，并增加了求解实例的数量，特别是在全板细化模式下。这些发现表明，符号优化可以增强视觉-语言推理的可靠性。

## Abstract
Vision--Language Models (VLMs) have recently demonstrated promising performance on structured visual reasoning tasks, including grid-based puzzles. However, despite strong perceptual capabilities, these models lack explicit mechanisms for enforcing logical consistency and frequently generate assignments that violate underlying constraints. In this paper, we propose a neuro-symbolic approach that integrates formal constraint reasoning into the VLM solving process via a Maximum Satisfiability (MaxSAT) oracle. Rather than computing solutions directly, the symbolic component acts as a consistency validator and refinement engine. Candidate placements generated by the VLM are encoded as soft clauses in a partial MaxSAT formulation, while Sudoku constraints remain hard clauses. When inconsistencies arise, the MaxSAT solver identifies a largest mutually consistent subset of assignments, which is then translated into structured textual and visual feedback to guide subsequent refinements. We evaluate our approach on a Sudoku dataset across multiple open-source and closed-access VLMs. Results show that MaxSAT-based feedback improves logical consistency and increases the number of solved instances, particularly in full-board refinement mode. These findings demonstrate that symbolic optimisation can enhance the reliability of vision-language reasoning.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：视觉-语言模型（VLM）在结构化视觉推理任务（如网格谜题）中展现出一定能力，但缺乏显式的逻辑一致性机制，常依赖模式识别生成违反底层约束的赋值，导致输出不稳定、不可靠。
- **核心问题**：能否将形式化约束优化（MaxSAT）集成到VLM求解过程中，利用符号推理引导VLM产生逻辑一致的解，从而提升其在数独等结构化推理任务上的可靠性。
- **整体含义**：本文提出一种神经符号交互框架，通过MaxSAT预言机验证和优化VLM的输出，将模型生成的候选放置视为软约束，数独规则视为硬约束，通过求解最大一致子集并生成文本/视觉反馈，引导模型迭代修正。该方法不追求超越专用符号求解器，而是探索符号优化能否增强通用VLM的推理可靠性。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：利用部分MaxSAT问题将VLM输出视为软约束，数独的完备约束（行、列、3×3子格、每格唯一数字）作为硬约束，求解器选择最大一致软子集，将不一致的放置转化为结构化反馈（文本+视觉高亮），驱动VLM逐步修正。
- **关键技术细节**：
  - **编码**：采用标准SAT编码，引入布尔变量 \( X_{r,c,d} \) 表示数字d放在(r,c)格；CNF编码每个“恰好一个”约束使用pairwise编码。
  - **MaxSAT公式**：硬子句 \(\Phi_h\) 为数独所有约束的CNF；软子句 \(\Phi_s\) 为VLM提议的放置对应的单元子句（如 \( X_{r,c,d} \)）。求解目标：满足所有硬子句，最小化违反的软子句数。
  - **交互协议**：两种模式：
    - **逐步求解（Step-by-Step）**：每一步VLM提议一个放置，MaxSAT检查是否与已有硬约束和之前接受的软约束一致，不一致则拒绝并提供反馈。
    - **全板修正（Full-Board Refinement）**：VLM一次提出完整棋盘，MaxSAT找出最大一致子集，告知冲突位置，VLM迭代修正。
  - **反馈形式**：文本描述不一致放置，并可视化高亮冲突单元格。

## 3. 实验设计：数据集、基准、对比方法

- **数据集**：HuggingFace上的数独数据集（Ritvik19/Sudoku-Dataset），包含9×9数独，附有难度标签。本文只选取难度0和1（易与中等），每个难度均匀采样100道（固定随机种子42），共200例。
- **基准**：无符号辅助下的VLM有效性反馈（仅文本“逻辑错误”），对比MaxSAT反馈（文本+冲突单元格高亮）。
- **对比方法**：
  - 三个VLM：闭源GPT-5.5（2026版）；开源：Qwen3-VL（32B, 2025）和Molmo2（4B, 2026）。
  - 两种交互模式（逐步 vs. 全板） × 两种反馈模式（仅有效性 vs. MaxSAT反馈），共4种配置。
- **评估指标**：求解数（#Solved）和平均完整度（Average Completeness, AC，即最终棋盘与真实解匹配的单元格比例）。

## 4. 资源与算力

- **硬件**：NVIDIA A100-SXM4 GPU 40GB，128GB RAM。
- **时间限制**：每次VLM交互5分钟，整个谜题最多30分钟。
- **MaxSAT求解器**：RC2（PySAT实现）。
- **训练开销**：文中未提及模型训练，仅进行推理和交互。也未说明使用的GPU数量（至少1个A100）。

## 5. 实验数量与充分性

- **实验数量**：3个VLM × 2个难度 × 2种交互模式 × 2种反馈= 24组实验（每组100道题）。此外还有逐步与全板两种比较，共覆盖200道题。但并未对不同难度进行消融超参数（如温度、提示变体）或多轮反馈次数分析。
- **充分性与公平性**：
  - 优点：固定随机种子、相同交互协议、温度设为0、无上下文历史（仅保留当前谜题解决过程），确保了比较公平。
  - 局限：数据集仅含两个难度（0,1），未测试更难的2+级别；未与纯符号求解器（如直接SAT求解）做对比（作者明确表示意图不是超越符号求解器，而是提升VLM）；没有对反馈格式/长度进行系统消融；各模型参数规模差异大（GPT-5.5未知，Qwen3-VL 32B，Molmo2 4B），但未控制推理预算或成本。整体实验数量尚可，但消融不够全面。

## 6. 论文的主要结论与发现

- **RQ1**：无符号辅助时，VLM常产生不完整或违规的放置，GPT-5.5表现最好但仍有限；开源模型更差。
- **RQ2**：MaxSAT反馈显著提升求解数和完整度。对GPT-5.5，全板模式下求解数从45增至73（+62%），完整度从72.0%升至80.7%；困难题（Diff.1）求解数翻倍（10→21）。
- **RQ3**：闭源GPT-5.5远优于开源模型；但开源模型也从MaxSAT反馈中受益。
- **RQ4**：难度0整体性能高于难度1，但MaxSAT反馈能缩小差距，尤其对GPT-5.5。
- **总体**：神经符号交互能有效提高VLM在结构化视觉推理中的逻辑一致性和鲁棒性，尤其在VLM生成接近全局一致但局部错误的全板解时，符号优化纠正效果显著。

## 7. 优点

- **新颖的交互设计**：将MaxSAT作为一致性验证与优化器，而非直接求解器，保留了VLM的感知和生成能力。
- **反馈形式丰富**：文本+视觉高亮，直观可解释。
- **统一的框架**：逐步和全板两种模式共享同一MaxSAT公式，灵活适应不同VLM能力。
- **实验公平**：固定种子、温度0、一致提示格式，排除了随机和提示误差。
- **开源与闭源对比**：揭示了当前VLM在结构化推理上的性能差距及符号辅助的普适性。

## 8. 不足与局限

- **实验覆盖有限**：仅测试两个难度（0,1），未包括更难的6×6或更大尺寸；数据集规模较小（200道）。
- **无成本/效率分析**：未讨论MaxSAT求解的时间开销（尽管RC2很快，但交互循环可能因多次反馈而累积）；未比较纯符号求解器的绝对性能。
- **模型规模不均衡**：GPT-5.5参数未知，但推测远大于Qwen3-VL和Molmo2，比较不够公平。
- **未系统消融反馈设计**：仅对比了“仅文本”与“文本+视觉”，未测试反馈粒度（如只给冲突数量）、反馈轮数上限等。
- **潜在偏差**：选择Molmo2和Qwen3-VL可能不代表所有开源VLM；GPT-5.5版本为2026年，后续版本可能不同。
- **缺少定性分析**：未展示失败案例或反馈如何影响VLM行为的具体示例。

（完）
