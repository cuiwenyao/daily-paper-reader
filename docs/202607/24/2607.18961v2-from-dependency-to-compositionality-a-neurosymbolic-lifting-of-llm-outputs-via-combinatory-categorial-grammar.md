---
title: "From Dependency to Compositionality: A Neurosymbolic Lifting of LLM Outputs via Combinatory Categorial Grammar"
title_zh: 从依存到组合性：通过组合范畴语法提升LLM输出的神经符号方法
authors: Remo Pareschi
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.18961v2"
tags: ["query:ns-xai"]
score: 10.0
evidence: 通过组合范畴语法对LLM输出进行神经符号提升
tldr: 针对LLM自回归生成缺乏严格语法的问题，本文发现其前缀驱动、类型补全的动态与Combinatory Categorial Grammar(CCG)的增量处理模型一致。据此提出神经符号框架，将LLM输出提升为可审计的CCG组合推导，并通过Curry-Howard对应将方法扩展到Solidity、SQL等形式语言。框架支持两层检查：组合层捕获结构性错误，内容层结合外部知识检测幻觉。该工作为LLM输出提供了原则性的组合语义重建，开辟了同步耦合新方向。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.18961v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1504, \"height\": 324, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18961v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 709, \"height\": 717, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18961v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1330, \"height\": 538, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18961v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1612, \"height\": 837, \"label\": \"Table\"}]"
motivation: 自回归LLM的前缀驱动生成动态与CCG的增量组合模型高度吻合，但现有研究未充分认识并利用这一对应关系。
method: 提出神经符号框架，将LLM输出通过CCG重建为类型化组合推导，并利用Curry-Howard对应推广至形式语言，实现跨领域应用。
result: 框架实现两层校验：组合层直接捕获结构失败，内容层对照外部知识源提前发现幻觉内容，提升输出可靠性。
conclusion: 展示了LLM输出可被审计地重构为组合语义，为可靠生成与形式验证提供新路径，并指出同步耦合的潜在方向。
---

## 摘要
大型语言模型通过从前缀逐步预测下一个标记来生成流畅文本。生成传统中的批评者认为此类系统缺乏真正的语法；依存语法视角的有影响力的回应则认为，LLM行为可以通过逐词构建的局部中心词-依存结构得到良好描述。我们认为一个更敏锐的观察被忽视了：自回归生成的前缀驱动、类型补全动态与组合范畴语法最初设计支持的增量处理模型高度一致。在此基础上，我们提出了一个神经符号框架，其中LLM输出被提升为类型化的组合推导——并非声称LLM内部实现了CCG，而是说它们的输出允许一种有原则的、增量的、可审计的CCG重建。两个结果随之而来。首先，通过Curry-Howard对应，这种提升超越了自然语言，扩展到LLM也产生的形式语言——编程语言如Solidity、描述逻辑和查询语言如OWL和SQL——类型系统变化而架构保持不变。其次，这种提升支持两层检查：一个组合层直接捕获结构失败，一个内容层根据外部知识源检查提升后的结构，从而实现尽早标记幻觉内容。因此，这种解释对生产者要求的不是认知，而是前缀驱动的生成剖面。最后，我们概述了同步LLM-CCG耦合作为该框架开辟的一个方向。

## Abstract
Large language models (LLMs) generate fluent text by incrementally predicting the next token from a prefix. Critics in the generative tradition argue that such systems lack genuine grammar; influential replies from the dependency-grammar perspective hold that LLM behavior is well described by local head-dependent structure built word by word. We argue that a sharper observation has been overlooked: the prefix-driven, type-completing dynamics of autoregressive generation align closely with the incremental processing model that Combinatory Categorial Grammar (CCG) was originally designed to support. On this basis we propose a neurosymbolic framework in which LLM outputs are lifted into typed compositional derivations -- not claiming that LLMs implement CCG internally, but that their outputs admit a principled, incremental, and auditable CCG reconstruction. Two consequences follow. First, through the Curry-Howard correspondence the lifting extends beyond natural language to the formal languages LLMs also produce -- programming languages such as Solidity, description-logic and query languages such as OWL and SQL -- with the type system varying and the architecture held fixed. Second, the lifting supports two layers of checking: a compositional layer that catches structural failures directly, and a content layer that checks the lifted structure against external knowledge sources, enabling the earliest possible flagging of hallucinated content. The account thereby requires of a producer not cognition but a prefix-driven generative profile. We close with a sketch of synchronous LLM-CCG coupling as one direction the framework opens.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，以下是对您提供的论文《From Dependency to Compositionality: A Neurosymbolic Lifting of LLM Outputs via Combinatory Categorial Grammar》的结构化、深入且客观的中文总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）

*   **研究动机：** 论文的核心动机是解决大型语言模型在生成文本时缺乏“真正的语法”和“可验证的组合语义”这一根本性问题。尽管LLM能生成流畅文本，但其自回归的、基于统计预测的生成过程常被批评为“无语法”。主流的依存语法视角虽能描述其局部结构，却未能捕捉到生成过程中的“预测性”和“类型补全”这一核心特征。
*   **整体含义：** 论文提出，LLM的前缀驱动、逐步补全的动态过程，与**组合范畴语法**（CCG）最初设计的增量处理模型高度吻合。基于此，论文主张不应声称LLM内部实现了CCG，而是可以**将LLM的输出“提升”（Lifting）为一种有原则的、增量的、可审计的CCG组合推导**。这并非为了解释LLM，而是为了以神经符号的方式，为其输出赋予可验证的组合语义，从而有效检测内容缺陷（如幻觉），并将这一方法论从自然语言扩展到LLM能生成的各类形式语言（如编程语言、查询语言）。

### 2. 论文提出的方法论：核心思想、关键技术细节

*   **核心思想：** 提出一个“后处理”（post-hoc）的神经符号框架，将LLM生成的文本通过CCG解析器重建为类型化的组合推导（Lambda表达式），实现从“统计依赖”到“组合意义”的认知提升。
*   **关键技术细节与流程：**
    1.  **双层的提升框架：**
        *   **生成层（Neural Generation）：** LLM以自回归方式生成token序列。
        *   **组合CCG层（Compositional CCG Layer）：** 使用CCG语义解析器（如ccg2lambda）对生成的文本进行解析，产出包含组合推导和Lambda演算项的表示。这一层能够回答“意义如何组合”以及“已组合部分的类型是什么”这两个关键问题。
    2.  **增量锁步提升（Incremental Lifting）：** 利用CCG的左到右、前缀驱动的特性（如类型提升和组合），使得**每个前缀**（例如“John loves”）都可以被类型化（如S/NP）并拥有一个部分Lambda项（如λy. love(john, y)）。这使得符号层的“类型化预期”与LLM的“统计预期”在每个生成步骤中都能对齐。
    3.  **两层检查机制：**
        *   **组合层（Compositional Layer）：** 检查结构失败，例如参数缺失或类型不匹配。这是由CCG推导本身决定的。
        *   **内容层（Content Layer）：** 检查内容失败，例如事实性错误（如“阿兹特克神宙斯”）。这一层将CCG提升产生的结构化表示（如谓词 `aztec(zeus)`）查询外部知识源（如词汇本体）。**CCG的增量性允许在句子完成之前（甚至在谓语出现之前）就进行内容检查，实现“尽早标记”。**
    4.  **跨领域扩展：** 通过**Curry-Howard对应（命题即类型）**，该方法论从自然语言扩展到形式语言。
        *   **核心观点：** 对LLM输出的CCG类型化提升是一种通用的“增量类型检查”操作。
        *   **应用场景：** 对于Solidity（智能合约）语言，其公式化的**行为契约**可以用类型系统（如Precondition & Effect）来描述。LLM生成的缺失`require`或事件`emit`的合约代码，其提升后的**类型**（如 `[φ_auth]. State`）会比预期的合约类型（如 `[φ_auth ∧ φ_val]. (State ⊗ Event)`）弱，从而在类型层面暴露结构缺陷。

### 3. 实验设计

*   **数据集与场景：** 论文本身是一篇**概念性、方法论导向**的文章，没有运行大型、端到端的实验来验证整个框架。
    *   **自然语言场景：** 使用了简单的句子（如“John loves Mary”）和构造的幻觉案例（如“The Aztec god Zeus...”, “The brother of Esau was Elijah”）作为**概念演示**，展示增量提升和内容检查的工作流程。
    *   **形式语言场景（主要案例）：** 重点关注**Solidity智能合约**的生成。引用了Salzano et al. (2026) 的**实证研究**作为问题背景，该研究显示当前LLM在零样本生成Solidity合约时的功能正确率仅为20-26%。
*   **基准（Benchmark）：** 没有明确的基准任务或数据集。论文的概念演示和形式化论证没有与现有方法（如FActScore）进行定量对比，而是通过定性分析指出其优势。
*   **对比方法：**
    *   **与依存语法对比：** 认为依存语法描述了“已链接的结构”，而CCG描述了“还缺少什么及其类型”，从而捕获了生成驱动的预测性。
    *   **与原子事实验证（FActScore）对比：** 指出CCG提升是一种有类型的、增量的替代方案，其优势在于保留了谓词的蕴含上下文，而不是将其分解为无关的独立事实。
    *   **与检索增强生成（RAG）对比：** 认为在RAG中，检索到的内容是以统计方式影响生成，而CCG提升能产生结构化对象，使得内容可以以组合方式被检查。

### 4. 资源与算力

*   **明确说明：** 论文**没有**进行需要大规模算力的实验。作为一个概念性框架，它**未提及**任何关于GPU型号、数量或训练时长的信息。

### 5. 实验数量与充分性

*   **实验数量：** 论文**没有进行任何定量实验**。它仅提供了零星的、示意性的定性案例（如“John loves Mary”的增量表，`setMinDepositAmount`函数的类型对比）。
*   **充分性分析：**
    *   **不充分：** 实验的缺乏是该论文最显著的局限性之一。提出的核心论点——CCG提升能有效检查LLM输出——在目前阶段只是一个理论断言，缺乏任何经验证据的支持。
    *   **不够客观公平：** 由于没有实验，无法评估该方法在复杂、真实场景下的鲁棒性、准确率或效率。与现有方法的优劣无法进行公平比较。

### 6. 论文的主要结论与发现

1.  **深层对齐的发现：** LLM的**前缀驱动、类型补全**的动态与CCG的**增量处理模型**之间存在着被忽视但至关重要的结构收敛。
2.  **有效提升的可行性：** LLM的输出可以（在事后）被原则性地、增量地、可审计地重建为CCG的组合推导，从而恢复其组合语义。
3.  **跨领域应用潜力：** 通过Curry-Howard对应，该提升方法可以统一应用于自然语言和程序代码等形式语言，为LLM生成的各种输出提供统一的验证框架。
4.  **两级检查的价值：** 组合层能捕捉结构缺陷，而内容层能在**生成过程中尽早**（在句子完成前）标记幻觉内容，这比事后检查更高效。
5.  **对“生产者”概念的澄清：** 该框架将重心从“认知”转向“生产剖面”（前缀驱动），认为LLM不需要拥有“心智”即可成为可验证组合语义的有效生产目标。

### 7. 优点：方法或实验设计上的亮点

*   **理论创新性高：** 敏锐地发现了LLM的生成动态与CCG形式主义之间的深层联系，这一视角极具原创性，为该领域提供了一个全新的、有启发性的研究方向。
*   **框架设计优雅：** 框架设计具有“分层谦逊”（layered modesty）的特点，不强行断言LLM内部如何工作，而是关注其输出的可重建性。其“两层检查”和“类型即接口”的构想非常精巧，为解决幻觉难题提供了清晰的蓝图。
*   **跨领域统一性：** 通过Curry-Howard对应，该方法论成功地将自然语言处理和程序验证/形式化方法联系起来，展现出强大的统一潜力，这是其最重要的理论贡献之一。
*   **前瞻性：** 提出了**同步LLM-CCG耦合**的未来方向，该设想即使目前只是草图，也指明了从“事后检测”转向“事前预防”的更强大路径。

### 8. 不足与局限

*   **缺乏实验验证：** 这是最致命的弱点。整篇论文都是理论构建，没有提供任何经验证据证明其提出的框架在实际任务中（如幻觉检测、代码生成验证）的有效性、效率和鲁棒性。
*   **实施依赖性强：** 框架的成功实施严重依赖于当前CCG解析器的性能，特别是其对不同领域、不同风格和正式程度的LLM输出文本的泛化能力。作者虽提及超标注器的泛化能力，但这是否足以应对真实世界LLM输出仍有巨大疑问。
*   **未解决核心复杂问题：** 论文坦诚承认了许多未解决的问题，如多句篇章结构、指代消解、语用依赖和歧义处理。这恰恰是该框架在真实应用中必须克服的核心挑战。
*   **应用边界模糊：** 提出将CCG提升应用于Solidity代码，但这需要**设计全新的针对编程语言效果和义务的类型代数**，并重新构建解析器，其工作量不亚于从零开始创建一套代码验证工具。论文对此的论述过于乐观，低估了工程实现的复杂性。
*   **定位不够清晰：** 该工作既是理论语言学论文，也是AI论文，但最终可能两边都不讨好。语言学家可能认为其CCG分析不够深入，AI研究者可能认为其缺乏实验而难以信服。

（完）
