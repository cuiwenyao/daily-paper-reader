---
title: Explainable and Resource-Efficient Spatial Reasoning in Multimodal LLMs for Decision-Critical Applications
title_zh: 面向决策关键应用的多模态大语言模型中可解释且资源高效的空间推理
authors: "Piyush Jain, Kousik Dasgupta, Rajarshi Roy, Subarna Tripathi"
date: 2026-07-29
pdf: "https://arxiv.org/pdf/2607.27145v1"
tags: ["query:ns-xai"]
score: 9.0
evidence: 多模态大语言模型中可解释且资源高效的空间推理
tldr: "多模态大模型在决策关键场景的空间推理需要可解释性和资源效率。本文提出ByDeWay-V2，无需训练，通过开放词汇检测器计算物体间几何关系并注入为结构化空间谓词，增强深度提示。在BLINK空间子集上相对LDP提升46% F1，在VSR上从随机恢复至0.53。轻量配置仅需CPU上40 token，适合实时决策支持。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.27145v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1255, \"height\": 1080, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27145v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1078, \"height\": 727, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.27145v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1177, \"height\": 376, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27145v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1061, \"height\": 374, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27145v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1097, \"height\": 375, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27145v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 948, \"height\": 436, \"label\": \"Table\"}]"
motivation: 现有MLLM空间推理细粒度不足且缺乏可审计证据，粗深度分层无法解决同平面物体关系。
method: 集成开放词汇检测器计算物体间几何关系，并作为结构化空间谓词注入MLLM提示，桥接3D深度与2D语义。
result: "BLINK空间子集相对LDP提升46% F1，VSR上从随机恢复至F1=0.53，CPU下40 token预算运行。"
conclusion: 无训练注入显式空间关系可提升MLLM空间推理的可解释性与资源效率，适用于实时决策场景。
---

## 摘要
随着多模态大语言模型（MLLMs）在机器人、具身AI和安全监控等决策关键流程中的日益部署，其空间判断的不透明性限制了操作员的信任和可审计性。MLLMs展现出强大的推理能力，但在细粒度空间理解和对象幻觉方面往往存在不足。先前的工作ByDeWay引入了基于分层深度提示（LDP），这是一个无需训练的框架，通过使用单目深度估计构建提示来减轻幻觉。然而，粗粒度的深度分层在解决同一几何平面内的对象间空间关系（如投影关系“左侧”、“上方”和拓扑关系“内部”、“接触”）方面存在不足。我们提出了ByDeWay-V2，它将显式的空间关系上下文与深度线索相结合，表示为人类可读的谓词，作为下游决策支持的可审计证据。使用开放词汇目标检测器（YOLO-World-L），我们的框架计算检测对象之间的逐对几何关系，并将其作为结构化空间谓词注入MLLM提示中，无需任何训练即可桥接3D场景深度和2D空间语义。我们在多个MLLM上评估了ByDeWay-V2在视觉空间推理（VSR）和BLINK基准上的表现，并通过POPE评估幻觉基础。在BLINK空间子集上，ByDeWay-V2相对于LDP在Qwen2.5-VL上实现了46%的相对F1改进，并将BLIP-Base在VSR上的空间推理从接近随机性能恢复至具有竞争力的F1值0.53。我们最轻量级的配置在CPU上严格限制在40个token的上下文预算内运行，表明该框架适用于资源受限的实时决策支持场景。

## Abstract
As Multimodal Large Language Models (MLLMs) are increasingly deployed in decision-critical pipelines such as robotics, embodied AI, and safety monitoring, the opacity of their spatial judgments limits operator trust and auditability. MLLMs demonstrate strong reasoning but often struggle with fine-grained spatial understanding and object hallucination. Prior work, ByDeWay, introduced Layered-Depth-Based Prompting (LDP), a training-free framework that mitigates hallucinations by structuring prompts using monocular depth estimation. However, coarse depth layering falls short in resolving object-to-object spatial relationships within the same geometric plane, such as projective ("left of", "above") and topological ("inside", "touching") relations. We propose ByDeWay-V2, which integrates explicit spatial relational context alongside depth cues, expressed as human-readable predicates that serve as auditable evidence for downstream decision support. Using an open-vocabulary object detector (YOLO-World-L), our framework computes pairwise geometric relations between detected objects and injects them as structured spatial predicates into the MLLM prompt, bridging 3D scene depth and 2D spatial semantics without any training. We evaluate ByDeWay-V2 on the Visual Spatial Reasoning (VSR) and BLINK benchmarks across multiple MLLMs, with hallucination grounding assessed via POPE. On the BLINK spatial subset, ByDeWay-V2 achieves a 46 percent relative F1 improvement over LDP for Qwen2.5-VL, and recovers BLIP-Base's spatial reasoning on VSR from near-random performance to a competitive F1 of 0.53. Our lightest configuration operates under a strict 40-token context budget on CPU, showing the framework's suitability for resource-constrained, real-time decision-support settings.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：多模态大语言模型（MLLMs）在机器人、具身AI和安全监控等决策关键场景中部署时，存在两个主要挑战：
  - **视觉幻觉**：MLLMs倾向于生成与视觉输入不一致的内容。
  - **细粒度空间推理不足**：模型难以准确解释同一平面内物体间的投影关系（如“左侧”、“上方”）和拓扑关系（如“内部”、“接触”）。
- **动机**：先前工作ByDeWay（V1）采用无需训练的分层深度提示（LDP），通过单目深度估计结构化解空间推理，但粗粒度的深度分层无法解决同深度层内物体间的精确关系。此外，决策关键场景要求**可审计性**：操作员需要可读的证据，而非黑箱输出。
- **背景**：现有训练免费的幻觉缓解方法主要提升准确率，但缺乏中间可读证据。论文旨在提出一个既提升空间推理准确性又产生显式、可检查谓词的框架。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

### 核心思想
- 在LDP基础上，增加一个**SpatialAnalyser模块**，通过开放词汇目标检测器（YOLO-World-L）提取显式的几何空间关系，并以人类可读的谓词形式注入MLLM提示中，桥接3D深度组织与2D空间语义。

### 关键技术细节
1. **阶段一：分层深度提示（LDP）**（沿用V1）
   - 使用DepthAnything V2生成逆深度图，按30%和70%分位数将图像分割为最近、中距、最远三个掩码区域。
   - 每个区域由KOSMOS-2生成自然语言描述，拼接成深度上下文字符串 \(C_{LDP}\)。

2. **阶段二：SpatialAnalyser模块**
   - **开放词汇目标检测**：YOLO-World-L从输入图像中检测物体，返回边界框 \((b_i, c_i)\)，其中 \(b_i = (x_\ell, y_t, x_r, y_b)\)。
   - **成对几何关系计算**：对于每对有序检测 \((i, j)\)，计算：
     - 中心坐标 \(cx_i = (x_\ell + x_r)/2\), \(cy_i = (y_t + y_b)/2\)
     - 垂直/水平关系：上/下、左/右（基于中心坐标比较）
     - 包含与接触：重叠比 \(OR = \text{Inter}(b_i, b_j) / \min(\text{Area}(b_i), \text{Area}(b_j))\)，阈值 \(\theta_{in}=0.70\) 判定“inside”，\(\theta_{touch}=0.10\) 判定“touching”
     - 邻近性：归一化中心距离 \(d_{ij} = \sqrt{(cx_i - cx_j)^2 + (cy_i - cy_j)^2} / \sqrt{W^2 + H^2}\)，阈值 \(\tau_{near}=0.30\) 判定“near”或“far from”
     - 优先级：包含 > 接触 > 垂直/水平 > 邻近
   - **自然语言序列化**：每个谓词渲染为句子 “the \(c_i\) is \(r\) the \(c_j\)”，拼接成空间上下文字符串 \(C_{Spatial}\)。

3. **混合提示构造**：最终提示 \(P = C_{LDP} \parallel C_{Spatial} \parallel Q\)，其中 \(Q\) 是用户查询。

### 算法流程（文字说明）
```
输入：图像I，用户查询Q
输出：最终提示P

1. 生成深度分层描述 C_LDP ← GenerateDepthCaptions(I)
2. 开放词汇检测 D ← YOLOWorldDetect(I, Q)
3. 初始化 C_Spatial = 空字符串
4. 对于每个有序对(i,j) in D:
   r ← ComputeGeometricRelation(i,j)
   C_Spatial ← C_Spatial + Serialise(c_i, r, c_j)
5. P = C_LDP + C_Spatial + Q
6. 返回P
```

## 3. 实验设计：数据集、benchmark、对比方法

### 数据集与基准
- **VSR (Visual Spatial Reasoning)**：10119个图像-标题对，覆盖7种空间关系（投影、拓扑、邻接、方向、接近、方向、未分类），用于空间关系真/假判断。
- **POPE (Polling-based Object Probing Evaluation)**：衡量物体存在性幻觉，使用150个样本的子集（源自ByDeWay V1原始评估）。
- **BLINK**：空间推理子集，测试相对位置（左/右/上/下）。

### 对比方法
- 基线：LDP（ByDeWay V1），仅使用深度层描述。
- 本文：LDP+Spatial（V2）添加YOLO-World-L空间谓词。
- 对于ViLT（严格40 token限制），使用“Spatial-Only”变体（仅注入空间谓词，省略深度描述）。

### 评估模型
- Qwen2.5-VL-7B（7B参数，最先进）
- BLIP-Base（约250M参数，轻量级零样本）
- ViLT-B/32（无需卷积，token高效，40 token限制）

### 评估指标
- 准确率、精确率、召回率、F1分数（对POPE还报告各分裂（对抗、流行、随机）的准确率）。

## 4. 资源与算力
- 论文明确说明：“所有实验在**无需训练**的设置下进行；未修改任何模型参数。”
- 具体算力：
  - Qwen2.5-VL推理在单块NVIDIA A100 GPU（40GB）上进行。
  - ViLT推理在CPU上进行。
- **未明确说明**：训练时长（因为无训练）、多GPU数量、总计算量等细节。仅提及“单GPU A100”和“CPU”。

## 5. 实验数量与充分性

### 实验数量
- 在**三个数据集**（VSR、POPE、BLINK）上进行评估，覆盖三个模型，共产生约9组主要结果（每个模型每个数据集对比LDP和V2）。
- 对VSR进行了**按类别分析**（7个空间关系类别），提供了BLIP-Base的逐类别F1。
- POPE评估了整体指标，并提及了每分裂准确率（但未全部列出）。
- 没有消融实验（如单独移除深度或空间谓词的效果、不同检测器的影响、阈值敏感性等），但论文将LDP和Spatial的组合视为整体；对比了“Spatial-Only”变体作为ViLT的特殊消融。

### 充分性评价
- **优点**：覆盖了从大型（Qwen2.5-VL）到小型（ViLT）的多尺度模型，验证了通用性；使用了三种不同难度的基准（空间推理、幻觉检测、相对位置）。
- **不足**：
  - 消融实验不够全面：例如未研究不同几何关系优先级的影响、检测器置信度阈值的影响。
  - 仅展示了一个子集（POPE小样本150例），不够标准化；未报告POPE标准三大分裂（对抗、流行、随机）的具体数字（虽然提及但未给出）。
  - 未在更多MLLMs（如LLaVA、GPT-4V）上验证。
  - 缺乏与有训练方法的对比（如SpatialVLM、指令微调模型）。
  - 客观上，实验设计总体公平，因为对比了同一模型的带/不带Spatial提示，控制变量。但未与最新的训练自由方法（如Set-of-Marks）比较。

## 6. 论文的主要结论与发现

1. **显式空间关系注入显著提升空间推理**：在BLINK空间子集，Qwen2.5-VL的F1从0.496提升至0.727（**46%相对提升**）；BLIP-Base在VSR上从近随机的F1=0.053提升至0.525（**近10倍提升**）。
2. **幻觉抵抗增强**：在POPE上，BLIP-Base的准确率从86%升至90.67%，F1从0.873升至0.919。
3. **轻量级可行性**：ViLT在40 token限制下，仅使用空间谓词（Spatial-Only）仍获得精确率提升（0.549→0.5747），表明紧凑谓词比深度描述更token高效。
4. **模型普遍性**：无需训练、模型无关，可直接应用于任何MLLM管线。
5. **精确率-召回率权衡**：对Qwen2.5-VL，V2虽提高精确率但降低召回率（因“视觉优先”指令导致模型忽视正确上下文），强调了对模型校准的开放性挑战。

## 7. 优点：方法或实验设计上的亮点

- **无需训练**：完全零参数更新，易于部署。
- **模块化设计**：SpatialAnalyser可独立替换或升级检测器。
- **可解释性**：输出显式的自然语言谓词，提供可审计的决策证据。
- **跨模型通用性**：在从7B到0.25B的不同规模模型上均有效。
- **资源意识**：通过分析ViLT的40 token限制，证明了紧凑谓词的高效性，适用于实时、低资源场景。
- **深度与几何互补**：巧妙结合3D深度信息和2D几何关系。

## 8. 不足与局限

- **检测器依赖**：YOLO-World-L的识别错误或漏检会直接导致错误/缺失谓词，影响后续推理。
- **阈值敏感**：几何关系计算中的阈值（θ_in、θ_touch、τ_near）为手动设定，未做敏感性分析。
- **视觉优先指令矛盾**：对Qwen2.5-VL使用“视觉优先”指示导致模型可能忽略正确上下文，削弱召回率；这一设计选择缺乏更精细的校准研究。
- **POPE评估规模小**：仅使用150样本子集，而非标准完整POPE（约3000+样本），结论推广性存疑。
- **缺乏标准基线对比**：未与Set-of-Marks、Grounding DINO等同样提供空间信息的训练自由方法比较；也未与有监督的SpatialVLM等比较泛化能力。
- **应用限制**：仅测试了静态图像场景，未在视频、动态环境或实时流中评估。实时性仅提及“CPU下40 token”，但未报告实际推理时间。

（完）
