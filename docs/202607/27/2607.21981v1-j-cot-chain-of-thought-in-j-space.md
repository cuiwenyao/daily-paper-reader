---
title: "J-CoT: Chain-of-Thought in J-Space"
title_zh: "J-CoT: J空间中的思维链"
authors: "Junde Wu, Jiayuan Zhu, Fengling Liu, Minhao Hu, Jiazhen Pan"
date: 2026-07-24
pdf: "https://arxiv.org/pdf/2607.21981v1"
tags: ["query:ns-xai"]
score: 8.0
evidence: 基于词汇索引连续空间的大语言模型递归推理框架J-CoT
tldr: 现有链式思维提示依赖自然语言接口，限制了推理灵活性；潜在推理方法传递连续隐状态，缺乏信息选择机制。J-CoT提出在J-space（词汇索引坐标系）中，将中间状态编码为词汇索引系数作为J-thought传递，再映射回隐空间，无需自然语言解码或完整状态循环。J-CoT-Zero在多个基准上匹配或超越最强潜在推理基线，J-CoT-Train在数学、科学、编码和路径推理任务中均取得最高分。该方法提供了语言基础但无需解码的中间接口，平衡了表达性与效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.21981v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1430, \"height\": 381, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21981v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1440, \"height\": 573, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21981v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1440, \"height\": 596, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.21981v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1452, \"height\": 429, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21981v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 718, \"height\": 247, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21981v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 721, \"height\": 227, \"label\": \"Table\"}]"
motivation: 现有推理方法受限于自然语言接口或缺乏信息选择，需要兼具语言基础与灵活性的中间接口。
method: J-CoT在词汇索引坐标系J-space中，将中间状态编码为稀疏系数传递，再映射回隐空间，形成J-thought。
result: J-CoT-Zero匹配或超越最强潜在推理基线，J-CoT-Train在所有评估任务中取得最高分。
conclusion: J-CoT提供了语言基础且无需完整解码的推理框架，显著提升了复杂推理任务的性能。
---

## 摘要
思维链提示通过跨连续计算步骤传递中间状态来改进语言模型的推理能力。然而，将自然语言作为唯一的循环接口过于局限，因为许多瞬态计算并不需要完全语言化。现有的潜在推理方法通过循环传播连续的隐藏状态来消除这一约束。然而，这些方法将密集的隐藏向量作为一个整体传递，缺乏明确的机制来选择和组织下一步推理所需的信息。这激发了一种中间接口，它仍然基于语言但不需要解码出完整句子。我们提出\textbf{J-CoT}，一个基于\emph{J-space}的循环推理框架，J-space是模型隐藏表示中基于词汇索引的坐标系。在每个循环内，模型在其完整的隐藏空间中计算。在循环边界处，J-CoT将中间状态表示为词汇索引的系数，将这些系数作为\emph{J-thought}向前传递，并在下一循环中映射回模型的隐藏表示。因此，J-CoT既不需要流畅的中间推理过程，也不需要对完整隐藏状态进行循环。在匹配的骨干和推理设置下，J-CoT-Zero在每个基准测试上达到或超过最强的已评估潜在推理基线，而J-CoT-Train在评估的数学、科学、编码和结构化路径推理任务中获得了最高分。

## Abstract
Chain-of-thought prompting improves language-model reasoning by carrying intermediate states across successive computation steps. However, relying on natural language as the only recurrent interface is overly restrictive, since many transient computations do not need to be fully verbalized. Existing latent-reasoning methods remove this constraint by recurrently propagating continuous hidden states. However, these methods pass a dense hidden vector as a whole, without an explicit mechanism for selecting and organizing the information needed by the next reasoning step. This motivates an intermediate interface that remains linguistically grounded without requiring a decoded sentence. We introduce \textbf{J-CoT}, a recurrent reasoning framework built on \emph{J-space}, a vocabulary-indexed coordinate system within the model's hidden representations. Within each cycle, the model computes in its full hidden space. At the cycle boundary, J-CoT expresses the intermediate state as vocabulary-indexed coefficients, carries these coefficients forward as a \emph{J-thought}, and maps them back into the model's hidden representation for the next cycle. J-CoT therefore requires neither a fluent intermediate rationale nor recurrence over the complete hidden state. Under matched backbone and inference settings, J-CoT-Zero matches or exceeds the strongest evaluated latent-reasoning baseline on every benchmark, while J-CoT-Train obtains the highest score across the evaluated mathematical, scientific, coding, and structured path-reasoning tasks.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：链式思维（Chain-of-Thought, CoT）提示通过生成中间自然语言推理步骤，显著提升了大语言模型的复杂推理能力。然而，自然语言作为唯一循环接口存在根本局限：许多瞬态计算不需要完全语言化，模型被迫将部分形成、不确定的分布式内部计算序列化为流畅的连续句子，浪费了容量且限制了可传递信息的形式。
- **现有解决方案**：潜在推理方法（如Coconut）通过循环传播连续的隐藏状态（密集向量）来消除语言约束，但这些方法将整个隐藏向量作为整体传递，**缺乏明确的机制来选择和组织下一步推理所需的信息**，导致无效信息残留。
- **本文目标**：提出一种介于显式CoT和密集潜在循环之间的**中间接口**，该接口具有语言基础（词汇索引）但无需解码为完整句子，支持在循环边界选择和压缩信息，同时保留循环内完整的隐藏空间计算能力。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：在模型的隐藏表示中构建一个**基于词汇索引的坐标系（J-space）**，在推理循环的边界处，将中间状态表示为词汇索引上的系数（称为**J-thought**），然后将该系数状态传递到下一循环，并映射回隐藏表示进行后续计算。
- **关键技术细节**：
  - **J-space字典构建**：对每一层 \( \ell \)，计算平均下游雅可比矩阵 \( J_\ell \)，将输出层的词汇方向（来自权重矩阵 \( W_U \)）通过 \( J_\ell \) 拉回到该层，形成字典 \( D_\ell \)。字典各列由词汇索引索引，且在不同层间共享索引空间。
  - **提取与重建**：提取使用非负弹性网分解（nonnegative elastic-net）：
    \[
    \Phi_\ell(h) = \arg\min_{a \ge 0} \frac{1}{2}\|h - D_\ell a\|_2^2 + \lambda_1\|a\|_1 + \frac{\lambda_2}{2}\|a\|_2^2
    \]
    重建为 \( \Psi_\ell(a) = D_\ell a \)。
  - **载波组织**：在输入末尾添加 \( M \) 个非语言载波位置（carrier positions），它们参与Transformer的注意力与FFN，但不参与语言建模目标。载波暴露一组激活用于读取/写入J-thought。
  - **循环机制**（公式(7)）：
    - 读取：前一个J-thought \( A_{t-1} \) 在读取层 \( \ell_r \) 重建，通过门控 \( G^\phi_r \) 与基线载波激活 \( B^{rx} \) 结合，得到 \( Cr_t \)。
    - 计算：经过Transformer块 \( F_{\theta,x}^{\ell_r:\ell_w} \) 得到 \( Zw_t \)。
    - 写入：在写入层 \( \ell_w \) 提取 \( A_t = \Phi_{\ell_w}(Zw_t) \)。
  - **自适应终止**：根据J-thought的归一化变化（式(8)）判断是否停止，条件为连续两次变化 < 0.02，最多8个循环。
- **两种配置**：
  - **J-CoT-Zero**：固定载波嵌入，单位读取强度，无需J-CoT特定训练。
  - **J-CoT-Train**：仅优化载波嵌入和共享读取门（两层MLP，瓶颈256），冻结Transformer和J-space字典。
- **理论保证**：命题1给出了J-thought跨层传输的稳定性界限，前提是字典设计使得提取-重建过程的恢复缺陷（\( \varepsilon_\ell(a) \)）小，从而保证系数状态在不同层间可复用。

### 3. 实验设计：使用的数据集/场景、Benchmark、对比方法

- **数据集与任务**：
  - 数学推理：GSM8K、MATH-500、AIME 2024
  - 科学问答：GPQA-Diamond
  - 代码推理：HumanEval+、MBPP+、LiveCodeBench、CRUXEval
- **主干模型**：主要实验使用 Qwen3-8B-Base（36层，隐藏维度4096）。缩放实验扩展到 Qwen2.5-7B/14B/32B 以及 Llama-3.1-70B/405B。
- **对比方法**：
  - 显式CoT：CoT（标准）、PS+（Plan-and-Solve）
  - 潜在推理：Coconut、CODI、SIM-Coconut
  - 所有方法基于相同的推理适应检查点（监督微调于混合数学、科学、编程问题），训练数据和使用方式保持一致。
- **评估设置**：贪婪解码，单次解答；数学/科学用精确匹配准确率，代码用pass@1（执行环境），CRUXEval用宏平均。超参数在开发集上统一选择，不在单独任务上调整。

### 4. 资源与算力

- **文中未明确说明使用的GPU型号、数量、训练时长等具体硬件信息**。只提到J-CoT-Train优化10,000步，使用AdamW，学习率2e-4，全局批大小128，最多16个循环。对于缩放实验，报告了推理FLOPs，但未提及训练资源。因此无法量化算力成本。

### 5. 实验数量与充分性

- **实验数量**：
  - 主结果表（表1）：8个基准 × 7种方法（含两个变体），共56个数据点（取3种子均值）。
  - 缩放实验（图2）：5个模型 × 3个循环预算，共15个配置，在2个基准上绘制。
  - 界面谱实验（图3）：9个λ值 × 1个任务（ProsQA），含分支因子和干扰路径变化。
  - 消融实验（表2）：两个面板各4个变体，共8个消融，在4个基准上取平均。
- **充分性与公平性**：
  - 所有方法共享相同的初始模型、训练数据、提示模板和解码配置，确保公平比较。
  - 报告3个种子的平均值和标准差，采用配对bootstrap置信区间。
  - 控制了计算预算（linguistic方法匹配循环方法的顺序FLOPs）。
  - 消融实验系统地验证了J-thought读取、载波结构、固定字典等设计选择。
  - **不足之处**：未与更多种类的基线（如Tree-of-Thoughts、Self-Consistency）对比；未在其他架构（如GPT、PaLM）上验证；AIME 2024基准上由于样本量小（？）标准差较大。

### 6. 论文的主要结论与发现

1. **性能优势**：J-CoT-Train在所有8个基准上取得最高分，平均准确率47.5%（SIM-Coconut）→ 50.2%（J-CoT-Train），提升2.7点。J-CoT-Zero以47.9%也在7个基准上超越SIM-Coconut。
2. **缩放行为**：随着模型容量增大和循环深度增加，J-CoT获得更大收益（例如，405B模型在MATH-500上从45.0%（4循环）提升至63.4%（16循环）），表明正交互作用。
3. **界面谱位置**：在密集潜在接口与显式语言接口的连续插值中，J-thought配置（λ=0.5）达到最高准确率（88.8%），平衡了“恢复”与“无效路径持久性”。
4. **消融确认**：移除J-thought读取、使用单载波、固定大小等都会降低性能，验证了各组件的重要性。

### 7. 优点

- **方法创新性**：提出了基于词汇索引坐标系的循环接口（J-thought），是显式CoT和密集潜在循环之间的自然中间点，兼具语言基础性和信息选择能力。
- **训练效率**：J-CoT-Zero无需任何额外训练即可获得提升，大幅降低应用门槛；J-CoT-Train仅优化极少量参数（载波嵌入和读取门），不改变原始模型参数。
- **理论分析**：提供了运输稳定性定理（命题1），从理论上支撑了跨层传递系数的可行性。
- **实验全面性**：覆盖数学、科学、代码生成、代码执行推理等多个领域，包含多个骨干模型和深度探索，消融实验细致。
- **自适应轮次**：根据状态变化动态决定循环次数，而非固定深度，更加灵活高效。

### 8. 不足与局限

- **模型覆盖有限**：主要实验基于单一架构（Qwen3-8B），缩放实验仅涵盖Qwen和Llama系列，未在更多开发模型（如Mistral、Gemma）或非Transformer架构上验证。
- **缺少与搜索方法的对比**：未与Tree-of-Thoughts、Self-Consistency（多次采样聚合）等基于显式搜索的方法比较，这些方法也在推理上有显著收益。
- **J-space字典依赖预计算**：字典需要校准数据（1K序列），虽然固定，但不同分布的任务可能需要重新校准？文中未讨论适应性。
- **AIME 2024结果方差大**：由于该基准问题数少（30道），标准差（约3%）较大，统计显著性可能不足。
- **未讨论解释性与可编辑性**：虽然系数可对应词汇标签，但未分析J-thought的具体语义内容或用人可干预的方式。
- **计算资源未报告**：缺少训练和推理的具体算力消耗，难以复现和比较成本。
- **应用限制**：载波位置在推理时引入额外计算（每循环约6层Transformer）；对于实时性要求高的场景可能不适用。

（完）
