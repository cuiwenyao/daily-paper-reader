---
title: "PACE: A Neuro-Symbolic Framework for Plausible and Actionable Counterfactual Explanations"
title_zh: PACE：一种面向合理且可执行的反事实解释的神经符号框架
authors: "Pavel Iakovets, Liyanapathiranage Sudeepika Wajirakumari Samarathunga, Martin Thomas Horsch, Fadi Al Machot"
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.01306v1"
tags: ["query:ns-xai"]
score: 10.0
evidence: 神经符号框架用于反事实可解释性
tldr: 现有反事实解释方法常因缺乏领域知识和干预约束而生成不切实际的建议。本文提出PACE，一种模块化神经符号框架，将神经网络预测与符号推理层分离，通过ASP规则显式编码可行修改。在Adult Income数据集上的案例表明，该方法能在保证可解释性的同时生成满足领域约束的反事实解释，展示了神经符号方法在可行感知反事实解释中的潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.01306v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1354, \"height\": 857, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01306v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1724, \"height\": 1378, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.01306v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 710, \"height\": 302, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01306v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 788, \"height\": 301, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01306v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 698, \"height\": 541, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01306v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1714, \"height\": 416, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01306v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1438, \"height\": 222, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01306v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 865, \"height\": 339, \"label\": \"Table\"}]"
motivation: 现有反事实解释缺乏显式领域知识和干预约束，导致推荐不切实际或不可行。
method: PACE框架将神经网络预测与符号推理层分离，利用ASP规则编码领域约束以生成可行反事实。
result: 在Adult Income数据集上，PACE生成的反事实解释比传统方法更符合领域可行性要求。
conclusion: 神经符号方法能有效平衡反事实有效性与可行性，提升解释的可信度与可操作性。
---

## 摘要
反事实解释通过识别能改变模型决策的最小输入变化来解释机器学习预测。尽管许多现有方法成功生成了改变预测的替代方案，但由于缺乏整合领域知识和干预约束的显式机制，它们常常产生不现实或不可行的建议。神经符号人工智能通过将数据驱动的预测模型与能够表示人类可理解规则和可行动作的符号推理相结合，提供了一个有前景的方向。本文提出了PACE，一个模块化的神经符号框架，用于生成可行性感知的反事实解释。该框架将预测和推理分离为两个组件：一个用于分类的神经预测模型，以及一个在反事实生成过程中强制执行领域特定约束的符号推理层。通过显式建模可行的干预措施，该框架产生的解释与领域知识一致，同时保持可解释性和可执行性。该方法与模型无关，适用于需要现实决策支持的领域。在Adult Income数据集上进行了案例研究，将多层感知器分类器与编码对教育、职业和工作时间进行可行修改（同时保留不可变属性）的答案集编程(ASP)规则相结合。结果凸显了反事实有效性与合理性之间的权衡，并表明符号约束产生的解释能更好地满足领域特定的可行性要求，展示了神经符号方法在可解释AI中实现透明、可行性感知反事实解释的潜力。

## Abstract
Counterfactual explanations explain machine learning predictions by identifying minimal input changes that would alter a model's decision. Although many existing methods successfully generate prediction-changing alternatives, they often produce unrealistic or infeasible recommendations due to a lack of explicit mechanisms for incorporating domain knowledge and intervention constraints. Neuro-symbolic AI offers a promising direction by combining data-driven predictive models with symbolic reasoning capable of representing human-understandable rules and feasible actions. This paper presents PACE, a modular neuro-symbolic framework for generating feasibility-aware counterfactual explanations. The framework separates prediction and reasoning into two components: a neural predictive model for classification and a symbolic reasoning layer that enforces domain-specific constraints during counterfactual generation. By explicitly modeling feasible interventions, the framework produces explanations consistent with domain knowledge while remaining interpretable and actionable. The approach is model-agnostic and adaptable to domains requiring realistic decision support. A case study is conducted on the Adult Income dataset, combining a multilayer perceptron classifier with Answer Set Programming (ASP) rules encoding feasible modifications to education, occupation, and working hours while preserving immutable attributes. Results highlight the trade-off between counterfactual validity and plausibility and show that symbolic constraints yield explanations that better satisfy domain-specific feasibility requirements, illustrating the potential of neuro-symbolic methods for transparent, feasibility-aware counterfactual explanation in explainable AI.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：现有反事实解释生成方法（如基于优化、生成式方法）虽然能有效找到改变预测的结果，但常常产生不现实或不可行的建议——因为它们缺乏显式机制来整合领域知识和干预约束。例如，推荐改变不可变属性（如年龄、性别），或进行不合理的职业跳跃（如从文员直接跳到高管）。这削弱了解释的实用性和可信度。
- **整体含义**：如何生成同时满足**有效性**（改变预测）、**最小性**（最少特征修改）和**可行性**（符合领域规则）的反事实解释，是可解释AI中的一个关键挑战。神经符号AI有望结合深度学习的表示能力和符号推理的规则约束，为这一挑战提供解决方案。
- **论文目标**：提出PACE框架，将神经网络预测与符号推理层分离，显式编码可行干预空间，生成可行性感知的反事实解释。

## 2. 方法论：核心思想、关键技术细节与流程

### 核心思想
- 将反事实生成建模为**约束搜索问题**：候选解释必须同时满足**预测翻转条件**和**符号可行性约束**。
- 采用**生成-验证**迭代循环，优先寻找最小修改（稀疏解释）。

### 关键技术细节
1. **问题形式化**：
   - 给定预测模型 \( f: \mathcal{X} \to \mathcal{Y} \)，事实实例 \( x \)，目标找到 \( x_{cf} \in \Omega \)（可行干预空间），使得 \( f(x_{cf}) \neq f(x) \) 且修改距离 \( d(x, x') \) 最小。距离定义为修改特征数：\( d(x, x') = \sum_j \mathbb{1}(x_j \neq x'_j) \)。
   - 可行空间 \( \Omega \) 由符号知识库定义：包括不可变属性 \( X_I \) 和可编辑属性 \( X_E \)，以及一组规则 \( R \)（如教育只能相邻级别变化、职业仅允许预定义转换、工时增减有限范围）。

2. **符号约束建模**：
   - 使用**Answer Set Programming (ASP)** 编码规则。例如：年龄、性别固定；教育只能相邻级变动（如 “HS-grad” ↔ “Some-college”）；职业按预定义图进行转换；工时每次增减不超过10小时，且限制在[1,80]。
   - 候选生成时，ASP推理引擎只产生满足所有约束的可行实例。

3. **搜索流程**：
   - 从最小预算 \( K=1 \) 开始（每次修改一个特征）。
   - ASP生成所有满足当前预算的候选。
   - 神经网络对候选进行预测评估；若找到翻转预测的候选，选择修改距离最小的返回。
   - 若未找到，预算 \( K \) 递增，扩展搜索范围，直到找到或达到最大预算。

### 算法流程（文字描述）
1. 输入：事实实例 \( x \)，预测模型 \( f \)，符号规则库 \( R \)，最大预算 \( K_{\max} \)。
2. 初始化：将 \( x \) 转换为ASP事实，设置预算 \( k=1 \)。
3. 循环 \( k \) 从1到 \( K_{\max} \)：
   - ASP生成所有满足预算 \( k \) 的可行候选集合 \( C_k \)。
   - 对每个候选 \( c \in C_k \)，调用 \( f(c) \) 检查预测翻转。
   - 若存在翻转候选，返回使 \( d(x,c) \) 最小的 \( c \)。
4. 若循环结束未找到，返回无解。

## 3. 实验设计

- **数据集**：**Adult Income** (UCI)，用于二分类（年收入是否 >50K）。清洗后共30,718个样本，按75%/25%分层划分训练/测试，最终测试6,144个实例，但有效性评估使用其中1,000个。
- **特征**：选取5个特征：年龄（不可变）、性别（不可变）、教育（可编辑）、职业（可编辑）、每周工时（可编辑）。数值属性标准化，分类属性one-hot编码。
- **预测模型**：多层感知器(MLP)，2个隐藏层(32,16)，ReLU激活，Adam优化器，学习率0.001，批大小128，正则化5e-4，早停轮数120。测试准确率0.82。
- **对比方法**（共6种）：
  - **PACE**（自己实现）
  - **Random Search**（无约束随机搜索）
  - **DiCE**（使用官方实现）
  - **Wachter-style**（简化实现，基于优化）
  - **VCNet-style**（简化实现，学习数据流形）
  - **C-CHVAE-style**（简化实现，在隐空间搜索）
- **评估指标**：有效性(Validity)、最小性(Minimality)、合理性(Plausibility，即满足符号约束的比例)、运行时间(Runtime)、翻转所需候选数(Iters to flip)。还计算了有效性×合理性的综合分数。

**实验组数**：
1. **主实验**：在1,000个测试实例上比较所有方法，报告平均指标（表4）。
2. **匹配约束比较**：随机抽取200个实例，预算K=2，每实例500候选，对比“盲搜索+拒绝”与“直接在Ω内搜索”以及PACE（表5），揭示合理性提高来源于显式约束。
3. **可行集大小扩展实验**：放宽约束（允许更宽的转换邻域宽度w=1~6），比较穷举搜索（PACE）与固定预算N=80的随机搜索在有效性上的差异（表6、图1）。显示随着可行集扩大，穷举搜索（即符号搜索）获得有效性优势。
4. **外部合理性评估**：计算PACE候选与无约束随机候选到k=5最近训练邻居的平均距离（PACE为0.354，随机为0.944），验证生成候选更接近数据流形。

## 4. 资源与算力

- 论文**未明确提及**所使用的GPU型号、数量、训练时长等算力资源。
- 训练MLP模型和运行ASP推理的计算量可能较小（单数据集、小网络），但无具体数据。
- 所有实验在普通CPU/GPU上即可完成，但文章未提供详细信息。

## 5. 实验数量与充分性

- **数量**：共4组正式实验（主表4 + 匹配约束表5 + 可扩展性表6 + 外部合理性分析）。消融方面：表5相当于消融了“直接在Ω内搜索 vs. 盲搜索”；表6消融了“穷举 vs. 固定预算随机”。另外还对比了6种基线方法。
- **充分性**：实验设计较为系统，覆盖了有效性、最小性、合理性、运行时间等多个维度，并通过匹配约束实验隔离了约束的影响。但**局限性**明显：
  - 仅使用**一个数据集**（Adult Income），缺乏跨领域泛化验证。
  - 约束规则由人工手动定义，未测试自动获取约束或更复杂规则的效果。
  - 基线方法（Wachter、VCNet、C-CHVAE）为简化实现，可能与原始论文结果有差异。
  - 测试实例数量（1000/200）适中，可进一步在更大规模上验证。
- **客观与公平**：比较条件基本一致（相同测试实例，相同评价设置）。但请注意，DiCE使用官方实现，而其他基线为简化版，可能存在实现差异导致不公平。作者也对此进行了说明。总体而言实验设计合理，但在多样性和真实性方面有提升空间。

## 6. 主要结论与发现

- **明确权衡**：无约束方法（Random Search、DiCE）有效性高（>0.74），但合理性极低（<0.05）；PACE实现了**完美合理性**（1.0），但有效性较低（0.240），体现有效性与可行性的根本矛盾。
- **约束贡献**：通过匹配约束实验（表5）证明，合理性的提高主要源于**显式约束定义**而非符号搜索本身——当所有方法共享相同可行集Ω时，它们达到相同的有效性。
- **符号搜索的增值**：当可行集变大时（表6），穷举符号搜索（PACE）相比固定预算随机搜索具有更高的有效性，表明符号推理在**大规模搜索空间**中能提供完备性收益。
- **外部合理性**：PACE的候选更接近数据流形（kNN距离更小），说明约束有助于生成更符合真实分布的解释。
- **最小性**：PACE平均修改1.242个特征，优于大多数基线（仅Wachter稍好，但Wachter有效性极低）。

## 7. 优点

1. **思路新颖**：将神经符号思想应用于反事实生成，显式建模可行干预空间，解决了现有方法忽视领域约束的问题。
2. **模块化设计**：预测模型与符号推理分离，模型无关，易于适配不同领域和规则。
3. **精度与可解释性平衡**：在保持约束严格满足的前提下，尽量生成稀疏修改。
4. **实验设计严谨**：包含匹配约束消融和可扩展性分析，系统揭示了约束与搜索策略的各自贡献。
5. **外部合理性验证**：利用kNN距离补充了非符号的合理性评价，增强了结论可靠性。
6. **代码开源**（GitHub），便于复现和扩展。

## 8. 不足与局限

1. **数据集单一**：仅Adult Income一个数据集，结果泛化性存疑。未来需要在医疗、金融、教育等更多真实场景验证。
2. **约束人工设定**：规则需领域专家手工编写，耗时且可能不完整；未探索自动约束获取或基于因果模型的规则学习。
3. **有效性较低**：PACE的有效性（0.240）明显低于无约束方法（0.742~0.778），这表明严格限制可行性可能以牺牲覆盖率代价。在某些需要高覆盖率解释的系统中可能成为短板。
4. **基线实现简化的风险**：Wachter、VCNet、C-CHVAE为简化版本，可能与原始论文性能有差异，导致比较不够公平。DiCE则使用官方实现，但其余方法可能被低估或高估。
5. **计算效率**：ASP穷举搜索在可行集较大时可能带来指数级候选数量，表6显示穷举与随机差距随可行集扩大而增大，但论文未提供大规模搜索的耗时分析。
6. **未考虑多目标优化**：只追求最小修改特征数，未考虑用户偏好（如优先修改某些属性、修改幅度等）。
7. **可扩展性**：当前只处理了5个特征，面对高维特征空间或复杂关系（如时序、组合约束）时，ASP推理可能成为瓶颈。
8. **缺乏用户研究**：虽然从指标上满足了可行性，但未通过人类实验验证解释的实际可用性和用户理解度。

（完）
