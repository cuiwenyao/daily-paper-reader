---
title: "From Dependency to Compositionality: A Neurosymbolic Lifting of LLM Outputs via Combinatory Categorial Grammar"
title_zh: 从依存到组合：通过组合范畴语法对LLM输出进行神经符号提升
authors: Remo Pareschi
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.18961v1"
tags: ["query:ns-xai"]
score: 9.0
evidence: 使用CCG进行神经符号提升的组合推导
tldr: 针对LLM生成文本缺乏真正语法的批评，本文发现自回归生成的前缀驱动、类型补全动态与组合范畴语法(CCG)的增量处理模型高度一致。据此提出神经符号框架，通过CCG将LLM输出重建为类型化组合推导，不修改模型内部。该提升通过Curry-Howard对应扩展到Solidity、SQL等LLM生成的形式语言。框架支持组合层结构检查与内容层外部知识校验，能够尽早标记幻觉内容。最终为LLM输出提供可审计的结构化表示，提升可靠性与可解释性。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.18961v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1504, \"height\": 324, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18961v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 705, \"height\": 543, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18961v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1330, \"height\": 539, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18961v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1094, \"height\": 320, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18961v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1612, \"height\": 842, \"label\": \"Table\"}]"
motivation: 揭示LLM自回归生成与CCG增量处理之间的深层对齐，为LLM输出提供组合性解释以弥补语法缺失。
method: 基于CCG增量推导重建LLM的token序列，输出类型化组合推导树，不修改LLM内部，仅对输出进行后处理。
result: 通过Curry-Howard对应扩展至Solidity等语言，实现组合层结构错误检测与内容层幻觉检查。
conclusion: 同步LLM-CCG耦合开辟可审计神经符号系统新方向，提升LLM输出的可靠性和可解释性。
---

## 摘要
大型语言模型通过从前缀逐步预测下一个令牌来生成流畅文本。生成传统中的批评者认为此类系统缺乏真正的语法；依存语法视角的有影响力的回应认为，LLM行为可以通过逐词构建的局部中心词-依存结构得到良好描述。我们认为一个更敏锐的观察被忽略了：自回归生成的前缀驱动、类型补全动态与组合范畴语法最初设计支持的自增处理模型高度一致。在此基础上，我们提出一个神经符号框架，将LLM输出提升为类型化组合推导——并非主张LLM内部实现了组合范畴语法，而是其输出可接受一种原则性、增量且可审计的组合范畴语法重构。这带来了两个后果。首先，通过Curry-Howard对应，这种提升从自然语言扩展到LLM也生成的形式语言——如Solidity编程语言、描述逻辑和查询语言如OWL和SQL——其中类型系统变化而架构保持不变。其次，这种提升支持两层检查：组合层直接捕捉结构失败，内容层针对外部知识源检查提升后的结构，从而能够尽早标记幻觉内容。因此，该解释要求生产者不是认知能力，而是前缀驱动的生成特征。我们最后简述同步LLM-CCG耦合作为该框架开辟的一个方向。

## Abstract
Large language models (LLMs) generate fluent text by incrementally predicting the next token from a prefix. Critics in the generative tradition argue that such systems lack genuine grammar; influential replies from the dependency-grammar perspective hold that LLM behavior is well described by local head-dependent structure built word by word. We argue that a sharper observation has been overlooked: the prefix-driven, type-completing dynamics of autoregressive generation align closely with the incremental processing model that Combinatory Categorial Grammar (CCG) was originally designed to support. On this basis we propose a neurosymbolic framework in which LLM outputs are lifted into typed compositional derivations -- not claiming that LLMs implement CCG internally, but that their outputs admit a principled, incremental, and auditable CCG reconstruction. Two consequences follow. First, through the Curry-Howard correspondence the lifting extends beyond natural language to the formal languages LLMs also produce -- programming languages such as Solidity, description-logic and query languages such as OWL and SQL -- with the type system varying and the architecture held fixed. Second, the lifting supports two layers of checking: a compositional layer that catches structural failures directly, and a content layer that checks the lifted structure against external knowledge sources, enabling the earliest possible flagging of hallucinated content. The account thereby requires of a producer not cognition but a prefix-driven generative profile. We close with a sketch of synchronous LLM-CCG coupling as one direction the framework opens.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：LLM生成的文本虽然流畅，但被生成传统批评为“缺乏真正语法”。尽管依存语法视角认为LLM行为可通过局部中心词-依存结构描述，但作者指出一个更深刻的观察被忽略：自回归生成的前缀驱动、类型补全动态与组合范畴语法（CCG）最初设计支持的增量处理模型高度一致。
- **整体含义**：作者提出一个神经符号框架，将LLM输出通过CCG重建为类型化的组合推导，从而为LLM输出提供可审计、结构化的表示。该框架不主张LLM内部实现了CCG，而是其输出可被原则性地重构。通过Curry-Howard对应，框架扩展到LLM生成的形式语言（如Solidity、SQL、OWL），支持两层检查（组合层和内容层），并能尽早检测幻觉内容，将生产者要求从“认知”降低为“前缀驱动生成特征”。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：利用CCG的增量、类型补全特点，对LLM生成的文本进行后处理“提升”（lifting）：使用现成的CCG语义解析器（如ccg2lambda）将每个句子解析为CCG推导和关联的lambda项，从而获得类型化的组合表示。
- **关键技术细节**：
  - **增量对齐**：LLM逐token生成，CCG解析器也逐步（逐词）解析前缀，每个前缀都有类型和部分lambda项，显式指示缺少什么类型的成分。
  - **Coherence性质**：CCG的不同推导顺序会收敛到相同的lambda项（组合等价性），保证提升良定义。
  - **Curry-Howard对应**：类型化lambda项对应于逻辑命题/程序类型，使框架可自然扩展到形式语言（编程语言、描述逻辑等），类型系统变化而架构固定。
  - **两层检查**：
    - 组合层：检测结构错误（如缺失参数、类型不匹配）。
    - 内容层：将提升后的结构（如谓词-实体绑定）对外部知识源查询，标记与事实不符的断言。提升的增量性支持最早可能标记（如名词短语组合后即可查询）。
- **算法流程**（文字说明）：
  1. LLM概率性地生成token序列。
  2. 对每个输出文本，CCG语义解析器分配词汇范畴，应用组合规则，生成推导树和lambda项。
  3. 组合检查：验证lambda项是否良类型、是否满足预期合同类型（对形式语言）。
  4. 内容检查：将lambda项中的原子命题映射为对外部知识库（如本体、经过验证的代码模式库）的查询，通过语义类型决定查询形式；当断言与外部知识矛盾时发出标记。

### 3. 实验设计
- **实验情况**：本论文为概念性/理论论文，**未进行实验验证**。作者明确声明“No datasets were generated or analysed during the current study”。
- **提及的相关工作**：引用Salzano et al. (2026)关于LLM生成Solidity合约的实证研究（正确率仅20-26%），作为框架适用性的动机；以及Stanojević & Steedman (2019)的增量CCG解析器与Transformer结合建模脑信号的认知神经科学工作，作为CCG与LLM可耦合的例证，但均非本论文的实验。
- **Benchmark与对比方法**：无。

### 4. 资源与算力
- **未提及**：论文不涉及训练或推理实验，因此未报告任何GPU型号、数量、训练时长等信息。框架本身是轻量后处理，仅需CCG解析器（如ccg2lambda）和外部知识库查询，不修改LLM。

### 5. 实验数量与充分性
- **无实验**：故不适用。论文主要通过理论论证、示例分析（如“John loves Mary”增量表、“Aztec god Zeus”幻觉检测示例、Solidity函数类型弱化示例）来支撑观点，缺乏实体验证的客观性。

### 6. 论文的主要结论与发现
- **发现1**：LLM的前缀预测动态与CCG的增量类型补全动态高度一致，使得CCG成为分析LLM输出特有的合适工具。
- **发现2**：CCG提升可为LLM输出提供可审计、透明的类型化组合表示，支持结构化错误检测和内容事实性检查（结合外部知识源），且能最早可能标记错误。
- **发现3**：通过Curry-Howard对应，该提升框架可跨越自然语言和形式语言（如Solidity、SQL、OWL），类型系统变化而架构固定，生产者只需是前缀驱动，不需要认知能力。
- **发现4**：框架区分了组合层和内容层，类似智能合约中链上验证与预言机（oracle）的结构，也对应检索增强生成（RAG）中的模式，但增加了类型化提升带来的精确性。
- **发现5**：同步LLM-CCG耦合（硬约束或软重加权）是开放的未来方向，可防止错误生成，但对自然语言处理需谨慎避免解析器诱导的花园路径。

### 7. 优点
- **理论对齐清晰**：精准揭示了LLM生成与CCG处理在形式上的契合，提供了比单纯依存语法更深层的组合解释。
- **框架谦虚分层**：不使用“LLM内部有语法”的强主张，而是后处理重建，不修改模型，不访问内部，仅依赖输出，极具保守性和实用性。
- **增量性质**：支持最早可能标记错误（即使是名词短语阶段），优于非增量方法。
- **跨域统一**：通过Curry-Howard将自然语言和多种形式语言纳入统一架构，类型系统可变，逻辑一致。
- **类型作为接口**：提出类型不仅指示组合方式，还指定了外部知识如何查询，连接了结构检查和内容检查。
- **可审计性**：每个组件可追溯到词汇条目或组合步骤，无黑箱推理。

### 8. 不足与局限
- **缺乏实现与实验**：论文仅为概念框架，无端到端实现或定量实验验证。未来需要构建CCG类型化词典（对于Solidity等）、训练supertagger、评估幻觉检测性能等。
- **多句子话语与指代问题未解决**：框架目前只处理单句，跨句指代、语用依赖、话语结构等未涵盖。作者承认并指出已有增量核心词消解工作（Grenander et al., 2022）可作为补充，但仍是未来工作。
- **CCG解析器鲁棒性**：现有CCG解析器主要训练于新闻语料（CCGbank），LLM生成文本覆盖寄存器、领域、非正式风格更广，可能影响解析质量。超tagger的泛化能力（如对未见词汇）有部分缓解，但结构层面的适应性仍是挑战。
- **外部知识源依赖**：内容层检查质量完全取决于外部知识源的准确性、完备性和时效性，框架本身不解决知识源的选择和元信任问题。
- **同步耦合的风险**：硬约束版本可能因解析器早期分析而导致“花园路径”，误屏蔽合法续写；软加权版本效果待定。
- **冷启动问题**：对于形式语言（如Solidity），需构造类型化词典，虽可利用编译器提供自动标注数据，但工作量仍大。
- **非前缀驱动生产者不适用**：框架限定于前缀驱动的生成器，对于一次生成全结构或其他生成模式的系统不直接适用。

（完）
