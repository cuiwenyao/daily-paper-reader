---
title: Interpretable Uncertainty for Adaptive Retrieval and Reasoning in Question Answering
title_zh: 问答中自适应检索与推理的可解释不确定性
authors: "Ritajit Dey, Iadh Ounis, Graham McDonald"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07380v1"
tags: ["query:ns-xai"]
score: 8.0
evidence: 基于不确定性的自适应检索与推理提升LLM透明度
tldr: 大语言模型在问答中易产生幻觉且透明度不足，检索增强生成（RAG）可提升事实性但决策策略不透明。本文提出基于模型内部表示的不确定性感知框架，通过单次前向估计区分知识不足与模糊冲突，自适应触发RAG或额外推理。该方法在保证性能的同时实现透明高效的检索与推理决策，为可解释问答工具提供实用方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07380v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 885, \"height\": 441, \"label\": \"Figure\"}]"
motivation: 现有RAG策略不透明或需多步提示，无法高效且可解释地决定何时检索或推理。
method: 从LLM隐藏状态单次前向提取知识不足与模糊冲突两种不确定性信号，指导自适应检索与推理。
result: 在保持问答性能的同时，提供透明且高效的检索与推理决策，降低幻觉风险。
conclusion: 提出一个基于可分解不确定性信号的透明自适应框架，为可解释用户工具提供可行替代方案。
---

## 摘要
大型语言模型在问答任务中表现出色，但仍易产生幻觉且透明度有限。检索增强生成可提升事实准确性，但关于何时以及如何从外部资源检索的决策通常基于不透明的策略或计算效率低下的多步提示流程。我们提出了一种基于显式信号的自适应问答不确定性感知框架，这些信号源自大型语言模型的内部表示。我们区分了知识不足与知识模糊或冲突，并通过单次前向传递从隐藏状态中高效估计这些信号。这些信号引导系统行为：当知识不足时触发检索增强生成，而当模糊或冲突程度较高时应用额外推理。通过将自适应决策建立在可分解且高效估计的不确定性信号上，该方法为现有的检索和推理策略提供了一种透明且实用的替代方案，支持可解释的用户导向工具的设计。

## Abstract
Large language models (LLMs) achieve a strong performance in question answering (QA), but remain prone to hallucinations and suffer from limited transparency. Retrieval-augmented generation (RAG) can improve factuality, yet decisions about when and how to retrieve from external resources are typically based on opaque policies or computationally inefficient multi-step prompting procedures. We propose an uncertainty-aware framework for adaptive QA based on explicit signals derived from LLM internal representations. We distinguish between knowledge insufficiency and knowledge ambiguity or conflict, and efficiently estimate these from hidden states in a single forward pass. These signals guide system behaviour: RAG is triggered when knowledge is insufficient, while additional reasoning is applied when ambiguity or conflict is high. By grounding adaptive decisions in decomposed and efficiently estimable uncertainty signals, this approach provides a transparent and practical alternative to existing retrieval and reasoning strategies supporting the design of interpretable user-facing tools.

---

## 论文详细总结（自动生成）

# 问答中自适应检索与推理的可解释不确定性 —— 论文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题**：大型语言模型在问答任务中性能强劲，但存在幻觉、依赖可能过时的内化知识、透明度低等问题。检索增强生成虽能提升事实性，但其“何时”与“如何”检索的决策策略通常基于不透明的学习策略（如Self-RAG、Adaptive-RAG）或计算低效的多步提示流程。
- **整体含义**：现有方法难以同时满足**可解释**和**高效**的要求。本文提出一种基于模型内部隐藏状态的不确定性感知框架，通过单次前向传播区分**知识不足**（Knowledge Insufficiency）与**知识模糊或冲突**（Knowledge Ambiguity/Conflict），并以此指导自适应检索或推理，从而构建透明、高效的用户导向问答工具。

## 2. 论文提出的方法论：核心思想与关键技术细节

### 核心思想
- 将不确定性显式分解为两种类型，并使用从LLM隐藏状态中训练的**轻量回归探针**（regression probes）在**单次前向传递**中同时估计这两种信号。
- **知识不足**：对应LLM预训练语料中该事实的出现频率（越低表示知识越不足，不确定性越高）。通过回归探针预测该频率。
- **知识模糊/冲突**：对应不同候选答案在预训练语料中出现次数的**熵**（entropy），熵越高表示冲突/模糊程度越高。同样通过回归探针预测。

### 关键技术细节
1. **回归探针训练**：利用LLM的隐藏状态作为输入，分别预测知识不足评分（事实出现次数）和模糊冲突评分（熵）。无需额外采样或多步提示。
2. **自适应决策机制**：基于阈值进行比较：
   - 若知识不足评分超过阈值 → 触发RAG（外部检索）。
   - 若模糊/冲突评分超过阈值 → 增加测试时计算（如Chain-of-Thought、Self-Consistency等推理策略）。
   - 两者均低时直接生成答案，无需干预。
3. **图形化流程**（文中Figure 1展示了该QA流水线）。

## 3. 实验设计

### 数据集与场景
- **数据集**：Natural Questions (NQ) —— 事实性问题问答基准。
- **场景**：针对知识密集型事实问答，验证不确定性信号能否有效触发RAG或推理。

### 基准方法
- **LLM-only基线**：直接使用LLM生成答案（无RAG）。
- **Always-on RAG基线**：始终使用RAG（检索后生成）。
- **对比的触发机制**：使用**Semantic Entropy (SE)** 和 **Weighted Entropy Production Rate (WEPR)** 来触发RAG（作为本文框架的前期验证）。

### 对比方法
- 文章声称本文框架（基于不确定性估计的触发）显著优于上述两种基线（根据McNemar检验）。但具体数据仅给出相对于LLM-only提升+5.9%（SE触发）/ +4.7%（WEPR触发），相对于Always-on RAG提升+3.3%（SE）/ +2.1%（WEPR）。

### 模型
- 使用**Llama-2-7b-chat**作为基础LLM。

## 4. 资源与算力

- 文中**未明确说明**所使用的GPU型号、数量、训练时长等算力信息。仅提及训练回归探针是在隐藏状态上进行的，但未给出详细硬件配置或时间开销。
- 可以推断：由于探针是轻量级线性模型，且仅在单次前向传播后训练，算力需求应远低于LLM本身微调或多步推理，但具体数字缺失。

## 5. 实验数量与充分性

- **实验组数**：仅涉及一组主实验（NQ数据集，使用两种触发信号对比两个基线），**没有消融实验**、**没有跨数据集验证**、**没有对比不同阈值或探针训练策略**。
- **充分性评价**：作为短文（3页），实验结果属于初步验证，证明了不确定性驱动自适应的潜力。但覆盖范围有限，缺乏对模型规模、知识领域、推理方法（Chain-of-Thought与Self-Consistency）的单独评估，也没有分析阈值敏感度或探针的训练数据来源。因此实验的**充分性一般**，结论的泛化性需更多验证。
- **公平性**：对比的基线为LLM-only和always-on RAG，但未与最先进的自适应策略（如Self-RAG、Adaptive-RAG）直接比较；此外，仅报告了相对提升百分比，未给出完整的准确率数值、误差线或统计显著性细节。因此公平性分析受限。

## 6. 论文的主要结论与发现

- **主要结论**：基于不确定性信号的自适应RQ机制（区分知识不足与模糊冲突）能够显著优于固定策略（始终不检索或始终检索），且效率高（单次前向）。
- **发现**：不同形式的不确定性对应不同的最优处理方式（检索解决知识不足，推理解决冲突），验证了分解不确定性的必要性。
- **主张**：可解释性应作为系统设计的直接原则，而非事后解释工具。本文框架天然提供决策理由（为何检索或为何推理），有助于用户理解和系统审计。

## 7. 优点

- **创新性**：首次在自适应QA中显式区分**知识不足**与**知识模糊/冲突**两种不确定性类型，并利用**隐藏状态回归探针**单次前向估计，兼具可解释性与效率。
- **高效性**：无需多步提示、多次采样或辅助模型，适合多轮对话和代理型场景。
- **可解释性**：决策依据清晰——知识不足触发检索，冲突增加推理。系统每步都可给出为何需要检索/推理的明确解释。
- **实用性**：框架设计简单（阈值机制），易于集成到现有RAG管线。

## 8. 不足与局限

- **实验覆盖不足**：仅在一个数据集（NQ）和一个模型（Llama-2-7b-chat）上验证，未测试其他LLM大小、类型或领域（如多跳、对话、医学等）。
- **阈值依赖性**：阈值设定未讨论，不同阈值可能影响性能，但文中未做分析。
- **探针训练代价**：虽然单次前向高效，但回归探针需要额外的监督数据（事实出现次数和熵），这些数据可能难以获取或定义，尤其对于长尾事实。
- **未与SOTA自适应策略比较**：未与Self-RAG、Adaptive-RAG等直接对比，缺乏在延迟、成本上的量化比较。
- **初步结果粗粒度**：仅给出准确率提升百分比，缺乏详细统计（如方差、P值等），且没有验证推理方法（CoT/ Self-Consistency）的实际效果。
- **泛化性风险**：知识不足的定义依赖于预训练语料中的频率，对于动态知识（如实时事件）可能失效；模糊冲突熵的估计可能不适用于开放式生成任务。

（完）
