---
title: "KANEx: Translating Kolmogorov-Arnold Networks' Interpretability to Medical Explainability"
title_zh: KANEx：将Kolmogorov-Arnold网络的可解释性转化为医学可解释性
authors: "Krithi Shailya, Ananya Lakshmi Ravi, Venkatanathan K. V., Sowmya S. Sundaram, Gokul S. Krishnan, Aditi Anand, Balaraman Ravindran"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24730v1"
tags: ["query:ns-xai"]
score: 9.0
evidence: 利用KAN的符号透明性增强可解释性
tldr: "医学影像模型的黑盒性质削弱临床信任，现有结合VLM的解释方案未解决视觉模型不透明性。本文提出KANEx框架，首次利用KAN网络的符号可解释性支撑VLM推理，并设计KAN-Map直接从KAN模型生成热力图。在MIMIC-CXR数据集上，KAN架构相比ResNet/ViT基线显著提升了语义相似度和显著性图忠实度，视觉定位与推理质量提升10%。该工作表明，将语言解释和视觉归因建立在数学可解释单元上是实现可信医学AI的关键步骤。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24730v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1077, \"height\": 634, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24730v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1014, \"height\": 713, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24730v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1242, \"height\": 572, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.24730v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1294, \"height\": 687, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24730v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1262, \"height\": 451, \"label\": \"Table\"}]"
motivation: 医学影像模型的黑盒性质影响临床信任，现有VLM解释未能解决视觉模型的不透明性。
method: 提出KANEx框架，利用KAN的符号可解释性，并设计KAN-Map直接从KAN模型生成热力图，为VLM提供基于上下文的输入。
result: "在MIMIC-CXR上，KAN架构提升语义相似度和显著性图忠实度，视觉定位与推理质量提升10%。"
conclusion: 将解释建立在数学可解释单元上是迈向可信医学AI的必要步骤。
---

## 摘要
计算机视觉模型在医学应用中已经变得非常高效，然而其黑箱特性持续削弱临床医生的信任。在临床工作流程中，胸部X射线分类器越来越常与视觉语言模型（VLM）配对，以生成自然语言解释。然而，这些系统增加了语言流畅性，却并未解决视觉模型本质上的不透明性。随着Kolmogorov-Arnold网络（KAN）的出现，其基于样条的分量提供了内在可解释的功能单元，我们研究是否可以利用这种架构的透明性来生成更可信的文本解释。我们引入了KANEx，这是首个利用KAN的符号透明性来锚定VLM推理的框架。这种可解释性也使得设计KAN-Map成为可能，这是一种直接从KAN模型而非梯度近似导出的新型热图生成方法。我们将这些锚定上下文输入下游VLM以增强可解释性。在MIMIC-CXR数据集上进行的基准测试表明，基于KAN的架构（以ResNet/ViT为基线）在生成明显更真实的显著性图的同时，语义相似性也得到了提升。KAN架构将视觉定位和下游推理质量提升了10%。我们的发现表明，将语言解释和视觉归因锚定在数学可解释单元中，是实现可信医学AI的必要步骤。

## Abstract
Computer vision models have become highly effective for medical applications, yet their black-box nature continues to undermine clinician trust. In clinical workflows, chest X-ray classifiers are increasingly paired with Vision-Language Models (VLMs) to generate natural-language explanations. However, these systems add linguistic fluency without addressing the underlying opacity of the visual model. With the emergence of Kolmogorov-Arnold Networks (KANs), whose spline-based components provide inherently interpretable functional units, we investigate whether this architectural transparency can be leveraged to produce more trustworthy textual explanations. We introduce KANEx, the first ever framework that leverages the symbolic transparency of KANs to ground VLM reasoning. This interpretability also made it possible to design KAN-Map, a novel heatmap generation method derived directly from KAN models rather than gradient approximations. We feed these grounded contexts into downstream VLMs for enhanced explainability. Benchmarked on the MIMIC-CXR dataset, we demonstrate that KAN-based architectures with ResNet/ViT baselines demonstrate improved semantic similarity while producing significantly more faithful saliency maps. KAN architectures improve visual localization and downstream reasoning quality by 10%. Our findings suggest that grounding linguistic explanations and visual attributions in mathematically interpretable units is a necessary step toward trustworthy medical AI.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：计算机视觉模型在医学影像分析中表现出色，但其“黑箱”特性严重削弱了临床医生的信任。虽然现有工作将胸部X光分类器与视觉语言模型（VLM）结合以生成自然语言解释，但这些系统仅增加了语言流畅性，并未解决底层视觉模型的不透明性。医生仍无法理解模型为何做出特定诊断，导致可信度不足。
- **整体含义**：论文旨在利用Kolmogorov-Arnold网络（KAN）的架构透明性——其样条基函数提供内在可解释的功能单元——来生成更可信的文本解释和可视化归因。作者认为，将语言解释和视觉归因锚定在数学可解释单元上是实现可信医学AI的必要步骤。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：提出KANEx框架，首次利用KAN的符号透明性来锚定VLM推理。通过将视觉模型中的MLP分类头替换为KAN变体，获得可解释的样条函数映射，进而设计KAN-Map热图生成方法，并将这些锚定上下文输入下游VLM，生成更忠实于模型内部机理的语言解释。
- **关键技术细节**：
  - **KAN网络结构**：KAN将MLP中的固定非线性激活函数替换为可学习的单变量样条函数（B样条）位于网络边。公式：\( x^{(l+1)}_i = \Phi_i \left( \sum_{j=1}^{n_l} \phi^{(l)}_{i,j}(x^{(l)}_j) \right) \)，其中\(\phi_{i,j}\)为参数化B样条，\(\Phi_i\)为固定非线性。
  - **KAN-Map热图生成**：给定卷积特征图\(\mathbf{F} \in \mathbb{R}^{C \times H \times W}\)，对每个空间位置\((x,y)\)，将特征向量\(\mathbf{F}_{x,y}\)依次通过训练好的KAN分类器的两个样条层（\(\phi_1: \mathbb{R}^C \to \mathbb{R}^D\)，\(\phi_2: \mathbb{R}^D \to \mathbb{R}^K\)），得到该位置对类别\(k\)的贡献值：\( h_k(x,y) = \phi_{2,k}(\phi_1(\mathbf{F}_{x,y})) \)。无需梯度或线性近似，直接反映样条映射。最终经过ReLU和归一化得到热图。
  - **VLM提示**：将原始X光图像与KAN-Map合成的注意力加权图像（或通道叠加）以及预测诊断标签输入LLaVA模型，通过精心设计的提示（要求生成符合放射科医生风格的报告，包含Findings和Explanation）生成结构化文本解释。

### 3. 实验设计：数据集、基准和对比方法

- **数据集**：使用MIMIC-CXR胸部X光数据集，结合MIMIC-IV中的诊断代码和MIMIC-IV-Note中的病历笔记，构建了20k样例的多模态数据集（含图像、诊断、分割掩码、自然语言理由）。其中30%作为测试集。分割掩码来源于MS-CXR数据集提供的边界框。
- **基准与对比方法**：
  - **视觉骨干**：ResNet (RN50)、Vision Transformer (ViT)作为基线，并将它们的分类头替换为三种KAN变体：VanillaKAN、GroupKAN、RationalKAN。
  - **热图生成方法**：对比Grad-CAM (梯度方法)、Attn-R（注意力滚动，仅用于ViT）和本文提出的KAN-Map。
  - **评估指标**：
    - 视觉可解释性：IoU（与真实边界框的交并比）、Energy@10、Area@50、Faithfulness（Deletion AUC和Insertion AUC之差）。
    - 文本解释质量：LExT-C（基于NER的词汇和事实重叠）。
  - **VLM**：采用LLaVA模型（基于LLaMA骨干），未进行医学领域微调。

### 4. 资源与算力

论文中**未明确说明**使用的GPU型号、数量或训练时长。仅提到所有实验的骨干权重、训练配置和优化设置保持一致，但未提供具体硬件信息。因此无法给出算力细节。

### 5. 实验数量与充分性

- **实验数量**：论文进行了多组实验：
  - 在2种骨干（RN50、ViT）× 3种KAN变体 = 6种模型上，分别测试了3种热图方法（Grad-CAM、Attn-R、KAN-Map），共18组视觉可解释性结果（部分组别Attn-R仅适用于ViT）。
  - 进行了LExT-C文本解释质量对比。
  - 进行了Friedman检验验证IoU分布的统计显著性（p < 0.001）。
  - 展示了热图可视化案例和VLM生成文本示例。
- **充分性评价**：实验设计较为全面，涵盖了不同骨干和KAN变体的组合，对比了多种热图方法，使用了多个维度指标。但存在以下潜在不充分之处：
  - 仅在MIMIC-CXR单一数据集上评估，缺乏对其他医学影像模态（如CT、MRI）的验证。
  - 文本解释质量仅使用LExT-C一个指标，未进行人工评估或临床专家评审。
  - KAN-Map仅在KAN头部上实现，未在完全KAN化（全网络KAN）的模型上测试。
  - 骨干被冻结，仅替换分类头，可能限制了KAN对特征提取的影响。

### 6. 论文的主要结论与发现

- KAN-Map在定位和忠实度上一致优于基于梯度的Grad-CAM和注意力滚动方法，因为其直接利用正向样条映射而非梯度近似。
- 基于KAN的架构（尤其GroupKAN和RationalKAN）相比ResNet/ViT基线，在视觉定位（IoU提升10-15%）和文本解释质量（LExT提升约20%）上有显著提升。
- KAN架构将视觉定位和下游推理质量提升约10%。
- GroupKAN在忠实度和LExT得分上最佳，RationalKAN在IoU上最高，但VanillaKAN和GroupKAN的热图更紧凑（更低的Area@50和更高的Energy@10），表明存在定位精度与聚焦模式的权衡。
- 将可解释性锚定在数学可解释单元（KAN样条）中是迈向可信医学AI的有效途径。

### 7. 优点

- **创新性**：首次将KAN的架构可解释性与VLM的自然语言解释相结合，提出端到端可解释性管道KANEx。
- **方法设计巧妙**：KAN-Map无需反向传播，仅通过正向传播即可获取类激活，计算更高效且避免了梯度近似误差。
- **系统性比较**：全面对比了三种KAN变体、两种骨干、三种热图方法，覆盖了视觉和文本解释两大维度。
- **实用导向**：提示设计贴近临床场景，生成的报告符合放射科医生语言风格。

### 8. 不足与局限

- **实验覆盖有限**：仅在一个胸部X光数据集（MIMIC-CXR）上测试，未在多种医学影像任务（如病灶检测、分割）或非公开数据集上验证，泛化性存疑。
- **方法局限性**：KAN-Map仅适用于KAN分类头，无法直接用于传统MLP头；骨干冻结可能限制了KAN对早期特征的影响；VLM未针对医学领域微调，可能产生不准确的描述。
- **缺少临床评估**：文本解释质量仅依靠自动指标LExT-C，未邀请放射科医生进行人工评分或盲法评估，实际临床价值未知。
- **偏差风险**：数据集来源于单一机构（MIMIC），可能存在人口统计偏差和获取偏差，影响结论的普适性。
- **算力与复现细节缺失**：未报告训练硬件和时间，不利于他人复现和性能比较。

（完）
