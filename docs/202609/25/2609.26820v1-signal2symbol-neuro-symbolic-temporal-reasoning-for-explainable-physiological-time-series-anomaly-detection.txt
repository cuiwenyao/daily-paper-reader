Title: Signal2Symbol: Neuro-Symbolic Temporal Reasoning for Explainable Physiological Time-Series Anomaly Detection

URL Source: https://arxiv.org/pdf/2609.26820v1

Published Time: Thu, 24 Sep 2026 00:01:49 GMT

Number of Pages: 9

Markdown Content:
# Signal2Symbol: Neuro-Symbolic Temporal Reasoning for Explainable Physiological Time-Series Anomaly Detection 

Naser Mansour 1, Sidahmed Benabderrahmane 1* , Ameer Rahwan 21 New York University, NYUAD, Computer Science Department, Division of Science. 

2 Koc University, Faculty of Medicine, Turkey. {nmd9736@nyu.edu; sidahmed.benabderrahmane@gmail.com; arahwan24@ku.edu.tr} 

Abstract —Physiological time series such as electrocardiograms (ECG) and electroencephalograms (EEG) exhibit complex tem-poral structure, substantial acquisition variability, and a strong need for transparent decision-making. Although deep models can achieve high detection performance, they often provide limited insight into why a segment is anomalous, how local anomalies relate over time, and whether a detection belongs to a broader recurring pattern. We propose Signal2Symbol, a neuro-symbolic framework for explainable biosignal anomaly detection. The method first converts ECG/EEG signals into symbolic sequences using either a learned VQ-VAE (Vector Quantized Variational Autoencoder) codebook or a SAX (Symbolic Aggregate approX-imation) baseline. It then constructs bigram-enriched token-window transactions and scores anomalies through rare itemset evidence derived from minimal rare itemset mining. Detected anomalous windows are merged into intervals and related using Allen interval algebra, enabling composite temporal explanations such as escalation chains, artifact overlap, and cross-channel synchrony. Finally, we introduce a rare temporal concept lat-tice based on Formal Concept Analysis (FCA), which groups anomalous intervals by shared rare symbolic evidence, Allen temporal relations, channel context, and robustness attributes. The resulting Galois lattice compresses many local detections into interpretable families of temporal-symbolic anomalies. We evaluate on three public benchmarks—MIT-BIH Arrhythmia (beat-level ECG), PTB-XL (record-level ECG), and the Bonn EEG dataset (segment-level EEG)—and stress-test robustness under additive noise and baseline-wander perturbations. The results highlight the value of neuro-symbolic tokenization for temporal anomaly analysis and show that Allen/FCA reasoning provides compact, interpretable summaries of local detections. 

Index Terms —Physiological Time Series, Anomaly Detection, Allen Algebra, Formal Concept Analysis, Galois lattice, XAI. 

I. I NTRODUCTION 

Physiological time-series anomaly detection is central to e-health, clinical monitoring, and biomedical decision support [1], [2]. Signals such as ECG and EEG contain subtle temporal structures that may indicate arrhythmias, ischemic changes, seizures, abnormal brain dynamics, or acquisition artifacts [3], [4]. In practice, physiological signals are affected by sensor noise, baseline wander, device differences, missing channels, and population shift. A useful anomaly detector must therefore be robust to drift while also providing interpretable evidence. Most modern machine learning methods output a numerical anomaly score or a saliency heatmap. Such outputs may be useful for ranking, but they often fail to answer three questions that matter to analysts and clinicians: What symbolic pattern made this segment unusual? How does this anomaly relate to nearby events over time? Does this interval belong to a recurring family of anomalies? 

Key idea. We convert continuous physiological signals into symbolic sequences, detect rare symbolic co-occurrences, lift local detections into temporal intervals, and organize the resulting intervals into interpretable concept lattices. The pipeline follows: signal → tokens → transactions → rare patterns 

→ intervals → Allen relations → FCA concepts .

This design combines neural representation learning, symbolic rarity mining, temporal reasoning, and concept-based explana-tion. 

Contributions. This paper makes four main contributions:  

> •

Symbolization for biosignals: We compare learned VQ-VAE tokenization against SAX as a transparent symbolic baseline for ECG/EEG signals.  

> •

Rare-pattern anomaly scoring: We propose a rarity-based scoring function that aggregates evidence from minimal rare itemsets over bigram-enriched sliding symbolic windows, and connect the weighting to an information-theoretic surprisal interpretation.  

> •

Temporal interval reasoning: We transform high-scoring windows into anomaly intervals and use Allen interval algebra to detect temporal configurations such as escalation, artifact overlap, staged onset, and cross-channel synchrony.  

> •

Rare temporal concept lattices: We introduce an FCA/Galois-lattice layer that groups anomalous intervals according to shared rare motifs, Allen relations, channel context, reconstruction fidelity, and robustness attributes, yielding compact visual explanations. II. R ELATED WORK 

Symbolic representations of time series. Symbolic Aggre-gate approXimation (SAX) [9] and related discretization meth-ods transform continuous time series into symbolic strings, en-abling motif discovery, rule mining, indexing, and interpretable analysis [16]. SAX is attractive because it is simple, fast, and transparent, but its quantization is based mainly on amplitude arXiv:2609.26820v1 [cs.LG] 20 Sep 2026 discretization and may miss morphology-level differences in ECG or EEG waveforms [17]. 

Discrete neural tokenizers. Vector-Quantized VAEs (VQ-VAE) [10] learn a discrete codebook in a latent space. Each signal window is encoded, assigned to the nearest codebook vector, and decoded for reconstruction. The code index can be interpreted as a learned symbol. Compared with fixed discretization, VQ-VAE learn prototypes to capture recurring waveform shapes, for a suitable biosignal tokenization. 

Anomaly detection and explainability in biosignals. 

ECG/EEG anomaly detection spans feature-based models, density estimators, isolation-based methods, autoencoders, CNNs, transformers, and hybrid pipelines [5], [1], [2], [3], [4]. Explainability is often provided through saliency maps or reconstruction errors. In contrast, symbolic approaches can provide direct evidence in the form of motifs, co-occurrences, and rules [6]. 

Temporal reasoning. Allen interval algebra [8] de-fines qualitative relations between time intervals, includ-ing before , meets , overlaps , starts , during , and 

finishes . In clinical monitoring, interval-level reason-ing helps convert local detections into higher-level tempo-ral patterns such as escalation, repeated bursts, or artifact-contaminated detections. 

Formal Concept Analysis. Formal Concept Analysis (FCA) [11] derives a concept lattice from a binary relation between objects and attributes. Each formal concept groups a set of objects sharing a maximal set of attributes. FCA has been used for interpretable knowledge discovery because it organizes patterns in a lattice structure rather than as an unstructured list of rules. In this work, we use FCA as an explanation layer over detected anomaly intervals: objects are anomalies, and attributes are rare symbolic evidence, Allen relations, and signal-context predicates. III. P ROBLEM FORMULATION AND OVERVIEW 

A. Problem Statement 

Let x(t) ∈ RC denote a multichannel physiological time series of length T with C channels, corresponding to ECG leads or EEG sensors. We segment x(t) into N units: 

{(xi, [tsi , t ei ]) }Ni=1 ,

where each xi is either an ECG beat-centered window or a fixed-length EEG window. The objective is to produce: 1) a record-level anomaly score Srec (x);2) a sequence of window-level scores {Swin (wj )};3) anomaly intervals I(x) = {(asj , a ej , conf j )};4) local explanations through rare symbolic patterns; 5) temporal explanations through Allen relations; 6) concept-level explanations through an FCA lattice. 

B. Pipeline Overview 

Figure 1 summarizes the complete pipeline. Raw signals are first symbolized using SAX or VQ-VAE. The resulting to-ken stream is converted into bigram-enriched sliding-window 

Fig. 1: Signal2Symbol pipeline. Raw ECG/EEG signals are tokenized using SAX or VQ-VAE, transformed into bigram-enriched symbolic transactions, scored by rare-pattern evi-dence, merged into intervals, related through Allen algebra, and organized into FCA/Galois lattices for visual explanation. transactions. Rare itemsets define a symbolic anomaly score. High-scoring windows are merged into anomaly intervals. Allen relations are computed between intervals. Finally, FCA organizes anomalous intervals into a rare temporal concept lattice. IV. S YMBOLIZATION OF TIME SERIES 

A. SAX Baseline 

Given a univariate series y(t), SAX [9] applies z-normalization, Piecewise Aggregate Approximation (PAA), and discretization into an alphabet of size A. For multichannel data, we apply SAX per channel or per selected channel group and concatenate the resulting symbols. SAX provides a simple and transparent baseline for symbolic anomaly detection. 

Hyperparameters. We vary alphabet size A ∈ { 4, 5, 6}

and PAA resolution. These parameters control the trade-off between coarse robustness and fine symbolic resolution. 

B. VQ-VAE Tokenization 

VQ-VAE [10] learns a dictionary of prototypical signal shapes. For each segment xi, the encoder produces a latent vector: hi = Eθ (xi) ∈ Rd. The vector is mapped to the nearest codebook entry: zi = arg min k∈{ 1,...,K } ∥hi − ek∥2,where {ek}Kk=1 is the learned codebook. The selected entry is decoded: ˆxi = Dϕ(ezi ). The discrete index zi is converted into a categorical symbolic token: Bzi for an ECG beat-centered segment and Ezi for an EEG window. Thus, an ECG segment assigned to codebook entry 17 becomes B_17 ,while an EEG segment assigned to entry 42 becomes E_42 .Because each token inherits the timestamp interval [tsi , t ei ]

of its source segment, the tokenizer outputs a time-aligned symbolic sequence. 

Training. We train VQ-VAE exclusively on normal signal segments. The codebook therefore learns prototypes of typical waveform morphologies. When an anomalous segment is pre-sented at test time, it is mapped to its nearest normal prototype; the anomaly signal arises from unusual combinations of these prototypes across sliding windows. This relational design Fig. 2: VQ-VAE tokenization. Each ECG beat or EEG win-dow is encoded, assigned to the nearest codebook prototype, reconstructed through a decoder, and represented as a symbolic token aligned with timestamps. means that codebook granularity and utilization directly affect sensitivity: dead codes reduce effective resolution, motivating the use of exponential moving average (EMA) codebook updates and codebook utilization monitoring. 

Reconstruction fidelity. We also record the per-segment reconstruction error ri = ∥xi −ˆxi∥2, which provides a comple-mentary anomaly signal. Segments that are poorly represented by any codebook prototype yield high reconstruction error and are flagged via a dedicated FCA attribute (Section VIII). 

Segmentation. ECG signals are represented using either R-peak-centered beat windows or complete record-level seg-ments, depending on the evaluation granularity. EEG signals are divided into fixed-length windows with overlap. 

Ablations. We study codebook sizes K ∈ { 128 , 256 , 512 }

and analyze the impact of dead codes, reconstruction quality, and token entropy. The output of this section is a timestamped symbolic sequence: 

S(x) = {(si, [tsi , t ei ]) }Ni=1 , si ∈ { 1, . . . , K } or Σ.

V. T RANSACTION CONSTRUCTION AND RARE PATTERN 

MINING 

A. Window-to-Transaction Mapping 

We slide a symbolic window of length W tokens over S(x).Each symbolic window is transformed into a transaction: 

wj = {si : i ∈ window j}.

Bigram enrichment. Converting a symbolic window to a set discards sequential order, which may cause the method to miss ordering-sensitive anomalies (e.g., ST-elevation followed by T-wave inversion in ECG). To partially recover ordering information while remaining within the itemset mining frame-work, we augment each transaction with consecutive token bigrams: 

w+ 

> j

= wj ∪ { (si → si+1 ) : i, i +1 ∈ window j}.

For example, the token sequence [A, B, C ] yields the enriched transaction {A, B, C, A →B, B →C}. Bigrams add lightweight order sensitivity without requiring a full sequential pattern miner. The corresponding transaction database is: 

T = {(w+ 

> j

, [τ sj , τ ej ]) }Mj=1 .

where ( τ sj ,τ ej ) is the time interval spanned by the tokens inside transaction ( w+ 

> j

). 

B. Rare Itemsets and Rare Rules 

Let I be an itemset. Its support on the normal training transaction database is: supp (I) = 1

|T train |

X

> w∈T train

1[I ⊆ w].

An itemset is rare if supp (I) ≤ τ . We denote the family of mined rare itemsets as R.

Monotonicity of rarity. If supp (I) ≤ τ , then for any superset I′ ⊃ I, supp (I′) ≤ supp (I) ≤ τ . Therefore the family of rare itemsets is upward-closed , and minimal rare itemsets (MRIs) serve as generators of the entire rare family. Mining only MRIs avoids redundancy: we identify the boundary between frequent and rare without enumerating exponentially many supersets [7]. 

Mining backend. We use a level-wise BtB (Break-the-Barrier) algorithm that extends Apriori with a rarity boundary: frequent itemsets at level k generate candidates at level k+1 ;candidates whose support falls below τ are recorded as MRIs and excluded from further expansion. This yields the complete set of MRIs efficiently. VI. R ARE -P ATTERN ANOMALY SCORING 

A. Window-Level Score 

For a transaction window wj , we define: 

Swin (wj ) = X 

> I∈R :I⊆wj

α(I), (1) where: 

α(I) = g(|I|)

supp (I) + ε . (2) Here g(|I|) = log(1 + |I|) and ε is a small constant for numerical stability. 

Information-theoretic interpretation. The weight α(I)

is inspired by surprisal: rarer itemsets should receive larger weights. Instead of using the logarithmic term − log supp( I),we use the inverse-frequency factor 1/(supp( I) + ϵ) as a stronger rarity amplifier, since it grows faster as support approaches zero. The length factor g(|I|) = log(1 + |I|)

mildly favors structured multi-token co-occurrences over rare singletons. 

B. Record-Level Score 

A record-level anomaly score aggregates the most suspi-cious windows: 

Srec (x) = TopKMean  {Swin (wj )}Mj=1 

 . (3) Top-K aggregation is robust to isolated noise while preserving localized abnormal bursts. C. Interval Extraction 

Window scores are transformed into anomaly intervals through thresholding and merging: 1) mark wj anomalous if Swin (wj ) > γ ;2) merge consecutive anomalous windows within a gap tolerance; 3) assign each interval a confidence using the max or mean score; 4) attach top-K triggered rare itemsets as local evidence. For each detected interval a, we define: Evidence (a) = TopK {(I, α (I)) : I ∈ R , I ⊆ wj , w j ⊆ a}.

VII. A LLEN TEMPORAL REASONING 

A. Allen Relations 

Given two intervals A = (as, a e) and B = (bs, b e),Allen interval algebra [8] defines 13 mutually exclusive qualitative relations: before , after , meets , met-by ,

overlaps , overlapped-by , starts , started-by ,

during , contains , finishes , finished-by , and 

equals (see Figure 1, step 9). We use these relations to describe how anomaly intervals interact with other anomalies, artifacts, and contextual events. 

B. Composite Event Library 

We define a compact temporal rule library:  

> •

Escalation: repeated anomaly intervals that meet or strongly overlap , e.g., A1 meets A2 meets A3. In ECG, this captures PVC couplets and triplets; in EEG, seizure buildup.  

> •

Artifact overlap: an anomaly interval overlaps aknown artifact interval, suggesting reduced detection con-fidence.  

> •

Staged onset: an anomaly starts shortly after another event within a tolerance ∆t, capturing pre-ictal to ictal transitions.  

> •

Cross-channel synchrony: anomaly intervals on differ-ent channels overlap or start together, indicating a spatially distributed event. 

C. Temporal Relation Graph 

We build a graph E = ( V, E ) where nodes are intervals and edges carry labeled Allen relations: 

(ai, ρ, a j ), ρ ∈ { meets , overlaps , . . . }.

Composite events are obtained by matching small patterns in this graph. 

Confidence refinement. Allen relations also enable post-hoc confidence adjustment: intervals participating in an 

artifact-overlap relation have their confidence dis-counted, while intervals forming an escalation chain have their confidence reinforced. This mechanism provides a principled way for temporal reasoning to influence final anomaly reporting beyond pure explanation. VIII. R ARE TEMPORAL CONCEPT LATTICES 

This section introduces the main explanation layer of the paper. Allen reasoning produces labeled temporal relations, but a record or cohort may still contain many intervals and many local explanations. To summarize them, we build an FCA formal context in which objects are detected anomaly intervals and attributes describe their symbolic, temporal, and signal-context properties. 

A. Formal Context 

A formal context [11] is a triple: K = ( G, M, J ), where 

G is the set of detected anomaly intervals, M is the set of binary attributes, and J ⊆ G × M is the incidence relation. 

(a, m ) ∈ J means that interval a has property m.

B. Attribute Construction 

We define five families of attributes. 

1. Rare symbolic attributes. For each rare itemset I ∈ R ,we create: mI = fires( I). An interval a satisfies mI if at least one transaction inside a contains I.

2. Temporal attributes. For each Allen-derived com-posite, we create predicates such as: overlaps -artifact ,

meets -anomaly , escalation -member , starts -after -event .

3. Signal-context attributes. These include channel/lead information and duration bins: lead -V5 , frontal -EEG ,

duration -short , duration -long .

4. Reconstruction fidelity attributes. When VQ-VAE to-kenization is used, we record the mean reconstruction error per interval. Intervals with error above the 90th percentile of normal training errors are assigned: high -recon -error . This at-tribute flags intervals that are poorly represented by any normal codebook prototype, complementing the rare co-occurrence signal. 

5. Robustness attributes. During robustness tests, intervals may be labeled: stable -under -noise , stable -under -wander ,or unstable -under -perturbation . Additional attributes can be added when further perturbations are evaluated. 

C. Formal Concepts 

For A ⊆ G , define A′ = {m ∈ M : ∀a ∈ A, (a, m ) ∈ J } .For B ⊆ M , define B′ = {a ∈ G : ∀m ∈ B, (a, m ) ∈ J } . A formal concept is a pair (A, B ) such that A′ = B and B′ = A.Here A is the extent (anomaly intervals) and B is the intent 

(maximal shared properties). Thus, a concept may be interpreted as: 

{a1, a 2, a 7}

| {z }

> similar anomalies

⇐⇒ { fires( {B42 , B 91 }), meets -anomaly , lead -II }

| {z }

> shared explanation

.

D. Toy Example 

To illustrate the construction, consider asimplified dataset with five anomaly intervals 

G = {A1, . . . , A 5} and five representative attributes 

M = {R1, R 2, long-duration , escalation , overlap-artifact },where R1 and R2 represent distinct rare symbolic patterns. The formal context is represented as a binary cross-table (Table I). TABLE I: Toy Formal Context mapping anomaly intervals to attributes.                                      

> Interval R1R2long-duration escalation overlap-artifact
> A1××
> A2×××
> A3××
> A4×××
> A5××××⊤
> |ext|={A1, A2, A3, A4, A5} {R1, long-dur} |ext|={A1, A2, A5} {overlap-artifact} |ext|={A2, A4} {R2, escalation} |ext|={A3, A4, A5} {R1, long-dur, overlap-artifact} |ext|={A2} {R1, R2, long-dur, escalation} |ext|={A5} {R2, escalation, overlap-artifact} |ext|={A4}
> ⊥

Fig. 3: Concept lattice of the toy formal context in Table I. Applying FCA to this context generates the concept lattice shown in Figure 3. The lattice organizes the intervals hierar-chically based on shared attributes. The top node ( ⊤) covers all intervals, while deeper nodes represent more specific concepts (more attributes, fewer intervals). For example, the concept 

{R1, long-duration } groups intervals {A1, A 2, A 5}, while its sub-concept {R1, long-duration , overlap-artifact } specifically isolates {A2}.

E. Iceberg Lattice and Explanation Compression 

Full concept lattices may be exponentially large in |M| .We therefore compute an iceberg lattice by retaining only concepts whose extent satisfies |A| ≥ σ, and restrict attributes to the top-K most discriminative rare patterns and the most informative Allen/context predicates. 

Complexity. Standard algorithms such as NextClosure [11] enumerate concepts in O(|G| · |M| · | concepts |) time. The iceberg threshold σ and attribute filtering to top-K together bound the effective number of concepts, keeping computation tractable for typical anomaly counts (tens to low hundreds of intervals per record or cohort). The concept lattice acts as an explanation-compression mechanism. Instead of reporting all local interval explanations independently, we report concept summaries: Concept c =(Ac, B c) : |Ac| intervals share Bc.Algorithm 1 provides the full pseudo-code of the proposed Signal2Symbol pipeline. 

F. Example Interpretation 

A concept with intent Bc =

{fires( {B42 , B 91 }), overlaps -artifact , lead -V5 } may be summarized as: “A family of V5 anomaly intervals is driven by the rare token co-occurrence {B42 , B 91 }

but overlaps artifact intervals; these detections should be interpreted with reduced confidence.” 

A concept with intent Bc =

{fires( {B17 , B 91 }), escalation -member , duration -long }

may be summarized as: “A repeated long-duration anomaly type appears as an escalation chain, suggesting a sustained arrhythmic episode or seizure buildup.” 

Algorithm 1 Signal2Symbol with Rare Temporal Concept Lattice 

Require: Signal record x(t), tokenizer type, window length 

W , rarity threshold τ , score threshold γ, support σ

Ensure: Record score, anomaly intervals, rare evidence, Allen graph, concept lattice 

1: Segment x(t) into windows or beats {xi}Ni=1 

2: Convert segments into tokens via SAX or VQ-VAE 

3: Build bigram-enriched transactions T = {w+ 

> j

}Mj=1 

4: Mine MRI rare itemsets R on normal training transactions 

5: for each transaction w+ 

> j

do 

6: Compute Swin (wj ) via Eq. (1) 

7: end for 

8: Aggregate Srec (x) via Eq. (3) 

9: Extract anomaly intervals I(x)

10: Attach top-K rare symbolic evidence to each interval 

11: Compute Allen relations among anomaly and context intervals 

12: Refine interval confidence via Allen-based adjustment 

13: Construct FCA context K = ( G, M, J ) with five attribute families 

14: Generate iceberg concept lattice with support σ

15: return scores, intervals, evidence, Allen graph, concept lattice IX. E XPERIMENTS 

A. Datasets 

We evaluate on two ECG benchmarks and one EEG bench-mark, chosen to cover beat-level, record-level, and segment-level granularities across two signal modalities.  

> •

MIT-BIH Arrhythmia Database [12]: 48 half-hour, two-channel ambulatory ECG recordings at 360 Hz with 

∼110,000 beat-level annotations spanning normal (N) and arrhythmic types (PVC, APC, LBBB, RBBB, Paced). We segment by R-peak and use beat-centered windows. Normal beats form the training set; arrhythmic beats are anomalies. Patient-wise splits prevent leakage. This dataset is our primary benchmark: beat-level annota-tions permit full evaluation of interval detection, Allen reasoning, and FCA concept formation across distinct arrhythmia families. Here, each beat is represented by a 216-sample R-peak-centered window, corresponding to 600 ms at 360 Hz.  

> •

PTB-XL [13]: 21,837 twelve-lead, 10-second ECG records at 100 Hz with record-level diagnostic labels (NORM, MI, STTC, CD, HYP). We use NORM records for training and pathological records as anomalies. Patient-wise stratified splits follow the recommended folds. PTB-XL tests scalability to a large modern bench-mark at record-level granularity. Here, we use the com-plete 10-s lead-II record, with 1,000 samples at 100 Hz.  

> •

Bonn EEG Dataset [14]: 500 single-channel EEG seg-ments (23.6 s each, 173.61 Hz) across five classes: eyes-open (A), eyes-closed (B), interictal hippocampal (C), interictal epileptogenic (D), and ictal/seizure (E). We use classes A–B as normal and C–E as varying degrees of abnormality. Here, each recording is divided into 512-sample windows, corresponding to approximately 2.95 s at 173.61 Hz, with 50% overlap. 

B. Baselines 

We compare five scoring backbones: IF (Isolation For-est, 200 trees) and LOF (k = 20 ) applied to flattened time-domain features; USAD [15], a deep learning base-line of two adversarially-trained autoencoders whose com-bined reconstruction error serves as the anomaly score; 

SAX+RareScore , using SAX tokens with rare-pattern scoring; and Signal2Symbol (VQ-VAE+RareScore), our full neuro-symbolic backbone. Detection metrics are reported for all scoring backbones; robustness is evaluated under signal per-turbations, while Allen/FCA are assessed through explanation-compression and case-study analyses. 

C. Metrics 

For detection we report AUROC and AUPRC. Explanation quality is assessed via: (i) rule sparsity (average evidence patterns per interval); (ii) concept compression ratio (local explanations divided by retained lattice concepts); (iii) concept stability (Jaccard overlap of concept intents across bootstrap samples); and (iv) mean intent length (average attributes per concept). Robustness is measured as ∆AUROC under Gaus-sian noise injection (SNR ∈ { 5, 10 , 20 } dB) and sinusoidal baseline wander (amplitudes ∈ { 0.1, 0.3, 0.5} mV). 

D. Implementation Details 

The VQ-VAE encoder consists of three Conv1d layers (32, 64, 128 filters) with a linear projection to R64 ; the codebook (K = 128 ) uses EMA updates with commitment weight β =0.25 and is trained for 100 epochs on normal segments with MSE loss. USAD shares the same encoder architecture with two decoders trained adversarially for 100 epochs. For SAX, we use alphabet size A = 5 and PAA reduction to 16 segments per window. The rare itemset miner uses τ = 0 .02 and max itemset length 5, with bigram-enriched transactions of window size W = 8 and step size 1. Anomaly scoring uses ε = 10 −6,Top-K aggregation ( K = 5 ), and score threshold γ at the 95th percentile of normal validation scores. The FCA iceberg lattice retains concepts with support σ ≥ 2 and top-50 attributes. 

E. Main Detection Results 

Table II reports detection performance across all three datasets. The results show that learned symbolic tokenization consistently improves rare-pattern anomaly ranking over SAX. VQ-VAE+Rare achieves the best AUROC and AUPRC on all three datasets, with particularly large gains on PTB-XL and MIT-BIH, suggesting that learned codebook prototypes capture morphology-level variations that fixed SAX discretiza-tion misses. On Bonn EEG, all symbolic methods obtain high AUPRC, while VQ-VAE+Rare further improves AUROC, indicating better separation between normal and abnormal TABLE II: Detection results across ECG and EEG datasets. MIT-BIH is evaluated at beat-level, PTB-XL at record-level (lead II only), and Bonn at segment-level.                                     

> Dataset Method AUROC ↑AUPRC ↑
> MIT-BIH  IF (200 trees) 0.790 0.405 LOF ( k= 20 )0.737 0.423 USAD 0.750 0.341 SAX+Rare 0.605 0.198 Signal2Symbol (VQ-VAE+Rare) 0.818 0.547
> PTB-XL  IF (200 trees) 0.546 0.615 LOF ( k= 20 )0.517 0.592 USAD 0.516 0.588 SAX+Rare 0.493 0.574 Signal2Symbol (VQ-VAE+Rare) 0.742 0.634
> Bonn  IF (200 trees) 0.469 0.876 LOF ( k= 20 )0.512 0.896 USAD 0.550 0.900 SAX+Rare 0.579 0.911 Signal2Symbol (VQ-VAE+Rare) 0.710 0.959

TABLE III: Ablation study: effect of tokenizer, alphabet size, and PAA resolution on Bonn EEG.                             

> Setting AUROC ↑AUPRC ↑Notes
> Tokenizer
> SAX ( |Σ|= 5 , P = 16 )0.579 0.911 fixed discretization SAX ( |Σ|= 6 , P = 16 )0.536 0.898 larger alphabet SAX ( |Σ|= 6 , P = 20 )0.512 0.891 more segments VQ-VAE ( K= 128 )0.710 0.959 learned codebook VQ-VAE ( K= 256 )0.710 0.959 saturates at K= 128

EEG segments. IF and LOF provide competitive classical baselines, while USAD offers a deep reconstruction baseline; nevertheless, Signal2Symbol achieves the best AUROC and AUPRC across all three datasets. These results support the use of VQ-VAE tokens as a stronger symbolic substrate for the subsequent Allen/FCA explanation layers. 

F. Ablation Study 

Table III ablates key design choices on the Bonn EEG dataset. The ablation confirms that learned VQ-VAE tokens provide a stronger symbolic representation than SAX on Bonn EEG, with performance saturating already at K = 128 .Increasing the SAX alphabet or PAA resolution does not improve detection, suggesting that finer fixed discretization may fragment useful motifs. 

G. Sensitivity to Support Threshold 

The pipeline involves interacting thresholds, notably the rarity threshold τ used during mining. To evaluate the robust-ness of our models to this hyperparameter, we performed a grid search over τ ∈ [0 .01 , 0.40] on the Bonn EEG dataset (Figure 4). The VQ-VAE ( K = 128 ) model demonstrates 0.01 0.02 0.05 0.1 0.2 0.4 Support Threshold (log scale)           

> 0.50 0.55 0.60 0.65 0.70 AUROC
> Sensitivity of AUROC to
> VQ-VAE ( K= 128) SAX (| | = 5)
> 0.01 0.02 0.05 0.1 0.2 0.4 Support Threshold (log scale)
> 0.88 0.89 0.90 0.91 0.92 0.93 0.94 0.95 0.96 AUPRC
> Sensitivity of AUPRC to
> VQ-VAE ( K= 128) SAX (| | = 5)

Fig. 4: Sensitivity of AUROC and AUPRC to the support threshold τ on the Bonn EEG dataset. The VQ-VAE model exhibits remarkable stability across lower thresholds. remarkable stability, maintaining its peak AUROC of 0.710 TABLE IV: Explanation quality across datasets. Compression is the ratio of detected anomaly intervals to retained iceberg-lattice concepts; Intent is the mean number of attributes per retained concept.                 

> Dataset Intervals Concepts Compression ↑Intent ↓
> Bonn EEG 2061 40 51.5 ×3.2
> MIT-BIH 2154 32 67.3 ×4.1
> PTB-XL 585 144 4.1 ×4.0

across all τ ≤ 0.05 . This occurs because the model extracts a highly specific, robust set of 21 Minimal Rare Itemsets (MRIs) that capture the dominant anomalous symbolic configurations. In contrast, the SAX tokenizer is much more sensitive to thresholding, requiring the mining of over 4,000 dense MRIs at 

τ = 0 .01 to marginally improve from 0.601 to 0.631 AUROC, sacrificing explainability. These results confirm that our neuro-symbolic approach is highly robust to hyperparameter selec-tion and does not require aggressive grid searching to achieve peak anomaly detection performance. Preliminary sensitivity checks on the remaining datasets showed the same trend. 

H. Explanation Quality 

Table IV reports explanation metrics on Bonn EEG, PTB-XL and MIT-BIH. As illustrated, our full pipeline achieves significant explanation compression across all three primary datasets. For Bonn EEG, the FCA layer systematically groups 2,061 individual anomaly intervals into just 40 distinct formal concepts, achieving a 51 .5× compression ratio. On the much larger MIT-BIH dataset, the compression is even stronger at 

67 .3×, collapsing 2,154 raw anomaly intervals into 32 high-level prototypes. Even on PTB-XL’s complex, multi-pathology ECG records, the lattice compresses 585 raw anomalous intervals down to 144 formal concepts (a 4.1× reduction). This allows domain experts to review a manageable handful of anomaly prototypes rather than thousands of isolated events. Furthermore, the average concept intent across all datasets consists of roughly 3 to 4 attributes, meaning each anomaly group is concisely described by a small, interpretable set of temporal and symbolic properties. 

I. Robustness 

Table V reports ∆AUROC under perturbation on Bonn EEG. The VQ-VAE tokenizer shows no observable AUROC degradation under the tested perturbation settings, maintaining the same measured AUROC of 0.710 within the reported precision ( ∆ = 0.000 ) across all tested noise levels (up to 5 dB SNR) and baseline wander amplitudes. In contrast, the SAX baseline suffers minor degradation under noise and a more noticeable performance drop ( ∆ = −0.024 ) when subjected to baseline wander. The neural codebook’s ability to smoothly map corrupted continuous segments to the same discrete latent prototypes makes it highly resilient to common clinical artifacts. 

J. Case Study: Explainability via Concept Lattices 

We present a qualitative case study on the Bonn EEG dataset (Set E, seizure recordings) to demonstrate how Formal TABLE V: Robustness (∆AUROC vs. clean) on Bonn EEG. Noise column reports worst-case ∆ across SNR 

∈ {5, 10 , 20 } dB; Wander column across amplitudes ∈{0.1, 0.3, 0.5}.             

> Method Clean AUROC Noise ∆Wander ∆
> SAX+Rare ( |Σ|= 5 )0.579 −0.002 −0.024 VQ-VAE+Rare ( K= 128 )0.710 0.000 0.000

Concept Analysis (FCA) provides structured explainability. Figure 5 visualizes a concept lattice built from 30 detected anomaly intervals during a seizure event. 

Local Symbolic Evidence. The symbolic tokenizer and rare pattern miner identify specific rare transitions and co-occurrences (e.g., R1, R 2) that flag these windows as anoma-lous against the background normal activity. 

Temporal Reasoning. The Allen temporal graph connects these intervals. The reasoner identifies that these anomalies are contiguous and overlapping, forming extended periods of abnormal activity, leading to the long-duration attribute. 

Concept Lattice. Rather than presenting the clinician with 30 independent alarms, the FCA lattice (Figure 5) organizes them hierarchically. The top concept ( ⊤) encompasses all 30 intervals, establishing that they all share the long-duration 

temporal characteristic. Sub-concepts then branch out based on specific subsets of rare patterns that fired within those intervals. For instance, the lattice groups sets of intervals that co-exhibit specific rare patterns, effectively compressing 30 distinct anomalies into 14 interpretable, rule-based families. X. D ISCUSSION 

Signal2Symbol is designed to make physiological anomaly detection explainable at three levels. First, rare itemsets pro-vide local symbolic evidence : they identify the unusual token co-occurrences that caused a window to be scored as anoma-lous. Second, Allen relations provide temporal structure :they describe how intervals meet, overlap, or follow one another, and enable confidence refinement. Third, FCA pro-vides conceptual compression : it groups intervals into shared temporal-symbolic anomaly families. This combination is im-portant because physiological time series often contain many local irregularities, not all of which are clinically meaningful. A list of isolated detections is difficult to interpret. A concept lattice, however, can reveal that several intervals share the same rare motif and the same temporal behavior. For example, artifact-overlap concepts can be separated from escalation concepts, and cross-channel concepts can be distinguished from single-channel events. 

Why VQ-VAE helps. SAX is transparent and efficient, but it discretizes mostly amplitude patterns. VQ-VAE can learn recurring waveform morphologies, making token co-occurrences more semantically meaningful. This is especially relevant for ECG beat morphology (e.g., distinguishing PVC from LBBB shapes) and EEG transient patterns. 

Why bigrams help. Pure set-valued transactions lose all sequential structure. Bigram enrichment recovers pairwise ordering at negligible cost, enabling the detection of anoma-Rare Temporal Concept Lattice                                                                                    

> Bonn EEG ·Set E(Seizure) ·30 anomaly intervals
> Legend
> long-duration
> |ext| =30
> R1, long-duration
> |ext| =3
> R2, R3, long-duration
> |ext| =4
> R4, long-duration
> |ext| =4
> R5, long-duration, R6
> |ext| =4
> R7, long-duration
> |ext| =4
> R8, long-duration
> |ext| =5
> R9, long-duration
> |ext| =5
> R10, long-duration
> |ext| =6
> R12, R13, R14, long-duration
> |ext| =6
> R10, R9, R1, long-duration
> |ext| =2
> R12, R7, R13, long-duration, R14
> |ext| =2
> R12, R13, long-duration, R8, R14
> |ext| =3
> R10, long-duration, R11
> |ext| =3Ri=rare pattern (MRI) R1 =e→c,b,c →b,d →dR2 =b→c,d →b,c →e,b R3 =c,b →c,d →b,c →eR4 =c,c →a,e →d,b R5 =b→c,c →d,c →e,b R6 =c,b →c,c →d,c →eR7 =e→c,c,d →b,c →cR8 =d→e,c →d,d →d,b

Fig. 5: Rare Temporal Concept Lattice generated from 30 anomaly intervals on Bonn EEG (Set E, Seizure). The FCA lattice compresses individual detections into a hierarchy of concepts based on shared temporal attributes (e.g., long-duration ) and rare symbolic patterns ( Ri). Deeper nodes represent more specific anomaly signatures. lies that depend on transition patterns (e.g., normal-to-PVC transitions) without requiring a full sequential pattern miner. 

Why FCA helps. Rare patterns alone may produce many fragmented explanations. FCA organizes them into a lattice of shared attributes, enabling visual inspection and explana-tion compression. This makes the method suitable as an AI reasoning and knowledge-discovery tool. 

Hyperparameter sensitivity. The pipeline involves several interacting thresholds: rarity threshold τ , window size W ,codebook size K, score threshold γ, and concept support σ.We note a qualitative interaction: larger W increases token co-occurrence within transactions, raising support values and requiring a correspondingly higher τ to identify rare patterns. The ablation study varies these parameters individually; a full factorial analysis is left to future work. 

Limitations. First, bigram enrichment recovers only pair-wise order; longer sequential dependencies require n-gram or sequential rule mining. Second, the lattice can become large if too many attributes are included; we address this with iceberg support thresholds and top-K attribute filtering. Third, VQ-VAE may suffer from dead codes without careful training (EMA updates and utilization tracking help). Fourth, the Allen/FCA explanation quality depends on reliable interval extraction; noisy thresholds can fragment or merge intervals incorrectly. 

Per-dataset analysis. On the Bonn EEG dataset, Sig-nal2Symbol (VQ-VAE+Rare) achieves 0.71 AUROC, substan-tially outperforming Isolation Forest (0.46) and SAX+Rare (0.57). EEG seizure activity produces distinctive temporal patterns that are well captured by learned codebook prototypes. On MIT-BIH, IF operates directly on the 216-dimensional beat morphology and achieves 0.79 AUROC, while our symbolic methods reach 0.81 (VQ-VAE) and 0.60 (SAX). On PTB-XL, IF achieves near-random performance (0.54 AUROC), while our approach reaches a score of 0.74. XI. C ONCLUSION AND FUTURE WORK 

We presented Signal2Symbol, a neuro-symbolic pipeline for explainable ECG/EEG anomaly detection. The method combines VQ-VAE/SAX tokenization, bigram-enriched rare-pattern anomaly scoring, Allen interval reasoning with con-fidence refinement, and FCA-based rare temporal concept lattices. Evaluation on MIT-BIH, PTB-XL, and Bonn EEG demonstrates the framework across beat-level, record-level, and segment-level granularities in two signal modalities. The resulting framework produces not only anomaly scores, but also symbolic evidence, temporal relations, and concept-level visual explanations. Future work will extend the current framework with order-aware sequence mining (n-grams and sequential rules), masked token modeling over learned biosig-nal tokens, larger cross-dataset evaluation including CHB-MIT seizure detection, and human-in-the-loop assessment of explanation usefulness in clinical workflows. DECLARATIONS 

The authors declare no competing interests. Public datasets were used; code and processed data are available upon request. REFERENCES [1] Zamanzadeh Darban, Z., et al. (2024). Deep learning for time series anomaly detection: A survey. ACM Computing Surveys, 57(1), 1-42. [2] Liu, Q., et al. (2024). Time-series anomaly detection: Overview and new trends. Proc. of the VLDB Endowment (PVLDB), 17(12), 4229-4232. [3] Tabassum, M., et al. (2024). Anomaly-based threat detection in smart health using machine learning. BMC Medical Informatics and Decision Making, 24(1), 347. [4] Niu, Y., et al. (2025). Explainable machine learning model based on EEG, ECG, and clinical features for predicting neurological outcomes in cardiac arrest patient. Scientific Reports, 15(1), 11498. [5] Arulmurugan, V. S., et al. (2026). Federated learning-enabled privacy-preserving framework for seizure forecasting and affective state analysis using multi-modal EEG-ECG data. Scientific Reports. [6] Liang, J., et al. (2026). Generative Adversarial Networks for Modeling Bio-Electric Fields in Medicine: A Review of EEG, ECG, EMG, and EOG Applications. Bioengineering, 13(1), 84. [7] Szathmary, L., et al. . (2007, October). Towards rare itemset mining. In 19th IEEE international conference on tools with artificial intelligence (ICTAI 2007) (Vol. 1, pp. 305-312). IEEE. [8] J. F. Allen, “Maintaining knowledge about temporal intervals,” Commun. ACM , vol. 26, no. 11, pp. 832–843, 1983. [9] J. Lin, et al. , “A symbolic representation of time series, with implications for streaming algorithms,” in Proc. SIGMOD DMKD Workshop , 2003. [10] A. van den Oord, et al. , “Neural discrete representation learning,” in 

Advances in Neural Information Processing Systems , 2017. [11] B. Ganter and R. Wille, Formal Concept Analysis: Mathematical Foun-dations . Berlin: Springer, 1999. [12] G. B. Moody and R. G. Mark, “The impact of the MIT-BIH arrhythmia database,” IEEE Eng. Med. Biol. Mag. , vol. 20, no. 3, pp. 45–50, 2001. [13] P. Wagner, et al. , “PTB-XL, a large publicly available electrocardiogra-phy dataset,” Scientific Data , vol. 7, no. 1, p. 154, 2020. [14] R. G. Andrzejak, et al. . “Indications of nonlinear deterministic and finite-dimensional structures in time series of brain electrical activity: Dependence on recording region and brain state,” Physical Review E ,vol. 64, no. 6, p. 061907, 2001. [15] Audibert, J., et al. . (2020, August). Usad: Unsupervised anomaly de-tection on multivariate time series. In Proc, of the 26th ACM SIGKDD conference (pp. 3395-3404). [16] Benabderrahmane, S., Mellouli, N., Lamolle, M., Mimouni, N. (2017, November). When deep neural networks meet job offers recommenda-tion. In 2017 IEEE 29th International Conference on Tools with Artificial Intelligence (ICTAI) (pp. 223-230). IEEE. [17] Benabderrahmane, S., Quiniou, R., Guyet, T. (2014, August). Evaluating distance measures and times series clustering for temporal patterns retrieval. In Proceedings of the 2014 IEEE 15th International Conference on Information Reuse and Integration (IEEE IRI 2014) (pp. 434-441). IEEE.