---
title: Neuron Activation-based Computation of Logical Explanations for Deep Neural Networks
title_zh: 基于神经元激活的深度神经网络逻辑解释计算
authors: "Tomáš Kolárik, Faezeh Labbaf, Fabrizio Leopardi, Grigory Fedyukovich, Michael Wand, Natasha Sharygina"
date: 2026-09-12
pdf: "https://arxiv.org/pdf/2609.14099v1"
tags: ["query:ns-xai"]
score: 8.0
evidence: 结合SMT求解器的神经网络符号解释
tldr: 现有神经网络形式化解释要么局限于单个输入特征、无法保证特征关系，要么难以扩展到深层架构。本文提出基于内部神经元激活参数化的符号框架，借助SMT等通用逻辑引擎，引导式高效计算网络行为的逻辑解释。方法不依赖专用验证器，解释形状不受限，并在图像识别与医学基准上验证了计算效率优势。它还能为先前逻辑方法难以处理深度网络生成逻辑解释。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.14099v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1102, \"height\": 484, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.14099v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1171, \"height\": 787, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.14099v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1183, \"height\": 787, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.14099v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1205, \"height\": 361, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.14099v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1065, \"height\": 460, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.14099v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1032, \"height\": 444, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.14099v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 475, \"height\": 263, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.14099v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1182, \"height\": 796, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.14099v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1258, \"height\": 830, \"label\": \"Figure\"}]"
motivation: 现有形式化解释或仅限单个输入特征且不保证关系，或无法扩展至深层网络。
method: 提出由内部神经元激活参数化的符号框架，用SMT等通用逻辑引擎引导计算逻辑解释，不依赖专用验证器。
result: 在图像识别和医学基准上实验，显示计算效率优势，并能为先前逻辑方法难处理的深度网络生成解释。
conclusion: 该框架将网络特定编码与算法分离，扩展了逻辑解释的可扩展性与适用范围。
---

## 摘要
分类神经网络（NN）的形式化可解释性是一个活跃的研究领域，它能够在输入特征空间的连续区域内为分类提供具有可证明保证的解释。然而，现有技术要么局限于单个输入特征，无法保证它们之间的关系，要么所提供的解决方案无法扩展到深层架构。本文通过引入一个灵活符号框架来解决这些问题，该框架用于高效、引导式地计算 NN 行为的解释，以内部神经元的激活为参数，并使用 SMT 求解器等逻辑引擎。与依赖专用 NN 验证器的先前方法不同，我们的方法产生的解释不受形状限制。我们的算法可在通用逻辑求解器之上实现，从而将 NN 特定的编码与算法框架分离。我们在来自图像识别和医学领域的大量基准上进行了实验，展示了新方法的优势，尤其是在计算效率方面。值得注意的是，我们的方法能够对先前基于逻辑的方法无法处理的深度网络进行逻辑解释。

## Abstract
Formal explainability of classifying neural networks (NNs) is an active area of research, providing explanations with provable guarantees of the classification within continuous regions of the input feature space. However, the existing techniques are either limited to individual input features without guarantees on their relations or the provided solutions fail to scale to deep architectures. This paper addresses these issues by introducing a flexible symbolic framework for an efficient, guided computation of explanations of the NN behavior, parametrized by the activations of internal neurons, and using logical engines such as SMT solvers. Unlike prior methods that rely on specialized NN verifiers, our method yields explanations that are not restricted in shape. Our algorithm is implementable on top of a general-purpose logical solver, isolating the NN-specific encoding from the algorithmic framework. We experimented with a wide range of benchmarks from the domains of image recognition and medicine, illustrating the advantages of the new method, particularly in computational efficiency. Notably, our approach enables logical explanation of deep networks not amenable to prior logic-based methods.

---

## 论文详细总结（自动生成）

# 论文总结：基于神经元激活的深度神经网络逻辑解释计算

## 1. 核心问题与整体含义

- **研究背景**：分类神经网络的形式化可解释性旨在为分类结果提供具有可证明保证的逻辑解释，即在输入特征空间的连续区域内保证分类不变。
- **现有问题**：
  - 早期逻辑解释方法多基于溯因推理，依赖专用 NN 验证器或 SMT 求解器，迭代放松输入约束。
  - 这些方法对输入特征数量高度敏感，需要大量昂贵查询，且解释常呈超矩形，难以捕捉输入特征之间的复杂关系。
  - 近期基于 Craig 插值的方法可产生形状不受限的解释，但直接编码整个 NN，难以扩展到深层架构。
- **论文目标**：提出一个灵活、可扩展的符号框架，通过内部神经元激活参数化 NN 编码与求解过程，在保持解释正确性和一般性的同时，显著提升深层网络上的逻辑解释计算效率。
- **整体含义**：该方法将 NN 特定编码与通用逻辑求解、泛化算法解耦，使逻辑解释可应用于先前方法难以处理的深层网络，并在图像识别和医学基准上取得明显性能优势。

## 2. 方法论

### 2.1 总体框架

- 输入：神经网络 \(M\)、样本点 \(s\)、样本分类 \(c\)、内部神经元配置。
- 核心流程：
  1. **Encoder**：将 NN 编码为逻辑公式 \(\psi\)，结合样本约束 \(\varphi_s\)，构造不可满足公式 \(\varphi_s \land \psi\)。
  2. **Solver**：使用 SMT 求解器证明不可满足性，并通过“决策引导”加速搜索。
  3. **Generalizer**：从不可满足证明中泛化样本约束，得到中间解释 \(\varphi'\)。
  4. **最终解释**：结合配置约束 \(\varphi_C\)，得到 \(\varphi = \varphi' \land \varphi_C\)。
- 逻辑解释定义：公式 \(\varphi\) 满足 \(\forall x \in \mathcal{F}.\ \varphi(x) \Rightarrow \kappa(x)=c\)，即分类为 \(c\) 的充分条件。
- 框架与后端求解器、泛化技术无关，可实例化为 Craig 插值、不可满足核心提取或区间扩展等。

### 2.2 神经元激活参数化配置

- 引入两类内部神经元集合：
  - \(G_H\)：**引导神经元**，求解器优先选择其激活相态，但不强制。
  - \(F_H\)：**固定神经元**，根据样本点处的预激活值固定 ReLU 分支，并添加配置约束 \(\varphi_C\) 保证相对原网络的正确性。
- **引导约束生成（Alg. 1）**：
  - 对每个 \(v_i^{(\ell)} \in G_H\)，若样本处 \(z_i^{(\ell)}(s)>0\)，则生成引导约束 \(z_i^{(\ell)}>0\)；否则生成 \(z_i^{(\ell)}\le 0\)。
  - 这些约束按层序排列，作为求解器的决策建议，不影响可满足性和解释正确性。
- **固定神经元编码（Alg. 2）**：
  - 若 \(v_i^{(\ell)} \in F_H\)，则根据 \(z_i^{(\ell)}(s)\) 只编码一个 ReLU 分支：\(a_i^{(\ell)}=z_i^{(\ell)}\) 或 \(a_i^{(\ell)}=0\)。
  - 同时将 \(z_i^{(\ell)}>
