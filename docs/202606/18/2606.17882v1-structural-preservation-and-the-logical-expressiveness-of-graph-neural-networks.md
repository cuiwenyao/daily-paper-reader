---
title: Structural Preservation and the Logical Expressiveness of Graph Neural Networks
title_zh: 结构保持与图神经网络的逻辑表达能力
authors: "Przemysław Andrzej Wałęga, Bernardo Cuenca Grau"
date: 2026-06-16
pdf: "https://arxiv.org/pdf/2606.17882v1"
tags: ["query:ns-xai"]
score: 8.0
evidence: 建立了图神经网络与逻辑形式主义之间的对应关系，桥接了符号推理与神经网络
tldr: 图神经网络与逻辑形式系统的对应通常依赖特定架构选择。本文从语义角度，通过结构保持性质（嵌入、单射同态、同态）对GNN分类，证明每类GNN等价于分阶模态逻辑的特定片段：嵌入保持对应存在性分阶模态逻辑，单射同态保持对应其存在性正片段，同态保持对应存在性正模态逻辑。这些结果不依赖具体架构，且每类均有相应GNN架构实现相同表达能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.17882v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1454, \"height\": 263, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17882v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 632, \"height\": 277, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17882v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 714, \"height\": 354, \"label\": \"Figure\"}]"
motivation: 现有GNN与逻辑的对应受限于特定架构，本文旨在从语义层面不依赖架构地刻画GNN的分类表达能力。
method: 利用树的有界高度上的良拟序结果，将结构保持性质与分阶模态逻辑的片段建立对应关系。
result: 三类结构保持性质分别对应分阶模态逻辑的三个不同片段，且每类都存在等表达能力的GNN架构。
conclusion: 建立了与架构无关的GNN逻辑表达能力表征，为理解GNN的语义提供新视角。
---

## 摘要
通过固定架构选择，如聚合、组合和激活函数的类型，图神经网络（GNNs）与逻辑形式体系之间的桥梁得以建立。这些选择定义了特定类别的GNN，通过证明逻辑公式可以转换为等价的GNN，反之亦然，可以建立与逻辑形式体系的紧密对应关系。在本文中，我们采取语义视角，建立那些在结构属性（嵌入、内射同态和同态）下保持的GNN分类器类别的逻辑表达能力。我们表明，对于每个这样的属性，存在一个度模态逻辑片段，其特征化了GNN的类别。特别地，在嵌入、内射同态和同态下的保持分别对应于存在性度模态逻辑、其存在-正片段以及存在-正模态逻辑。这些结果特征化了广泛GNN类别的表达能力，独立于具体的架构选择，但我们也表明，这些类别中的每一个都承认具有相同表达能力的GNN架构。在技术上，我们的方法使用了有界高度树的新良拟序结果，从而得到了展开不变类别的有限表示。

## Abstract
Bridges between graph neural networks (GNNs) and logical formalisms have been established by fixing architectural choices, such as the types of aggregation, combination, and activation functions. These choices define restricted classes of GNNs for which tight correspondences with logical formalisms can be obtained, by showing that logical formulae can be translated into equivalent GNNs and, conversely, that GNNs can be translated into equivalent formulae.   In this paper we take a semantic perspective by establishing the logical expressiveness of classes of GNN classifiers that are preserved under structural properties: embeddings (extensions), injective homomorphisms, and homomorphisms. We show that, for each such property, there exists a fragment of graded modal logic characterising the class of GNNs. In particular, preservation under embeddings, injective homomorphisms, and homomorphisms corresponds to existential graded modal logic, its existential-positive fragment, and existential-positive modal logic, respectively. These results characterise the expressiveness of broad classes of GNNs independently of specific architectural choices, but we also show that each of these classes admits a GNN architecture of the same expressiveness.   Technically, our approach uses a new well-quasi-order result for trees of bounded height, yielding finite representations of unravelling-invariant classes.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：图神经网络（GNN）与逻辑形式体系的对应关系以往依赖于特定的架构选择（如聚合函数、组合函数、激活函数类型）。这些选择定义了受限的GNN类别，并能够与逻辑公式建立一一对应。然而，这种“架构驱动”的刻画方式不够通用，难以揭示GNN分类能力的本质语义。  
- **研究动机**：希望从**语义角度**出发，不依赖具体架构，仅通过**结构保持性质**（preservation under structural relations）来刻画GNN分类器的逻辑表达能力，从而建立更根本、更广泛的对应关系。  
- **整体含义**：该工作为GNN的表达能力提供了独立于实现的逻辑表征，有助于理解GNN的推理能力、设计可解释模型，并桥接神经方法与符号方法。

### 2. 论文提出的方法论：核心思想、关键技术细节

**核心思想**：利用GNN的局部性（L层GNN只依赖节点的L跳邻域）和结构保持性质，将问题简化为对有界高度树的分析。通过证明此类树上的嵌入、单射同态、同态关系是**良拟序**（well-quasi-order），得到有限个最小树，进而用分阶模态逻辑公式刻画对应类别。

**关键技术细节**：
1. **L-展开（L-unravelling）**：将任意点图 \((G, v)\) 转化为高度为L的树 \(\text{Unr}_L(G,v)\)，GNN的L层输出与此树上的输出相同（不变性）。
2. **良拟序新结果（Theorem 4）**：证明了任意有界高度树类在嵌入关系下是良拟序，从而仅含有限多个极小元。该结果对单射同态和同态同样成立。
3. **逻辑对应关系（Theorem 7 & Corollary 8）**：
   - 嵌入保持 ↔ 存在性分阶模态逻辑（∃GML）
   - 单射同态保持 ↔ 存在性正分阶模态逻辑（∃⁺GML）
   - 同态保持 ↔ 存在性正模态逻辑（∃⁺ML）
4. **架构实现（Theorem 16）**：每个逻辑片段都存在等表达能力的特定GNN架构：
   - 增强单调GNN（augmented MGNN）等价于 ∃GML
   - 增强MAX-MGNN等价于 ∃ML
   - MGNN等价于 ∃⁺GML
   - MAX-MGNN等价于 ∃⁺ML

### 3. 实验设计

- **无实验**：本文为纯理论推导，未设计任何实验。没有使用数据集、基准方法或对比实验。所有结论均通过数学证明获得。

### 4. 资源与算力

- **未提及**：论文未说明任何GPU型号、数量、训练时长等计算资源信息。作为理论工作，不依赖实验算力。

### 5. 实验数量与充分性

- **实验数量**：0组实验。  
- **充分性评价**：虽然缺少实验验证，但论文提供了完整的数学证明（包括定理、引理、推理链），从理论角度是严谨且充分的。对于理论计算机科学和逻辑学方向的论文，这种不设实验的做法是常见且合理的。结论的有效性建立在证明正确性之上，而非实证结果。

### 6. 论文的主要结论与发现

- **三项精确对应**：
  - 在嵌入（embedding）下保持的GNN分类器↔ ∃GML公式（深度≤L）
  - 在单射同态（injective homomorphism）下保持的GNN分类器↔ ∃⁺GML公式
  - 在同态（homomorphism）下保持的GNN分类器↔ ∃⁺ML公式
- **架构实现**：每种结构保持类别都存在对应的单调GNN架构（含/不含增强层、MAX聚合等），且这些架构的表达能力正好等于对应逻辑片段。
- **权衡关系**：更强的结构保持性质（如同态）导致更弱的逻辑表达能力（仅∃⁺ML），而较弱的保持性质（嵌入）允许更丰富的表达（∃GML）。这揭示了结构不变性与表达力之间的基本取舍。

### 7. 优点

- **语义视角新颖**：跳出了以往依赖具体架构的方法，首次从结构保持的语义角度统一刻画GNN表达力。
- **理论深度强**：使用了良拟序、模态逻辑、有限模型理论等工具，并证明了有界高度树的新良拟序结果（Theorem 4），独立有趣。
- **完整性好**：不仅给出逻辑刻画，还证明了每种逻辑片段都有对应的GNN架构，形成双向等价。
- **普适性强**：结果适用于任何聚合-组合范式GNN，不限制激活函数或权重符号；且可直接推广到有向图。

### 8. 不足与局限

- **任务局限**：仅针对**节点分类**任务，未扩展到图级分类或边级预测。
- **架构局限**：只考虑基于邻域聚合的局部GNN，不涵盖全局注意力、虚拟节点、图Transformer等非局部机制。
- **分类器限制**：仅使用阈值函数作为分类器，未考虑更复杂的输出层。
- **标签离散性**：假设节点标签为有限长度的二进制向量，无法直接处理连续特征或高维实值属性。
- **不变性范围**：结论依赖于GNN在有限展开深度下的不变性，无法处理需要全局信息或无限深度的属性。
- **无实验验证**：纯理论论文缺少实验支撑，实际应用中架构选择与理论最优可能仍有差距（例如数值精度、有限参数的影响）。

（完）
