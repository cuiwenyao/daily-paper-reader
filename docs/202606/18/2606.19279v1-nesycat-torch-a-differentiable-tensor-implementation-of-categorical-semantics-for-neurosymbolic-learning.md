---
title: "NeSyCat Torch: A Differentiable Tensor Implementation of Categorical Semantics for Neurosymbolic Learning"
title_zh: NeSyCat Torch：神经符号学习中范畴语义的可微张量实现
authors: "Daniel Romero Schellhorn, Till Mossakowski, Björn Gehrke"
date: 2026-06-17
pdf: "https://arxiv.org/pdf/2606.19279v1"
tags: ["query:ns-xai"]
score: 9.0
evidence: NeSyCat Torch 提供了神经符号学习的可微张量框架，直接集成符号语义与神经网络用于可解释推理
tldr: 神经符号语义因经典、模糊、概率和神经系统的不同归纳规则而碎片化。NeSyCat通过参数化于强单子和聚合结构统一这些语义，但缺乏神经网络学习的谓词和函数实现。NeSyCat Torch填补这一空白，使用分布单子做参考语义、懒惰对数张量单子做可微训练、批处理单子做高效批处理，并在HaskTorch、JAX和PyTorch中实现。在MNIST加法任务上，该方法在速度和精度上均优于LTN和DeepProbLog，接近DeepStochLog，且保持统一框架。其关键贡献是提供了一个参数化于单子的统一构造，可扩展至其他单子（如Giry单子）以处理连续概率。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.19279v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1545, \"height\": 502, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19279v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1169, \"height\": 265, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19279v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1350, \"height\": 255, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19279v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1512, \"height\": 514, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19279v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1556, \"height\": 308, \"label\": \"Table\"}]"
motivation: 现有NeSyCat缺乏对神经网络学习的谓词和函数的支持，且需要可微训练和高效批处理。
method: 使用分布单子做参考语义，懒惰对数张量单子实现可微训练，批处理单子支持高效批处理，并基于HaskTorch、JAX、PyTorch实现。
result: 在MNIST加法上，NeSyCat Torch在速度和精度上优于LTN和DeepProbLog，接近DeepStochLog。
conclusion: 提供了一个统一且参数化于单子的神经符号学习框架，可扩展至连续概率分布。
---

## 摘要
神经符号语义是碎片化的：经典系统、模糊系统、概率系统和神经网络系统各自通过自身的归纳规则定义真值。NeSyCat是ULLER的扩展，将这些系统统一在一个单一的归纳真值定义下，该定义以强单子和真值上的聚合结构为参数。迄今为止，NeSyCat缺乏对由神经网络学习的谓词和函数的描述。我们提供NeSyCat Torch作为缺失的一环，通过神经网络解释计算符号，并在概率编程和基于张量的后端中实现该框架。我们使用分布单子进行参考语义和度量评估，并辅以一个用于数值稳定、可微训练的单子：对数半环上的惰性对数张量单子。为了高效的批量训练，我们还采用了批处理单子。公理是源代码：一旦用基于单子的do标记法编写，单子绑定执行边际化，惰性地剪枝不需要的分支。在MNIST加法任务上，我们的HaskTorch、JAX和PyTorch实现在速度和准确率上优于LTN和DeepProbLog，同时达到与DeepStochLog几乎相同的准确率。然而，与DeepStochLog不同，我们保持在一个统一的框架内，适用于许多一阶神经符号方法。也就是说，构造以单子为参数；例如，用Giry单子实例化它，可将方法扩展到连续概率（此处将神经网络的实现留作未来工作）。

## Abstract
Neurosymbolic semantics is fragmented: classical, fuzzy, probabilistic and neural systems each define truth by their own inductive rules. NeSyCat, extending ULLER, subsumes them under a single inductive definition of truth, parametric in a strong monad and an aggregation structure on truth-values. NeSyCat has so far lacked an account of predicates and functions learned by neural networks.   We provide NeSyCat Torch as the missing link and interpret computational symbols via neural networks, implementing the framework in probabilistic programming and tensor-based backends. We use the distribution monad for reference semantics and metric evaluation, and complement it by a monad for numerically stable, differentiable training: the lazy log-tensor monad over the log-semiring. For efficient training in batches, we furthermore employ a batch monad. The axioms are the source code: written once in monad-based do-notation, monadic bind performs marginalisation, lazily pruning unneeded branches.   On MNIST addition, our HaskTorch, JAX, and PyTorch implementations outperform LTN and DeepProbLog in speed and accuracy, while achieving nearly the accuracy of DeepStochLog. However, unlike DeepStochLog, we stay in a uniform framework that applies to many first-order NeSy approaches. Namely, the construction is parametric in the monad; instantiating it with, e.g., the Giry monad extends the approach to continuous probability (working out a neural representation here is left for future work).

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题**：神经符号（NeSy）语义存在碎片化现象——经典逻辑、模糊逻辑、概率逻辑和神经网络系统各自采用不同的归纳规则定义“真值”，导致知识库和学习目标难以在不同系统间迁移。已有的 ULLER 框架提供了三种独立的语义（经典、模糊、概率），但缺乏统一的归纳定义。NeSyCat 通过单子（monad）将上述语义统一到一个参数化的归纳真值定义中，但缺少对神经网络学习的谓词和函数的实现。
- **整体含义**：本文提出的 NeSyCat Torch 填补了这一空白，使 NeSyCat 框架能够实际用于神经网络训练，同时保持语义的统一性。通过可微分的张量实现，将符号推理与深度学习无缝集成，为神经符号学习提供了一种既统一又可扩展的计算框架。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：使用三种不同层级的单子（monad）分别处理概率参考、可微训练和批量并行：
  - **分布单子（D）**：作为参考语义和度量评估，处理有限概率分布。
  - **对数张量单子（LogTens）**：基于对数半环（log-semiring）的惰性实现，保证训练中数值稳定且可微，避免梯度退化。LogTens 的 bind 操作对应 log-sum-exp 边际化。
  - **批处理单子（B）**：基于批量索引的 reader 单子，实现小批量样本的并行评估。训练时使用复合单子 B ∘ T。
- **关键技术细节**：
  - 系统分为四个层次：范畴层、逻辑层、领域层和语法层。前三个层定义符号及其解释，第四个层通过单一归纳规则生成项和公式并赋予单子语义。
  - 符号分为三种：Tarski（确定性）、Kleisli（结果进入单子）和 Nesy（输入输出均为单子化）。神经网络的实现对应 Nesy 符号。
  - 公式以 do-notation 编写（类似 Haskell/Python 生成器语法），单子 bind 自动执行边际化（概率中为全概率公式，张量中为离散卷积）。例如 MNIST 加法公理：`do { d1 ← digit(x); d2 ← digit(y); return (n == d1 + d2) }` 在分布单子下计算概率，在对数张量单子下计算 logits 的卷积。
  - 训练目标：最小化知识损失 \( \hat{L}(\theta) = \frac{1}{N}\sum_i -\log \phi_\theta(s_i) \)，即数据样本上公理真值的负对数似然均值。
  - 惰性求值：仅在实际需要时才进行边际化，避免不必要的计算从而提高效率。

## 3. 实验设计：使用了哪些数据集 / 场景，benchmark 是什么，对比了哪些方法

- **数据集/场景**：
  - MNIST 手写数字加法（MNIST Addition）任务：弱监督学习，只给出两幅数字图像的标签（它们的和），而不给出单个数字的标签。
  - 扩展至多位数加法：包含两个 4 位数相加（N=4，和最大为 19998）。
- **Benchmark**：测试集上的“和”准确率（sum accuracy），即预测的加法结果与真实结果一致的样本比例。
- **对比方法**：
  - LTN（Logic Tensor Networks）及其对数版本 logLTN。
  - DeepProbLog、DeepStochLog、A-NeSI、NeuPSL（部分结果来自论文引用）。
  - 参考值：0.99^(2N) 作为理论上限（假设单个数字分类准确率 99%）。
- **实验设置**：使用相同的 CNN 骨干、批量大小和训练轮次（针对不同基线分别采用其典型设置，如 LTN 风格与 DPL 风格）。

## 4. 资源与算力

- **GPU 型号**：单张 A100 GPU（文中明确提到 “All numbers below come from the JAX backend on a single A100 GPU”）。
- **训练时间**：单种子训练约 1.5 分钟（LTN 风格，20 个 epoch，batch=32）至 5.6 分钟（DPL 风格，到收敛，batch=2）。15 个种子的完整扫描约数十分钟。
- **算力消耗**：训练步骤时间在 0.2–0.5 ms/step 之间（batch=32），远低于 LTN 在 V100 上报告的 5.36/3.44 ms/step。
- **说明**：论文未提供更详细的算力信息（如多 GPU 配置、FP16 精度等）。

## 5. 实验数量与充分性

- **实验数量**：
  - 单一数字加法（N=1）: 两组实验（LTN 风格和 DPL 风格），训练集 3000 对，测试集未知；均取 15 个随机种子，报告均值±标准差。
  - 数字对加法（N=2）: 训练集分别为 1.5k 对（LTN 风格）和 1.5k 对（DPL 风格），以及额外 15k 对（对比 DeepStochLog）。
  - 四位数加法（N=4）: 训练集 7.5k 对或 15k 对。
  - 未进行显式的消融实验（如去掉批处理单子、不使用惰性求值等）。
- **充分性与客观性**：
  - 15 个随机种子重复实验，报告均值和标准差，增加了统计可靠性。
  - 与多个基线方法（LTN、DeepProbLog、DeepStochLog、A-NeSI、logLTN）进行公平比较，部分数据直接引用原论文。
  - 对比实验采用了报告方自身的最优结果设置（LTN 报告 10/15 最佳结果，本文平均 15 个种子，且仍然更好）。
  - 但缺乏更多的消融分析和扩展任务的验证（如其他视觉推理场景），因此充分性一般。

## 6. 论文的主要结论与发现

- NeSyCat Torch 在 MNIST 加法任务上（N=1）达到 94.6% ± 0.6%（LTN 风格）和 94.2% ± 0.7%（DPL 风格）的测试和准确率，优于 LTN（93.5%）和 DeepProbLog（92.2%），且速度更快。
- 在多位数加法（N=2, N=4）上：
  - N=2: 与 DeepStochLog 和 A-NeSI 处于同一水平（约 96% 左右）。
  - N=4: 达到 92.0%，接近理论参考值 92.27%，而 LTN 和 DeepProbLog 超时无法运行；DeepStochLog 略高（92.7%），但其代价是改变了聚合结构（丢失质量）。
- 统一的单子参数化框架使得经典、模糊、概率、神经符号语义均可通过切换单子实例化，无需重写公式。这是核心贡献。
- 懒惰张量单子帮助实现了高效的边际化和可微训练，避免了 #P-hard 的加权模型计数问题。

## 7. 优点

- **统一性**：通过单子抽象将多种语义统一在同一框架内，公式只需编写一次，通过更换单子即可在概率、模糊、经典语义之间切换。
- **可微与数值稳定**：LogTens 在对数空间工作，避免 softmax 过早归一化，梯度不退化，数值稳定性好。
- **效率**：惰性求值只在需要时执行边际化；批处理单子支持并行计算，训练速度快（毫秒级 batch step）。
- **可扩展性**：可扩展到多位数加法，而 DeepProbLog 等因模型计数复杂性超时。
- **开源实现**：提供了 HaskTorch、JAX、PyTorch 三种后端代码，便于验证和使用。

## 8. 不足与局限

- **实验覆盖有限**：仅在 MNIST 加法任务上验证，缺乏更多领域（如视觉关系推理、自然语言处理）的测试。
- **未做消融研究**：没有分析不同单子组件（如惰性、批处理单子、LogTens 与普通张量）的贡献度；也未对比不同神经网络架构的影响。
- **连续概率扩展未实现**：论文提到未来可用 Giry 单子处理连续概率，但当前版本仅支持有限支撑分布（离散）。
- **缺陷/偏差风险**：比较基线时，部分基线（如 LTN）报告的是其 10/15 最佳结果，而本文取 15 个种子平均，虽然本文结果仍更优，但直接对比不够对称。
- **其他限制**：目前不支持无限域、不支持动态调整公式结构（尽管指出可通过 Python 程序实现）。未讨论处理更大规模数据集时的扩展性瓶颈。

（完）
