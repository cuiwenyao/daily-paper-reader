---
title: "Neuro-Relational Programs: Unifying Queries and Neural Computation over Structured Data"
title_zh: 神经关系程序：统一结构化数据上的查询与神经计算
authors: "Arie Soeteman, Balder ten Cate, Maurice Funk, Benny Kimelfeld, Carsten Lutz, Moritz Schönherr"
date: 2026-06-10
pdf: "https://arxiv.org/pdf/2606.11946v1"
tags: ["query:ns-xai"]
score: 9.0
evidence: 将神经计算与Datalog风格符号推理统一
tldr: 针对关系数据库上的深度学习，现有方法使用图神经网络或直接处理数据库。本文提出神经关系程序(NRPs)，一种扩展Datalog的声明式查询语言，支持嵌入向量操作，统一关系推理与可学习组件。NRPs可视为带训练组件的查询计划或具关系结构的神经网络。不同语法片段可恢复现有架构：零元NRPs对应非自适应查询算法，一元NRPs泛化GNN消息传递并精确捕获深度同态网络。通过扩展一阶逻辑计数逻辑FOCQ刻画其表达力，建立了与有序数据库上一致TC^0的精确联系。
source: arxiv
selection_source: fresh_fetch
motivation: 现有深度学习对关系数据库的处理要么将数据库转为图后使用GNN，要么在数据库上直接操作但缺乏统一形式化框架。
method: 提出NRPs，在Datalog规则基础上增加嵌入向量的组合、聚合与变换操作，实现关系推理与神经组件的交织。
result: 零元NRPs对应非自适应查询；一元NRPs捕获GNN消息传递和深度同态网络；表达力通过FOCQ逻辑刻画，对应一致TC^0。
conclusion: NRPs提供了查询和神经网络计算于关系数据上的统一声明式框架，建立了与经典计算模型的联系。
---

## 摘要
关系数据库深度学习的传统方法是将图神经网络（GNN）等神经模型应用于数据库的图表示。近期方法则直接操作数据库，将元组与嵌入关联，并扩展查询机制以联合处理嵌入和关系内容。受这些进展启发，我们引入了神经关系程序（NRP），这是一种针对事实携带数值向量嵌入的关系数据库的声明式查询语言。NRP扩展了Datalog风格的规则，通过组合、聚合和变换嵌入的操作，将关系推理和可学习的神经组件交织在单一形式体系内。这产生了一种在关系数据上进行神经计算的通用方法：NRP既可以看作带有可训练组件的查询计划，也可以看作内置关系结构的神经架构。NRP的自然句法子片段能够还原现有架构和查询形式体系。零元NRP对应于非自适应查询算法；单子NRP泛化了GNN风格的消息传递并精确捕捉了深度同态网络，我们将这一联系扩展到具有行ID的数据库上的边界保护NRP。我们通过FOCQ（一阶逻辑在实权结构上带计数的扩展）刻画了使用ReLU-FFN变换的无限制NRP的表达能力，从而与有序数据库上的均匀TC^0建立了精确联系。这些结果共同确立了NRP作为关系数据上查询和神经计算的广泛声明式框架。

## Abstract
The conventional approach to deep learning over relational databases applies neural models, such as Graph Neural Networks (GNNs), to a graph representation of the database. Recent approaches instead operate on databases directly, associating tuples with embeddings and extending query mechanisms to jointly process embeddings and relational content. Inspired by these developments, we introduce Neuro-Relational Programs (NRPs), a declarative query language for relational databases whose facts carry numeric vector embeddings. NRPs extend Datalog-style rules with operations that combine, aggregate, and transform embeddings, thereby interleaving relational reasoning and learnable neural components within a single formalism. This yields a general approach to neural computation over relational data: an NRP can be read both as a query plan with trainable components and as a neural architecture with relational structure built in.   Natural syntactic fragments of NRPs recover existing architectures and query formalisms. Zero-ary NRPs correspond to non-adaptive query algorithms; monadic NRPs generalize GNN-style message passing and precisely capture Deep Homomorphism Networks, a connection that we extend to frontier-guarded NRPs over databases with row-ids. We characterize the expressive power of unrestricted NRPs with ReLU-FFN transformations by FOCQ, an extension of first-order logic with counting interpreted over real-weighted structures, yielding a precise connection with uniform TC$^0$ over ordered databases. Together, these results establish NRPs as a broad declarative framework for querying and neural computation over relational data.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：关系数据库上的深度学习目前存在范式割裂——传统方法先将数据库转为图结构，再使用图神经网络（GNN）处理；而更近期的方法虽然直接在数据库中操作元组嵌入，但缺乏统一的形式化框架去将关系查询与神经计算有机融合。  
- **整体含义**：作者提出 **神经关系程序（NRPs）**，将 Datalog 风格的声明式规则与向量嵌入的组合、聚合和变换操作相结合，使得一个 NRP 既可以解释为带有可训练组件的查询计划，也可以视作内置关系结构的神经架构。这统一了数据库查询和深度学习两种范式，并建立了与经典计算模型（非自适应查询算法、深度同态网络、统一 TC⁰）之间的精确联系。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将事实（tuple）与数值向量（embedding）关联，在 Datalog 规则的基础上引入三种规则类型——**合取规则**（实现连接和投影，通过组合函数和聚合函数处理嵌入）、**析取规则**（实现并集，聚合不同来源的嵌入）、**变换规则**（通过可微映射如 ReLU-FFN 更新单个事实的嵌入）。  
- **关键技术细节**：  
  - 固定元素级乘积（⊙）作为默认组合函数，元素级求和（sum）作为默认聚合函数。  
  - 证明了使用 sum 或 ⊕（拼接）作为组合函数的规则都可以通过引入额外的变换规则模拟为只用 ⊙ 的形式，且保留句法片段性质（零元、单子、边界保护等）。  
  - 通过定义不同的句法片段（零元、单子、边界保护）来连接已有的表达形式：零元 NRP 捕获非自适应查询算法；单子 NRP 捕获深度同态网络（DHN），并进一步推广到带行 ID 的数据库上的边界保护 NRP（可表达边嵌入更新等）。  
  - 使用 FOCQ（一阶逻辑在实权结构上带计数的扩展）来刻画无限制 NRP 的表达能力，并证明在有序经典数据库上 FOCQ 等价于 FO+C，从而对应均匀 TC⁰。

### 3. 实验设计：使用的数据集 / 场景、benchmark、对比方法

- **本文本身是纯理论论文**，未提供具体的实验数据、标准基准或数值结果。  
- 作者仅在结论部分提到配套实现系统（参考文献 [38]）将 NRP 编译为嵌入感知的关系代数，进而编译为 PyTorch、cuDF 和 SQL 的物理计划，并声称实验结果达到了与先进实现相当的性能且表达式更简洁。但该实验细节不在本论文中展示。

### 4. 资源与算力

- 文中未提及任何 GPU 型号、数量、训练时长等算力信息。由于本文是理论性工作，不依赖具体硬件资源进行实验验证。

### 5. 实验数量与充分性

- **实验数量**：零。本论文没有任何数值实验、消融实验或对比实验。  
- **充分性**：作为理论贡献，论文通过形式化证明（如定理 4.1、5.5、5.14、6.1、6.3 等）建立了表达力刻画，这些证明本身构成了验证。但缺乏实际数据上的性能评估，无法判断在真实场景下的效率、可扩展性等。

### 6. 论文的主要结论与发现

- NRP 是一个统一声明式框架，能够同时表达关系查询和神经计算。  
- 零元 NRP 与非自适应查询算法等价（定理 4.1）。  
- 单子 NRP 精确捕获深度同态网络（DHN），且比 GNN 表达力更强（定理 5.5、推论 5.8、5.9、5.10）。  
- 边界保护 NRP 在带行 ID 和线性序的数据库上可规约为单子 NRP，进而可由 DHN 计算（定理 5.14、推论 5.15）。  
- 使用 ReLU-FFN 变换的 NRP 的表达力由逻辑 FOCQ 刻画；在有序经典数据库上，它等价于 FO+C 并捕获均匀 TC⁰ 类（定理 6.1、6.3）。  
- 在有序有理权结构上，FOCQ 严格包含于均匀 TC⁰ 中（定理 6.4）。

### 7. 优点：方法或实验设计上的亮点

- **理论深度与统一性**：将数据库查询、图神经网络、深度同态网络、非自适应查询算法、描述复杂性等不同领域用单一形式框架连接起来，揭示了它们之间的内在关系。  
- **简洁性与可扩展性**：NRP 语法简单（仅三种规则），但通过调节句法片段即可恢复或扩展已有架构。  
- **形式化验证**：所有表达力结果均有严格证明，逻辑清晰，为未来实现提供了坚实的理论基础。

### 8. 不足与局限

- **缺乏实验验证**：论文没有提供任何数值实验，无法证明 NRP 在实际任务中的有效性、训练效率或稳定性。  
- **计算复杂度未深入讨论**：虽然刻画了表达力，但未分析不同片段规则的计算复杂度（如连接规则可能产生巨大数量的嵌入操作）。  
- **聚合与组合函数固定**：仅使用 sum 和 ⊙，虽然可通过变换模拟其他操作，但可能限制某些应用场景的易用性。  
- **训练细节未涉及**：文中提及“反向传播梯度优化”，但未讨论参数初始化、收敛保证、数值稳定性等实际问题。  
- **仅考虑非递归 Datalog**：当前 NRP 不允许递归规则，无法表达如传递闭包等需要递归的查询。

（完）
