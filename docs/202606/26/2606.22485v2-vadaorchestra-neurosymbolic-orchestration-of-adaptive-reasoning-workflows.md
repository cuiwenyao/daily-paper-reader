---
title: "VADAOrchestra: Neurosymbolic Orchestration of Adaptive Reasoning Workflows"
title_zh: VADAOrchestra：自适应推理工作流的神经符号编排
authors: "Teodoro Baldazzi, Luigi Bellomarini, Andrea Coletta, Michela Iezzi, Carsten Maple, Alessandro Pesare, Emanuel Sallinger"
date: 2026-06-21
pdf: "https://arxiv.org/pdf/2606.22485v2"
tags: ["query:ns-xai"]
score: 9.0
evidence: 神经符号框架结合LLM灵活性与符号工作流严谨性
tldr: 现实决策需动态调整，但传统BPM缺乏运行时适应性，而纯LLM系统不透明且难以扩展。VADAOrchestra提出神经符号框架：用LLM编排器增量规划工作流并编码为Datalog+/-逻辑程序，由符号引擎执行推理，提供可审计的推理轨迹。在金融用例上，相比标准智能体架构，它实现了更好的忠实性、可扩展性和可解释性。该工作融合了两种范式的优势。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22485v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1807, \"height\": 487}, {\"url\": \"assets/figures/arxiv/2606.22485v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1759, \"height\": 497}, {\"url\": \"assets/figures/arxiv/2606.22485v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 658, \"height\": 577}, {\"url\": \"assets/figures/arxiv/2606.22485v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 652, \"height\": 875}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22485v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 884, \"height\": 223}]"
motivation: 结合BPM的严格审计和LLM的灵活性，同时解决LLM的不透明性和大规模数据上的可扩展性问题。
method: LLM编排器根据用户查询动态生成Datalog+/-逻辑程序，其中谓词对应工具调用，规则含领域依赖和按需合成的逻辑，然后用符号引擎执行推理。
result: 在真实金融用例中，VADAOrchestra相比标准智能体架构，展现出更高的忠实性、可扩展性和可解释性。
conclusion: VADAOrchestra有效结合LLM的灵活性和符号推理的严谨性，实现了可审计、可扩展的复杂决策流程。
---

## 摘要
现实环境中的决策很少遵循固定脚本。相反，它表现为一个动态推理过程，其中合适的行为路径会随着新的上下文和数据可用而演变。传统的业务流程管理系统提供了严谨性、确定性和可审计性，但通常在运行时难以适应执行。相比之下，基于大型语言模型（LLM）的智能体系统为决策带来了灵活性，但其本质上是不可透明的，往往不可靠，并且在处理大型数据集时面临显著的可扩展性限制。为了结合这两种互补范式，我们引入了VADAOrchestra，这是一个神经符号框架，将复杂工作流建模为不断演化的推理过程。该框架采用混合方法：给定用户查询和一系列数据源，基于LLM的编排器逐步规划并调整工作流。这被编码为Datalog+/-片段中的逻辑程序，其中谓词对应工具调用，规则既表示预定义的领域依赖关系，也表示按需合成的逻辑结构以操作中间结果。所有逻辑推理任务随后由最先进的Datalog+/-符号引擎执行。该方法提供了可验证的推理轨迹，支持整个过程的审计性和可重复性。此外，通过将高层编排与符号推理解耦，它解决了可扩展性问题，通过目标数据查询实现对大型数据集的复杂推理。我们在真实金融用例上评估了VADAOrchestra，证明了与标准智能体架构相比，其具有忠实性、可扩展性和可解释性。

## Abstract
Decision-making in real-world settings rarely follows a fixed script. Instead, it unfolds as a dynamic reasoning process in which the appropriate course of action evolves as new context and data become available. Traditional Business Process Management systems provide rigor, determinism, and auditability, yet they generally struggle to adapt their execution at runtime. Conversely, agentic systems based on Large Language Models (LLMs) bring flexibility to decision-making, but they are inherently opaque, often unreliable, and suffer from significant scalability constraints when operating over large datasets. To combine these complementary paradigms, we introduce VADAOrchestra, a neurosymbolic framework that models complex workflows as evolving reasoning processes. The framework adopts a hybrid approach: given a user query and a collection of data sources, an LLM-based orchestrator incrementally plans and adapts the workflow. This is encoded as a logic program in a fragment of Datalog+/- where predicates correspond to tool invocations and rules represent both predefined domain dependencies and logic constructs synthesized on demand to manipulate intermediate results. All logical inference tasks are then executed by a state-of-the-art Datalog+/- symbolic engine. This approach provides a verifiable reasoning trace, supporting the auditability and reproducibility of the entire process. Furthermore, by decoupling high-level orchestration from symbolic inference, it addresses scalability concerns, enabling complex reasoning over large datasets through targeted data querying. We evaluate VADAOrchestra on real-world financial use cases, demonstrating faithfulness, scalability, and explainability compared to standard agentic architectures.

---

## 论文详细总结（自动生成）

# VADAOrchestra: 中文总结

## 1. 核心问题与整体含义（研究动机与背景）
- **问题**：现实世界的决策（如金融监管中的集中度风险评估）是动态的，不能预先定义固定工作流。传统BPM系统提供严谨性和可审计性，但缺乏运行时适应性；而基于LLM的智能体系统虽灵活，但存在不透明（后验解释不可信）、不可靠（幻觉）、可扩展性差（上下文窗口有限、“迷失在中间”现象）等严重缺陷。
- **核心矛盾**：如何同时获得BPM的结构化严谨性与LLM的动态适应性，并保证高可扩展性和可审计性？
- **目标**：设计一个神经符号框架，将工作流建模为不断演化的逻辑推理过程，既支持动态规划，又能提供完全可验证的推理轨迹。

## 2. 方法论：核心思想与关键技术细节
### 2.1 核心思想
- 采用**LLM编排器 + 符号推理引擎**的分层架构：LLM负责高层规划、工具选择、参数绑定和按需规则合成；所有数据推理和逻辑计算由符号引擎（基于Vadalog，即Datalog+/-方言）执行。
- 将整个工作流（包括结构化步骤和动态步骤）编码为一个**逐步构建的逻辑程序**，其中：
  - **谓词**对应MCP工具调用（如`get_loans`、`get_bank_exposures`）；
  - **规则**表示领域依赖（如暴露计算依赖贷款和授信数据）和按需合成的数据操作（如聚合、连接）。
- 通过分离规划与执行，解决可扩展性问题：LLM只处理高层面摘要和策略，符号引擎处理大规模数据集。

### 2.2 系统架构
- **MCP客户端（编排器）**：选择工具、定义参数、管理依赖、迭代重规划、合成操作规则。
- **MCP服务器（执行器）**：通过暴露四大类工具（推理工具、发现工具、决策支持工具、数据操作工具）执行所有推理，结果物化到持久存储。
- **通信**：基于Model Context Protocol (MCP)，客户端发工具调用，服务器返回结果预览和元数据（基数、统计信息等）。

### 2.3 编排管道（Algorithm 1）
1. **工具发现与选择**：根据用户查询q，LLM从工具目录中选出语义匹配的工具，并提取参数绑定。
2. **依赖感知规划**：通过发现工具构建依赖有向无环图（DAG），确保工具按拓扑顺序执行；若LLM遗漏依赖，系统自动补全低层工具。
3. **执行计划生成**：生成阶段序列，每个阶段内可并行执行无依赖的工具，保证谓词安全性（同一工具不同参数不能同时写）。
4. **迭代执行**：按阶段顺序执行，每阶段完成后记录逻辑轨迹（调用规则）。
5. **目标数据检索**：对结果基数超过阈值τ的谓词，先统计分布再通过`get_top_k`选择性检索，避免上下文溢出。
6. **按需数据操作**：LLM合成Vadalog规则（如连接、聚合、算术），经语法验证后交符号引擎执行，结果物化并记录。
7. **重规划**：LLM评估当前数据是否足以回答查询，决定是否加入新工具调用，系统补充依赖后扩展计划。为防止无限循环，限制最大迭代次数及禁止相同工具相同参数重复调用。

### 2.4 逻辑轨迹（Logical Trace）
- 轨迹由**调用规则**（每个工具调用映射为一条规则）和**操作规则**（LLM合成的Vadalog规则）组成，按执行顺序排列。
- **保证**：轨迹忠实记录所有决策，重新执行轨迹可完全复现结果（无需LLM参与），实现可审计性和可重复性。
- 示例：图2展示了完整轨迹，包括结构化阶段（0–3）、阈值检查（4）、动态调查（5–8）、组级聚合（9）。

## 3. 实验设计
### 3.1 数据集与场景
- **知识图谱**：来自某欧洲央行的真实金融知识图谱，约10k三元组，9种关系。
- **Bank数据集**：278个模板问题（含通用、计数、多跳QA），源自ReFactX论文。
- **Bank+数据集**：对Bank的扩展，计数查询需要1到1000个不同实体关系，用于可扩展性压力测试。

### 3.2 对比方法
- **LLM-only**：仅LLM参数化知识。
- **ReFactX**：约束生成方法，在知识图谱三元组前缀树上限制解码。
- **LLM+RAG**：检索增强生成，检索相关数据注入上下文。
- **LLM+MCP**：智能体方法，使用与VADAOrchestra相同的MCP推理工具，但无显式依赖感知规划和无按需数据操作能力。
- **VADAOrchestra**：本方法。

### 3.3 评估指标
- **Exact Match (EM)**：大小写不敏感的字符串匹配。
- **LLM-as-a-Judge**：独立LLM（Llama-3.3-70B）评估答案正确性与完整性。

### 3.4 模型与运行配置
- 两个LLM骨干：**Llama-3.3-70B-Instruct**（开源）和**GPT-4o**（专有API）。
- 无额外算力说明（未提及GPU型号、数量、训练时长）。

## 4. 资源与算力
- 论文**未明确说明**使用的GPU型号、数量或训练时长。仅提及LLM通过API或本地推理运行，Vadalog引擎执行逻辑推理。实验开销仅通过执行时间和token生成间接提及。

## 5. 实验数量与充分性
### 5.1 实验组数
- **主实验**：在Bank数据集上，5种方法 × 2种LLM骨架 = 10组对比（图3），报告EM和Judge分数。
- **可扩展性实验**：在Bank+数据集上，选取3种最佳方法（VADAOrchestra、LLM+MCP、LLM+RAG三种top-k配置），按复杂度阈值（1–1000）绘制累积准确率曲线（图4）。
- **定性案例**：在一个具体集中度风险查询上，对比3种方法的结果（图5），展示可审计性差异。
- **消融实验**：无消融实验（未对VADAOrchestra内部组件如数据操作、依赖感知等单独剥离测试）。

### 5.2 充分性评价
- **积极方面**：覆盖了代表性基线（纯LLM、约束生成、RAG、纯智能体），使用了两种不同能力的LLM，并引入了可扩展性压力测试，结果统计完整。
- **不足**：缺乏对VADAOrchestra内部模块的消融分析（如去掉重规划、去掉合成规则的效果），也未进行不同阈值τ的敏感性分析。实验仅限于金融领域，泛化性未验证。
- **公平性**：LLM+MCP使用了与VADAOrchestra相同的MCP工具集，对比公平；RAG配置了多种top-k值，避免参数偏差。

## 6. 主要结论与发现
- **准确性**：VADAOrchestra在GPT-4o骨干下达到最佳性能（EM 0.65，Judge 0.78），高于LLM+MCP（0.63/0.77）和RAG（0.42/0.60）。
- **可扩展性**：在简单查询下与LLM+MCP接近；在复杂查询（≥50实体关系）时，VADAOrchestra保持稳定（EM≥0.63，Judge≥0.8），而LLM+MCP和RAG均显著下降（RAG降至0.35左右），证实了符号引擎处理大规模数据的优势。
- **可审计性**：只有VADAOrchestra能正确识别集中度风险（合成基金总暴露30.75%超过25%阈值），并输出完整逻辑轨迹（图2），而LLM+RAG完全忽略所有权结构，LLM+MCP推测到风险但无法可靠聚合。
- **依赖LLM能力**：小模型（Llama）下规则合成质量差，导致VADAOrchestra性能低于LLM+MCP；强模型（GPT-4o）下优势显现。

## 7. 优点
1. **创新架构**：将工作流编码为演化逻辑程序，同时获得LLM的灵活性和符号系统的严谨性。
2. **可审计性与可重复性**：逻辑轨迹忠实记录每一步，重新执行无需LLM，符合金融监管高要求。
3. **可扩展性**：通过选择性数据检索和符号引擎处理大规模数据，避免上下文限制。
4. **按需规则合成**：允许LLM在运行时生成数据操作规则，处理不可预见的分析需求。
5. **实验设计务实**：使用真实金融数据、压力测试和多种基线，结果有说服力。
6. 论文写作清晰，案例贯穿始终，便于理解。

## 8. 不足与局限
1. **规则合成质量依赖LLM**：小模型生成的Vadalog规则可能语义错误，降低整体准确性；论文未提供规则验证增强机制。
2. **执行时间开销**：多次LLM调用和规则合成带来额外token消耗，未对比端到端时间成本。
3. **消融实验缺失**：未分离评估依赖感知规划、按需操作、目标检索等各组件的贡献。
4. **泛化性不足**：仅测试金融领域，未在其他知识密集型领域（法律、医疗）验证。
5. **资源与效率未报告**：缺少GPU、推理时间、成本等量化数据，不利于实际部署决策。
6. **阈值参数影响未分析**：目标检索阈值τ、最大迭代次数等超参对性能的影响未讨论。
7. **依赖MCP协议**：虽便于扩展，但可能引入对特定协议实现的依赖，通用性存疑。

（完）
