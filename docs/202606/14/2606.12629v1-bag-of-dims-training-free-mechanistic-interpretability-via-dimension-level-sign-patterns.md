---
title: "Bag of Dims: Training-Free Mechanistic Interpretability via Dimension-Level Sign Patterns"
title_zh: 维度袋：基于维度级符号模式的无训练机制可解释性
authors: Varun Reddy Nalagatla
date: 2026-06-10
pdf: "https://arxiv.org/pdf/2606.12629v1"
tags: ["query:ns-xai"]
score: 8.0
evidence: 机械可解释性，维度级符号模式，无需训练
tldr: "论文提出Bag of Dims框架，发现transformer标准基中单个维度的符号直接编码语义内容，幅度表示置信度。仅用符号模式即可达72-93%的top-5 next-token准确率。通过单次前向传播，无监督发现175个语义类别，平均AUC 0.80。该结构存在于注意力投影和FFN中，可扩展至1500特征，证明标准基维度是充分的特征基础，无需训练或优化。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.12629v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1421, \"height\": 710, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12629v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1439, \"height\": 600, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12629v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1435, \"height\": 1312, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12629v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1400, \"height\": 501, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.12629v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1178, \"height\": 374, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.12629v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1263, \"height\": 609, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.12629v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1209, \"height\": 216, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.12629v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1257, \"height\": 257, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.12629v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1288, \"height\": 219, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.12629v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1265, \"height\": 373, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.12629v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1298, \"height\": 219, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.12629v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 842, \"height\": 217, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.12629v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 899, \"height\": 377, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.12629v1/table-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 890, \"height\": 294, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.12629v1/table-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 1539, \"height\": 298, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.12629v1/table-012.webp\", \"caption\": \"\", \"page\": 0, \"index\": 12, \"width\": 1263, \"height\": 257, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.12629v1/table-013.webp\", \"caption\": \"\", \"page\": 0, \"index\": 13, \"width\": 755, \"height\": 372, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.12629v1/table-014.webp\", \"caption\": \"\", \"page\": 0, \"index\": 14, \"width\": 1429, \"height\": 219, \"label\": \"Table\"}]"
motivation: 现有机制可解释性方法依赖训练或优化，本文探索无需训练的标准基维度符号模式是否足以编码语义特征。
method: 分析transformer hidden state每个维度的符号作为二元特征，通过符号一致性进行无监督聚类，并验证符号模式在预测和注意力中的有效性。
result: "符号模式预测准确率72-93%，无监督聚类175个类别AUC 0.80，FFN中20%特征映射到单个神经元，扩展至1500特征且稀疏性99%。"
conclusion: 标准基维度符号模式构成无训练的特征基础，可跨模型和路径实现可解释性，仅需单次前向传播。
---

## 摘要
我们证明，变换器隐藏状态的标准基已经提供了一个无需训练、架构通用的特征基。单个维度通过其符号编码语义内容，通过其幅度编码置信度，充当独立的二进制寄存器。我们通过四个渐进实验在三个模型家族（Qwen 3.5-4B、Gemma 3-4B、Mistral 7B）上验证了这一维度袋框架。仅符号模式就承载了预测内容：将所有幅度替换为统一值，通过语言模型头实现了72-93%的top-5下一令牌准确率，而纯汉明评分（无解码器）达到了80-90%的top-4096准确率。这些符号模式组织成语义特征：使用单令牌类型缓存（每个词汇令牌一次前向传播，无上下文），我们通过50个锚点的逐维度符号一致性（平均AUC 0.80）发现了175个类别，且无需任何训练。一个训练好的探针仅增加了+0.018 AUC，并收敛到轴对齐权重，确认了可忽略的跨维度结构。这种结构扩展到注意力：所有175个类别在K和V投影中仍然可发现。在写入侧，静态FFN权重检查将20%的特征链接到单个写入神经元（一致性>0.70；随机对照：0%），通过多数投票，top-200神经元联盟在99.9%的原型上实现>0.70的一致性。完全无监督发现（随机种子，无标签）在三个模型上扩展到1500个特征，产量100%，稀疏度99%，成对互信息为0.0014比特，确认了低维度间耦合。这些结果确立了标准基已足以在整个变换器计算路径中进行特征读取，无需训练、无需优化，且每个词汇令牌仅需单次前向传播，无需GPU天数。

## Abstract
We show that the standard basis of transformer hidden states already provides a training-free, architecture-general feature basis. Individual dimensions encode semantic content via their signs and confidence via their magnitudes, functioning as independent binary registers. We validate this Bag of Dims framework across three model families (Qwen 3.5-4B, Gemma 3-4B, Mistral 7B) through four progressive experiments. Sign patterns alone carry predictive content: replacing all magnitudes with unity achieves 72-93% top-5 next-token accuracy through the LM head, and pure Hamming scoring without any decoder reaches 80-90% top-4096. These sign patterns organize into semantic features: using a single-token type cache (one forward pass per vocabulary token, no context), we discover 175 categories via per-dimension sign consistency (mean AUC 0.80) from 50 anchors with zero training. A trained probe adds only +0.018 AUC and converges to axis-aligned weights, confirming negligible cross-dimension structure. This structure extends to attention: all 175 categories remain discoverable in K and V projections. On the write side, static FFN weight inspection links 20% of features to individual writer neurons (>0.70 agreement; random controls: 0%), with top-200 neuron coalitions achieving >0.70 agreement on 99.9% of prototypes via majority vote. Fully unsupervised discovery (random seeds, no labels) scales to 1500 features at 100% yield and 99% sparsity across all three models, with pairwise MI of 0.0014 bits confirming low inter-dimension coupling. These results establish that the standard basis already suffices for feature reading throughout the transformer compute pathway, requiring no training, no optimization, and no GPU-days beyond a single forward pass per vocabulary token.

---

## 论文详细总结（自动生成）

好的，以下是对给定论文《Bag of Dims: Training-Free Mechanistic Interpretability via Dimension-Level Sign Patterns》的详细中文总结。

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：随着Transformer语言模型规模扩大并部署在安全关键应用中，理解其内部计算机制变得至关重要。现有主流方法（如稀疏自编码器SAEs）通常需要**训练独立的可解释性模型**，消耗大量GPU算力和超参数调优时间。论文旨在探索一种**无需训练**、**架构通用**且**计算成本极低**的机制可解释性方法。
- **核心问题**：Transformer隐藏状态的**标准基**（standard basis）是否本身就构成了一个有效的特征基？单个维度是否能直接编码语义信息？
- **核心发现**：论文发现，在训练好的Transformer中，隐藏状态的单个维度（dimension）实际上充当了**独立的二进制寄存器**。维度的**符号（±1）** 编码了语义内容（如“动物”、“情感”），而**幅度（magnitude）** 则表示了模型对该内容的**置信度**。这一现象被称为“维度袋”（Bag of Dims）框架，它通过对维度符号的简单计数和比较，即可实现无需训练的特征读取。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将Transformer的隐藏状态视为一个“词袋”的维度版本，忽略维度间的几何结构（防止过拟合），将每个维度视为一个独立的二元投票器。特征被定义为具有一致符号模式的维度子集。
- **关键技术细节**：
    1.  **单令牌类型缓存（Single-Token Type Cache）**：为发现每个维度编码的内容，论文构建了一个“类型级缓存”。具体做法是，将模型词汇表中的**每个词**作为一个单独的输入（无上下文），通过模型进行**一次前向传播**，提取其在特定中间层（如第24层）的隐藏状态。由此得到一个维度为 V（词汇表大小） × D（隐藏维度）的矩阵 H。该缓存为所有后续分析服务。
    2.  **基于逐维度AUC的特征发现**：
        - **Step 1: 锚点选择**：为每个目标类别（如“动物”）手工挑选50个单词锚点。
        - **Step 2: 逐维度AUC计算**：对于每个维度，计算其符号（+1/-1）能区分锚点与全部词汇表中其他词的能力 (AUC评分)。负样本是全部词汇表，而非精心挑选的反例。
        - **Step 3: 构建签名原型**：选取AUC高于阈值（如0.75）的维度，并记录它们的预期极性（正或负），构成一个“特征”的签名原型。
    3.  **分类/评分**：对于一个新词，计算其维度符号与原型期望符号之间的一致性分数（1 - 汉明距离 / 维度数量）。这个过程无需任何学习到的权重，仅依靠计数。
    4.  **扩展到注意力**：同样的发现方法可以应用于Key（K）和Value（V）投影的输出维度，验证该结构是否存在于注意力路径中。
    5.  **FFN写入机制追踪**：通过检查前馈网络（FFN）的`down_proj`权重矩阵，寻找那些（经过幅度阈值过滤后的）写入模式与发现的残差流原型高度一致的神经元。

## 3. 实验设计：数据集、场景、Benchmark与对比方法

- **模型**：Qwen 3.5-4B, Gemma 3-4B-pt, Mistral 7B v0.3 三个不同架构的模型家族。
- **数据集/场景**：
    - **类型级**：使用模型自身的全部词汇表（约32K-262K个token）作为发现和评估的基础。
    - **上下文级**：使用200个提示（prompt），共25,600个上下文令牌（200 prompts × 128 tokens），用于验证特征在上下文中的有效性。
- **Benchmark与对比方法**：
    1.  **符号预测准确率**：对比 `sign(h)·W`（去掉幅度）与 `h·W`（全表示）及随机符号的控制组。
    2.  **纯汉明预测**：在没有LM头的情况下，对比基于符号多数投票的汉明距离方法与全点积基线（`h_final·h_0`）。
    3.  **特征发现性能**：
        - 与**线性探针（Logistic Regression）** 和**2层MLP**对比，看训练能否带来超越其无训练方法的优势。重点分析探针权重是否收敛到轴对齐投票。
        - 与**稀疏自编码器（Gemma Scope 2, 16K特征）** 进行头对头比较，看其无训练的原型特征与SAE找到的最佳特征在175个类别上的AUC。
    4.  **FFN写入链路**：对比训练好的权重、随机权重和列打乱的训练权重，验证强写入神经元是否存在。
    5.  **随机初始化控制**：在随机初始化的模型上运行相同的发现流程，以确认发现的语义结构是源于训练而非架构或方法本身的伪影。

## 4. 资源与算力

- **资源消耗**：论文明确指出其方法**无需GPU天数**。
- **具体算力**：构建“单令牌类型缓存”所需的算力极低，仅需在**单个GPU**上进行**一次前向传播**（per vocabulary token）。论文估计对于Qwen/Gemma模型（约24-26万token词汇表）耗时约 **20分钟**。所有后续分析（特征发现、原型评分）都是基于该缓存的轻量级NumPy矩阵运算。
- **与SAE对比优势**：相比需要数百万上下文令牌和大量GPU小时进行训练的SAEs，该方法优势巨大。

## 5. 实验数量与充分性

- **实验数量**：论文通过**四组渐进实验**（符号预测、零训练特征发现、注意力投影保真度、FFN电路追踪），并辅以**多种消融研究**和**对照实验**，实验量非常充分。
- **充分与公平性**：
    - **跨模型验证**：在Qwen、Gemma、Mistral三个不同家族、不同规模的模型上复现结果，证明了其发现的通用性，而非单一模型的偶然现象。
    - **严格对照**：设置了随机符号、随机权重、列打乱权重、随机初始化模型等多种基线，其结果均显著逊于训练好的模型，有力反驳了“结果源于偶然”的质疑。
    - **消融实验**：通过与线性探针、MLP的对比，证明了跨维度结构无实际价值，进一步巩固了其“维度独立”的核心论点。
    - **头对头对比**：与最新的、专门训练的SAE进行直接比较，且在大多数类别上，其无训练原型特征甚至优于SAE的最佳单特征。
    - **上下文验证**：验证了类型级原型在上下文中的泛化能力，并测试了多义词（polysemy），确认其读取的是“意义”而非“词汇身份”，设计巧妙。
    - **校准与统计显著性**：引入了空分布（null calibration），通过比较真实类别AUC与随机锚点分布的p95/p99阈值，排除了多重测试膨胀，统计方法严谨。

## 6. 论文的主要结论与发现

1.  **维度作为独立二进制寄存器**：训练后的Transformer隐藏状态中，单个维度通过符号编码语义，通过幅度表达置信度。维度间耦合极低（成对互信息为0.0014比特）。
2.  **符号携带预测内容**：仅使用符号模式（去掉幅度）即可达到72-93%的top-5下一令牌预测准确率。纯汉明距离评分（无解码器）即可实现80-90%的top-4096准确率。
3.  **零训练特征发现**：仅基于词汇表单次前向传播的缓存和50个种子词，即可无监督发现175个语义类别，AUC平均达0.80，全部超过空分布阈值。训练好的线性探针仅带来极小的性能提升（+0.018 AUC），并收敛到轴对齐权重。
4.  **结构存在于整个计算路径**：该结构不仅存在于残差流，还**通过K/V投影保持不变**，可通过同样的方法在注意力K/V空间中发现。FFN的写入机制也是轴对齐的，多数神经元通过`down_proj`的**多数投票**共同写入特征。
5.  **可扩展性**：该方法可扩展到1500个特征，且保持高稀疏度（99%）和100%的发现成功率。这说明标准基维度组合的编码容量是巨大的。
6.  **高效与无需训练**：该方法证明了标准基本身就足以用于特征读取，无需任何训练、优化或额外的GPU算力。

## 7. 优点

- **极致的计算效率**：无需任何训练，这是其与所有现有方法相比最显著的优势。仅通过一次前向传播和轻量级数学计算即可获得有价值的可解释性。
- **架构通用性**：在三个不同模型家族上得到验证，说明该方法具有普适性，而不仅仅是某个特定模型的特性。
- **方法简洁且透明**：最终使用的技术是简单的符号计数和汉明距离计算，逻辑极其清晰且易于复现和理解，避免了黑盒“探针”的复杂性。
- **强大的可解释性**：不仅揭示了“是什么”（维度编码内容），还初步解释了“为什么”（通过追踪FFN写入链路），构成了完整的电路追踪。
- **实验设计严谨**：对照实验、消融研究和统计检验设计得非常全面，结论的说服力强。
- **对标最新方法**：直接将方法用于和当前最流行的SAE进行头对头对比，并取得了有竞争力的结果，有助于确立该方法在领域内的地位。

## 8. 不足与局限

- **模型规模限制**：实验仅限于3-7B参数规模的模型。论文自身也承认，该结构能否扩展到70B+的更大规模模型仍是开放问题。
- **上下文信息利用不充分**：约42%的低幅度维度在上下文中会翻转符号。论文虽然将其归结为上下文编码机制，但目前其类型级框架还不能很好地表征这些低幅度维度所携带的丰富上下文信息。
- **行为操控效果不佳**：初步尝试通过翻转维度符号进行行为控制（Steering）的结果不一致，表明类型级原型可能过于粗糙，不足以实现精确的行为级干预。
- **特征集不完整**：175个手工类别和1500个无监督特征只是模型特征全集的一个子集。论文没有声称覆盖了所有语义空间。
- **跨模型变异性**：各个模型的特征发现性能（AUC）存在差异，与模型容量（隐藏维度、词汇表大小）相关。Gemma 3-4B的性能相对较弱（平均AUC 0.763，比Qwen/Mistral低），这可能暗示不同模型的该性质存在差异。
- **对训练的依赖**：虽然方法本身无需训练，但观察到的“维度独立性”这一现象是**训练的结果**，而非模型架构的先天属性。在随机初始化模型上，该特性完全消失，这验证了其有效性，同时也表明它不是普适的架构特征。

（完）
