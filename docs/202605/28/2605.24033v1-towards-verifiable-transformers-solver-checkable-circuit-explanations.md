---
title: "Towards Verifiable Transformers: Solver-Checkable Circuit Explanations"
title_zh: 面向可验证的Transformer：求解器可检验的电路解释
authors: Neel Somani
date: 2026-05-21
pdf: "https://arxiv.org/pdf/2605.24033v1"
tags: ["query:ns-xai"]
score: 9.0
evidence: 通过SMT求解器进行神经符号验证
tldr: 当前机械可解释性通过示例和消融验证电路，缺乏形式化证明。本文提出可验证Transformer框架，将局部电路转化为SMT求解器可检查的命题。通过直接验证（使用SMT可表示架构）在小符号序列任务上穷举验证了投影功能等价性、内容不变性、边缘必要性等属性。针对难编码算子，采用代理中介验证，在GPT-2规模上展示了可验证符号解释和反例生成。该工作为将机械解释转化为可证明或证伪的形式命题提供了具体路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.24033v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1599, \"height\": 485, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24033v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1022, \"height\": 581, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24033v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1023, \"height\": 579, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.24033v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1705, \"height\": 601, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.24033v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1713, \"height\": 169, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.24033v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1631, \"height\": 285, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.24033v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1712, \"height\": 167, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.24033v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1571, \"height\": 604, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.24033v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1710, \"height\": 327, \"label\": \"Table\"}]"
motivation: 现有机械解释缺乏形式化验证，存在从发现电路到证明其功能间的鸿沟。
method: 将Transformer局部电路编码为SMT约束，直接验证或通过可编码代理间接验证。
result: 在小符号任务上穷举验证了多种属性；GPT-2规模下证明了代理验证的可行性。
conclusion: 为机械解释的形式化证明提供了可行框架，而非全模型验证。
---

## 摘要
机制可解释性通常识别Transformer模型内部的电路，但这些电路的解释通常通过示例、消融和手动推理来验证。这留下了在发现一个合理电路与证明电路功能之间的差距。我们引入了可验证Transformer，这是一个将任务局部的Transformer电路转换为有界、可被求解器验证的声明的框架。给定一个行为、一个有限任务域和一个候选标记投影，我们提取一个任务电路并验证诸如投影功能等价性、边必要性、任务相关不变性和最终残差鲁棒性等属性。直接验证将提取的电路本身编码到SMT求解器中。当电路包含无法精确或易处理编码的算子时，代理中介验证拟合一个可SMT编码的代理，在有限域上针对提取的电路进行验证，并针对代理验证符号解释。我们使用带符号L1 BandNorm、sparsemax注意力和LeakyReLU的GPT风格架构实例化直接验证。在小型符号序列任务上，我们训练一个SMT可表示的Transformer，提取用于引号闭合和括号类型追踪的稀疏电路，并详尽验证投影功能等价性、内容不变性、边必要性和最终残差鲁棒性。在GPT-2规模上，相同的算子堆栈在OpenWebText上稳定训练，尽管朴素的直接SMT验证仍然难以处理。我们还展示了在带有难以编码注意力机制的任务局部电路上的代理中介验证，展示了已验证的符号解释和求解器生成的反例。目标不是全模型验证，而是为将机制电路解释转化为可证明或反驳的形式化命题提供一条具体路径。

## Abstract
Mechanistic interpretability often identifies circuits inside Transformer models, but explanations of those circuits are usually validated through examples, ablations, and manual reasoning. This leaves a gap between finding a plausible circuit and proving what the circuit does. We introduce Verifiable Transformers, a framework for converting task-localized Transformer circuits into bounded, solver-checkable claims. Given a behavior, a finite task domain, and a candidate-token projection, we extract a task circuit and verify properties such as projected functional equivalence, edge necessity, task-relevant invariance, and final-residual robustness. Direct verification encodes the extracted circuit itself into an SMT solver. When a circuit contains operators that are not exactly or tractably encodable, surrogate-mediated verification fits an SMT-encodable surrogate, validates it against the extracted circuit over the bounded domain, and verifies symbolic explanations against the surrogate. We instantiate direct verification with a GPT-style architecture using Signed L1 BandNorm, sparsemax attention, and LeakyReLU. On small symbolic sequence tasks, we train an SMT-representable Transformer, extract sparse circuits for quote closing and bracket type tracking, and exhaustively verify projected functional equivalence, content invariance, edge necessity, and final-residual robustness. At GPT-2 scale, the same operator stack trains stably on OpenWebText, although naive direct SMT verification remains intractable. We also demonstrate surrogate-mediated verification on task-localized circuits with hard-to-encode attention, showing both verified symbolic explanations and solver-generated counterexamples. The goal is not full-model verification, but a concrete path for turning mechanistic circuit explanations into formal propositions that can be proven or refuted.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

当前机械可解释性（Mechanistic Interpretability）主要通过示例、消融实验和手动推理来识别Transformer内部的电路并验证其解释。这种做法缺乏形式化保证：即使找到了一个看似合理的电路，也无法证明它确实“做”了所声称的任务。因此，**存在从发现电路到证明电路功能的鸿沟**。论文提出“可验证Transformer”（Verifiable Transformers）框架，旨在将任务局部的Transformer电路转化为有界、可被SMT（Satisfiability Modulo Theories）求解器自动检查的形式声明，从而将机械解释从定性分析提升为可证明或可反驳的命题。

### 2. 论文提出的方法论：核心思想、关键技术细节

**核心思想**：给定一个行为、一个有限任务域和一个候选标记投影，提取一个任务电路，然后对电路进行形式化验证。

**关键技术细节**：
- **电路提取与验证路径**：从全模型出发，提取保留边集E的局部电路CE。验证分两条路径：
    - **直接验证**：当电路中的所有算子均可被SMT精确编码且问题规模可处理时，直接将CE编码为SMT公式，检查属性（如功能等价性）。对每个属性，将否命题编码为可满足性问题，若不可满足则证明，可满足则返回反例。
    - **代理中介验证**：当电路包含难以直接编码的算子（如标准QK注意力）时，先训练一个可SMT编码的代理模型S（如小ReLU网络），在有限域上验证S与CE的行为等价性，然后将符号解释程序P与S进行形式化等价检查。因S是可编码的，故可实现求解器验证。
- **SMT可表示架构**：为使直接验证可行，设计了GPT风格的架构，替换了传统Transformer中难以精确编码的组件：
    - *Signed L1 BandNorm*：替代LayerNorm，通过投影式尺度控制（L1球投影和抬升机制），避免除法和平方根，运算由仿射映射、比较、max等组成。
    - *Sparsemax Attention*：替代Softmax，将欧几里得投影到概率单纯形，得到分段线性注意力权重，可产生精确零值，但QK乘积和加权求和仍引入非线性。
    - *LeakyReLU*：替代GELU，精确分段线性。
- **验证的属性**（见表1）：
    - 投影功能等价性（与参考符号程序一致）
    - 边必要性（每条边对于任务行为是必要的）
    - 任务相关不变性（对指定无关变化敏感）
    - 最终残差鲁棒性（在最终残差的小扰动下决策稳定）

### 3. 实验设计：数据集、场景、基准、对比方法

**直接验证实验（小模型）**：
- **任务**：两个符号语法任务 —— 引号闭合（判断下一个标记是单引号还是双引号）和括号类型（判断是方括号还是花括号）。
- **数据集**：每个任务构建有限穷举域，仅含128个离散序列。任务域为固定模板族，词汇表自定义32个标记。
- **基准**：参考符号程序根据规则给出正确答案。
- **对比方法**：无外部对比；作者将验证结果与穷举枚举（PyTorch前向计算）进行一致性校验，作为编码和提取的“健全性检查”。

**GPT-2规模可训练性实验**：
- **数据集**：OpenWebText（训练和验证）、WikiText-103（验证）。
- **基准**：本地训练的GPT-2 baseline（使用标准LayerNorm、Softmax、GELU）。
- **对比方法**：三个变体：(1) 仅用sparsemax替换softmax；(2) 仅用BandNorm替换LayerNorm；(3) 全栈（BandNorm + sparsemax + LeakyReLU）。主要比较验证损失和困惑度。

**代理中介验证实验**：
- **任务**：引号追踪和括号计数。
- **数据**：有限任务示例用于拟合代理和候选程序选择。
- **对比**：有限数据集上的准确率 vs. 形式化验证结果（程序等价性）。

### 4. 资源与算力

论文**未明确说明**使用的GPU型号、数量、训练时长等具体资源信息。仅提及在GPT-2规模实验中使用了“相同的tokenizer、上下文长度、优化方案和评估代码”，但未提供算力细节。关于小模型实验也未提及训练硬件和时长。

### 5. 实验数量与充分性

- **实验数量**：
    - 小模型直接验证：两个任务，每个任务均验证了4个属性（等价性、不变性、边必要性、鲁棒性），结果如表2。
    - GPT-2规模训练：三个变体加上 baseline，共4组实验，报告验证损失和困惑度（表3），并附有归一化消融历史（表5）。
    - 代理中介验证：两个任务（引号和括号），表4展示结果。
    - 另有附录中多种归一化尝试的消融（表5）。
- **充分性与公平性**：
    - 小实验：由于域极小，穷举验证是完备的，属性验证被正确完成。但仅为概念验证，不涉及泛化。
    - GPT-2规模：比较了同一个实验框架下的基线，相对公平。但作者承认BandNorm性能远差于LayerNorm，且通过引用2025年的LayerNorm移除工作指出后者可能是更好的替代方案。Sparsemax-only损失接近基线，但BandNorm引起较大退化。
    - 代理实验：展示了有限数据集拟合不足的问题（引号任务反例说明表面准确率不足以证明），具有启发性；但仅两个任务，通用性有限。

### 6. 论文的主要结论与发现

1. **直接验证可行**：在小型符号任务上，可以训练一个SMT可表示Transformer，提取稀疏电路，并通过SMT求解器穷举验证所有定义属性（功能等价、边必要性、不变性、鲁棒性）。
2. **GPT-2规模可训练但不具可验证性**：全栈SMT可表示架构（BandNorm + sparsemax + LeakyReLU）可以稳定训练至收敛，但直接SMT验证因QK乘积、注意力加权乘积、分支爆炸等问题在当前不可行。
3. **代理中介验证是一种松弛但实用的替代**：当原始电路难以直接编码时，代理中介方法可以验证符号解释或返回反例，提供了正式验证与当前可解释性实践之间的桥梁。
4. **归一化仍是主要瓶颈**：Sparsemax替代Softmax影响较小，但BandNorm替代LayerNorm损失较大；近期LayerNorm移除工作可能更有效。
5. **形式化声明需要领域指定**：任务域、候选标记集、电路提取语义是声明的一部分；承诺是有界的、投影的、电路级别的。

### 7. 优点：方法或实验设计上的亮点

- **问题清晰**：系统地将机械解释的形式化验证问题拆解为有界、投影的电路级声明，避免了“全模型验证”的过度目标。
- **双路径设计**：直接验证与代理中介验证互补，覆盖了不同可表示性程度的电路。
- **架构设计目标明确**：为可验证性设计专门架构（BandNorm、sparsemax、LeakyReLU），并展示了可训练性，为后续研究提供了具体算子候选。
- **反例的价值凸显**：在代理验证中，求解器生成的引号反例突显了仅依赖示例准确率的不足，展示了形式化验证的附加价值。
- **严谨的声明纪律**：清楚列出不能声称的内容（全模型、全词汇、全局最小、任意长度），避免了过度推广。

### 8. 不足与局限

- **有限域限制**：所有保证仅在有界域上成立，不保证任意长度泛化。
- **小实验过于简单**：直接验证仅在128个输入的玩具任务上完成，实用性有限。作者承认等价性本身通过穷举即可证明，SMT的价值在于其他连续量属性的验证。
- **GPT-2规模验证不可行**：论文未展示GPT-2规模电路的直接SMT验证，主要贡献是训练可行性而非验证。
- **Sparsemax注意力仍非线性**：即使sparsemax是分段线性的，QK乘积和注意力加权求和仍引入乘法非线性，导致SMT编码困难。
- **BandNorm性能差**：与LayerNorm及其移除方法相比，BandNorm在GPT-2规模上损失较大，且非唯一有效方案。
- **代理中介验证依赖代理保真度**：若代理与教师电路未在域上完全一致，则证明不严格依赖于原始模型。
- **DSL搜索不完整**：符号解释程序搜索仅限于所选模板族，失败不等于不存在更复杂的等价程序。
- **求解器精度问题**：证明基于精确实数/有理数编码，而非浮点数值，存在实现与编码的潜在不一致（虽通过PyTorch健全性检查减轻）。
- **分支不可判定**：鲁棒性验证可能因分支稳定性无法认证而返回“未知”。

（完）
