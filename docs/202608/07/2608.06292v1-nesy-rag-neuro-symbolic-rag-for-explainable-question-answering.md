---
title: "NeSy-RAG: Neuro-Symbolic RAG for Explainable Question Answering"
title_zh: NeSy-RAG：面向可解释问答的神经符号 RAG
authors: "Jonas Gann, Michael Gertz"
date: 2026-08-06
pdf: "https://arxiv.org/pdf/2608.06292v1"
tags: ["query:ns-xai"]
score: 10.0
evidence: 神经符号RAG，用Prolog模块实现可解释问答
tldr: "现有RAG推理过程不透明且难以验证，缺失用户上下文也常被忽略。NeSy-RAG将检索文本转化为可归属的Prolog模块，生成语义谓词并组合为查询，同时引入符号知识缺口检测自动识别缺失用户事实。在ShARC基准上无领域训练即达61.1%准确率，远超同模型RAG基线42.8%。该方法提供透明可追踪的推理路径，并支持交互式补全信息，显著增强可解释性与可靠性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.06292v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1582, \"height\": 575, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.06292v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1575, \"height\": 599, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.06292v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1313, \"height\": 831, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.06292v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1413, \"height\": 411, \"label\": \"Table\"}]"
motivation: RAG推理过程不透明，中间步骤难验证，且缺失用户上下文无法系统检测。
method: 将文本转为Prolog模块，用联合嵌入检索谓词，执行查询，并通过符号知识缺口检测触发交互。
result: "ShARC基准上准确率61.1%，较同模型RAG基线42.8%大幅提升。"
conclusion: 实现可解释、可归属的RAG推理，并能主动检测与补全缺失用户上下文。
---

## 摘要
检索增强生成（RAG）通过将大型语言模型（LLM）锚定在外部知识（如文本语料库）中，从而改进了问答。然而，其推理过程在很大程度上仍是不透明的：中间推理步骤难以验证，且无法可靠地归因于具体证据。此外，缺失的用户特定上下文很少被系统性检测到，常常导致不完整或错误的输出。我们提出了 NeSy-RAG，一个模块化的神经符号 RAG 框架，它从检索到的文本片段中合成出可归因的 Prolog 模块。对于每个片段，系统生成语义上有意义的谓词，这些谓词编码布尔声明，并可能依赖于用户事实。通过使用自然语言-代码联合嵌入，谓词被检索并组合成 Prolog 查询。为解决不完整的用户上下文，我们引入了一种符号知识缺口检测机制，该机制识别缺失的用户事实——其真值会影响查询结果——并自动触发后续交互。执行生成的 Prolog 查询会产生确定性答案，以及透明的执行轨迹，将每个推理步骤关联到其来源。在 ShARC 基准测试上，无需特定领域训练，NeSy-RAG 达到了 61.1% 的准确率，优于达到 42.8% 准确率的同模型 RAG 基线。

## Abstract
Retrieval-augmented generation (RAG) improves question answering by grounding large language models (LLMs) in external knowledge such as text corpora. However, its reasoning process remains largely opaque: intermediate reasoning steps are difficult to verify and cannot be reliably attributed to specific evidence. Moreover, missing user-specific context is rarely detected systematically, often leading to incomplete or incorrect output.   We propose NeSy-RAG, a modular neuro-symbolic RAG framework that synthesizes attributable Prolog modules from retrieved text chunks. For each chunk, the system generates semantically meaningful predicates that encode Boolean claims, which may depend on user facts. Using joint natural language-code embeddings, predicates are retrieved and composed into Prolog queries. To address incomplete user context, we introduce a symbolic knowledge-gap detection mechanism that identifies missing user facts whose truth values affect the query outcome and automatically triggers follow-up interactions.   Executing the resulting Prolog queries yields deterministic answers together with transparent execution traces that link each reasoning step to its originating source. On the ShARC benchmark, without domain-specific training, NeSy-RAG achieves 61.1% accuracy, outperforming a same-model RAG baseline that achieves 42.8% accuracy.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：检索增强生成（RAG）通过外部知识库提升LLM问答的准确性，但其推理过程依然不透明——中间步骤难以验证、无法可靠归因到具体证据；同时，缺失的用户特定上下文很少被系统地检测到，常导致不完整或错误的回答。
- **现有不足**：
  - 标准链式推理（CoT）无法提供可信的解释；
  - 输出对源文本的归因没有保证；
  - 已有的神经符号方法或依赖单一大型知识库、人工规则，或生成的逻辑程序缺乏清晰来源，且普遍未处理推理时缺失用户信息的问题。
- **整体含义**：NeSy-RAG旨在将神经符号推理与RAG结合，构建一个可解释、可归因、能主动补全缺失用户上下文的问答框架，适用于医疗、法律等高可靠性领域。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- 将检索到的文本片段转换为**模块化Prolog知识库**，执行符号推理获得确定性答案和可追踪的执行轨迹；
- 通过**0-arity谓词抽象**和**联合NL–PL嵌入检索**实现可扩展的查询构建；
- 通过**符号知识缺口检测**识别缺失用户事实并自动触发追问。

### 技术细节
- **可归因Prolog合成（Attributable Prolog Synthesis）**：
  - 每个文本片段 \(c_m\) 独立合成一个Prolog模块 \(p_m = (R_m, F_m)\)，包含规则和事实；
  - 每条规则/事实都能唯一映射到源文本片段，执行轨迹可附带模块（来源文件）信息；
  - 模块化设计使知识库可随检索结果灵活聚合，支持大规模语料的符号推理。
- **知识缺口检测与动态事实**：
  - 将Prolog事实分为**通用事实**（值完全由领域知识确定）和**动态事实**（至少一个参数需在推理时从用户获取，例如“用户是否达到法定年龄”）；
  - 动态事实可带单个布尔参数，对应生成**交互方法（interaction method）**，在Prolog执行到该事实时自动触发，向用户提问或请求确认；
  - 通过评估缺失动态事实在真假两种赋值下的查询结果，可判断是否影响最终答案。
- **查询翻译（Query Translation）**：
  - 0-arity规则将模块功能抽象为自包含的布尔声明（如 `can_not_get_winter_fuel_payment`）；
  - 使用联合NL–PL嵌入模型（如 jina-code-embeddings-1.5b）对0-arity规则与用户问题计算余弦相似度，选出top-k规则；
  - 仅向LLM暴露这top-k规则以组合成Prolog查询，而非整个知识库，从而减少上下文窗口、抑制幻觉。
- **答案生成**：
  - 执行Prolog查询得到确定性结果与执行轨迹；
  - 轨迹可唯一映射到源文本，支持更细粒度的解释与验证。

## 3. 实验设计：数据集、Benchmark和对比方法

- **数据集**：ShARC（Saeidi et al., 2018）——基于英国政府网站法律文本的对话式问答数据集。每个条目包含文本片段、问题、用户场景、历史记录、可提取证据和目标标签（yes/no/irrelevant/more）。
- **实验设置**：
  - 将场景、证据、历史合并为上下文并**对系统隐藏**，模拟必须通过追问获取用户信息的情境；
  - 使用独立的LLM扮演“模拟用户”，依据隐藏上下文回答系统的追问；
  - 测试集共8,276条，每个目标片段混合随机干扰片段（共3个片段）模拟检索结果。
- **对比方法**：
  - **LLM RAG Baseline**：与NeSy-RAG相同设置，但用纯LLM推理（先判相关、再预测、必要时生成追问）。
  - **LLM Upper Baseline**：给LLM完整上下文（无需追问），作为无训练LLM的性能上界。
  - **BiAE（Luo et al., 2023）**：有监督训练的当前SOTA，作为训练方法上界。
- **模型**：gpt-oss:20b（本地Ollama）、gpt-5-mini；嵌入模型为 jina-code-embeddings-1.5b。

## 4. 资源与算力

- 论文**未系统报告训练算力**（因为方法无需训练，仅用预训练LLM和嵌入模型推理）。
- 提及的具体算力信息：
  - gpt-oss:20b 运行于 **NVIDIA RTX 3090**（使用Ollama）；
  - 未说明GPU数量、训练时长（本方法无训练过程）或总Token消耗。
  - 作者指出模块复用和较小上下文窗口可节省Token，但未给出定量分析。

## 5. 实验数量与充分性

- **实验数量**：
  - 主要实验：ShARC测试集上的整体准确率和平均耗时对比；
  - 针对NeSy-RAG的0-arity谓词数量参数做了k=3/k=10/k=20的敏感性分析；
  - 提供了归一化混淆矩阵分析（NeSy-RAG vs 两个LLM基线）；
  - 报告了2.2%的Prolog语法错误率及基线结构化输出失败等误差。
- **充分性与公平性评价**：
  - **优点**：设置较为公平，RAG基线与NeSy-RAG共享相同检索设置；模拟用户机制将“系统推理能力”与“用户反应”解耦，便于独立评估。
  - **不足**：
    - 仅在一个数据集（ShARC）上评估，缺乏跨领域验证；
    - 未进行真正的端到端检索实验（人为给定单一相关片段+随机干扰，未测试多跳或多片段场景）；
    - 未报告偏差分析（如不同问题类型、不同LLM的稳定性）；
    - 消融实验较少（除k值外，未单独消融0-arity检索、知识缺口检测等组件）。

## 6. 论文的主要结论与发现

- NeSy-RAG在ShARC测试集上达到 **61.1%** 准确率（k=10），显著优于同模型LLM RAG基线的 **42.8%**，也优于gpt-5-mini RAG基线的53.7%。
- 平均执行时间更低（7.4s vs 11.4s），源于模块复用、小上下文窗口和算法化追问触发。
- 知识缺口检测能力突出：NeSy-RAG对 `more` 类别召回61%，而LLM RAG基线仅19%、LLM Upper仅23%，证明符号方法能有效识别信息不足。
- LLM Upper基线在yes/no上准确率更高（77%/85%对53%/63%），说明完整上下文先验的优势；NeSy-RAG的主要错误模式是将yes误判为more（31%），源于动态谓词过度生成。
- 总体结论：模块化神经符号RAG可以在不训练的情况下超越标准LLM RAG，并提供透明推理与主动追问能力，有利于高风险领域部署。

## 7. 优点

- **可解释性**：每个推理步骤可追溯到具体文本片段，执行轨迹天然支持验证与审计。
- **模块化与可扩展性**：Prolog模块与文本块绑定，检索时可同时聚合符号知识，避免大型单一知识库，支持选择性推理。
- **查询构建可伸缩**：0-arity谓词抽象+联合嵌入检索，将查询生成限制在极小子集，降低LLM负担和幻觉风险。
- **主动知识缺口检测**：符号执行中自动识别缺失用户信息，触发有目标的追问，明显改善 `more` 预测。
- **高效**：模块复用、小上下文窗口和免LLM追问→执行时间更低、Token更省。
- **无需领域训练**：零样本即可工作，便于迁移到新领域。

## 8. 不足与局限

- **动态谓词过度生成**：产生过多不必要的用户追问，导致部分可回答实例被保守标记为 `more`，限制准确率提升空间。
- **评估覆盖面窄**：仅在ShARC上测试，未覆盖医疗、金融等其他高风险域，也未验证多跳、多片段复杂推理。
- **检索模拟简单**：未集成真实密集检索系统，无法反映实际检索噪声对结果的影响。
- **依赖LLM生成质量**：Prolog合成和查询生成仍依赖LLM，存在2.2%语法错误；更大模型是否提升性能未探索。
- **背景假设受限**：当前知识缺口检测仅支持一阶布尔动态事实，对更丰富的数据类型或复杂参数结构支持不足；问题限定为布尔问答。
- **公平性细节待完善**：与BiAE等训练方法的对比中未做同设置对齐（BiAE直接获取上下文而NeSy-RAG需交互），可能造成比较偏差；未报告置信区间或多次重复实验方差。

（完）
