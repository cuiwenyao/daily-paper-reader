---
title: Learning a Vector-Symbolic Model for Socio-Cultural Tasks
title_zh: 学习一个用于社会文化任务的向量符号模型
authors: "Meera Ray, Swapnika Dulam, Christopher L. Dancy"
date: 2026-08-03
pdf: "https://arxiv.org/pdf/2608.02807v1"
tags: ["query:ns-xai"]
score: 6.0
evidence: 向量符号自编码器结合神经表示与符号组合结构
tldr: 认知建模中表征社会文化结构对决策的影响需遍历多层语义，但模型难以确定最相关层次。提出用于ACT-R的向量符号记忆系统，通过向量符号自编码器编码多级语义，并用HRR操作区分情景与语义记忆。在种族情境化IAT任务上验证了该系统可有效反映文化关联，为建模文化决策提供新方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.02807v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 579, \"height\": 659, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.02807v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 871, \"height\": 332, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.02807v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 887, \"height\": 594, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.02807v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 741, \"height\": 332, \"label\": \"Table\"}]"
motivation: 现有句子模型难以判断社会文化情境中最相关的语义层次，且忽略自我表征在记忆中的作用。
method: 在ACT-R中加入向量符号自编码器记忆，用HRR操作分离情景记忆与语义向量并生成块激活。
result: 在种族情境化IAT中测试，该记忆系统能体现文化关联对决策的影响。
conclusion: 向量符号记忆为认知模型表征多层级社会文化语义关联提供可行途径。
---

## 摘要
我们如何更好地在计算认知模型中表示社会文化结构对决策的影响？对这种影响进行建模需要遍历多个语义表示层次，然而对于建模者来说，并不立即清楚哪些表示层次对给定情境最显著。尽管大型语言模型和基于认知的语料库模型可以通过共现来表示广泛的语义关联，但为了确定文化关联如何塑造决策，应该考虑自我表示在记忆中的作用。我们提出了一个声明性记忆系统，用于ACT-R认知架构，通过向量符号自编码器在多个层次上表示语义关联。我们使用简单的HRR操作来对情景记忆进行编码，与从文本中提取的语义记忆向量不同，从而为记忆请求产生最终的块激活。我们使用种族情境化内隐联想测验（IAT）的ACT-R认知模型来测试这个新的声明性记忆系统。

## 速览
**TLDR**：社会文化结构影响决策，但计算认知模型难以确定最相关的语义层次，现有模型多依赖共现关系，忽略自我表征的作用。本文提出一个用于ACT-R的向量符号声明性记忆系统，通过自编码器表示多级语义关联，并用HRR操作融合情景与语义记忆，生成记忆请求的激活。在种族情境的内隐联想测验上进行了验证。该方法为在认知模型中纳入社会文化因素提供了新途径。 \
**Motivation**：社会文化结构影响决策，但模型需遍历多个语义层次，难以确定哪个层次最显著。 \
**Method**：提出向量符号自编码器，在ACT-R中表示多级语义关联，用HRR结合情景与语义记忆生成激活。 \
**Result**：在种族情境的内隐联想测验（IAT）上测试了新系统，验证了其有效性。 \
**Conclusion**：展示了在认知模型中通过多层次语义表征纳入社会文化影响的途径。

---

## Abstract
How can we better represent the impact of sociocultural structures on decision making in computational cognitive models? Modeling this impact requires traversing multiple levels of semantic representation, however it is not immediately clear to a modeler which levels of representation are most salient to a given situation. Though large language models and cognitively grounded corpus models can represent broad semantic associations through co-occurences, the role of self representations in memory should be accounted for to determine how cultural associations shape decision making. We propose a declarative memory system to be used in the ACT-R cognitive architecture that represents semantic associations at multiple levels via a vector-symbolic autoencoder. We use a simple HRR operation to encode episodic memories differently from semantic memory vectors extracted from text to produce a final chunk activation for a memory request. We use ACT-R cognitive models of a racially contextualized implicit association test (IAT) to test this new declarative memory system.