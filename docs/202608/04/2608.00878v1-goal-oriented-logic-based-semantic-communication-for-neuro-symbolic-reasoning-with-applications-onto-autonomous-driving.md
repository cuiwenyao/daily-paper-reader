---
title: Goal-Oriented Logic-based Semantic Communication for Neuro-Symbolic Reasoning with Applications onto Autonomous Driving
title_zh: 面向神经符号推理的基于目标逻辑语义通信及其在自动驾驶中的应用
authors: "Ahmet Faruk Saz, Duo Xu, Faramarz Fekri"
date: 2026-08-01
pdf: "https://arxiv.org/pdf/2608.00878v1"
tags: ["query:ns-xai"]
score: 9.0
evidence: 基于一阶逻辑的神经符号决策语义通信
tldr: 面向自动驾驶的协同感知中，车辆需将局部观测传给路侧单元进行逻辑推理，但上行带宽受限。本文提出基于一阶逻辑和Dirichlet-Categorical归纳概率模型的目标导向语义通信，优先传输最降低任务目标不确定性的证据。在CARLA模拟器中用152条交通规则验证，相同通信预算下语义选择无碰撞，均匀选择则发生碰撞，展示了语义通信的优越性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.00878v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1864, \"height\": 867, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.00878v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 925, \"height\": 907, \"label\": \"Table\"}]"
motivation: 自动驾驶协同感知中上行带宽有限，需选择对评估交通规则最关键的观测证据进行传输。
method: 用Dirichlet-Categorical模型建模归纳逻辑概率，推导目标导向信息瓶颈，优先传输降低任务目标不确定性的语义证据。
result: 在CARLA的MDrive模拟器上基于152条交通规则测试，语义选择在相同通信预算下无碰撞，均匀选择出现碰撞。
conclusion: 目标导向语义通信能有效提升神经符号推理下自动驾驶的安全性与通信效率。
---

## 摘要
我们考虑在自动驾驶网络等协作环境中，基于一阶逻辑（FOL）的语义通信用于神经符号决策。每辆联网自动驾驶车辆（CAV）将其部分传感器观测转换为自然语言场景描述及相应的基于事实的FOL证据。在上行链路预算约束下，每辆车的语义编码器选择对评估交通规则最具信息量的观测，并传输至路侧单元（RSU）。RSU融合所有接收到的证据，评估协作规则，执行逻辑推理，为受限下行链路传输生成特定于车辆的安全和路权信息。每辆CAV将接收到的推理结果与其本地描述相结合，使本地LLM智能体能够选择高层驾驶动作。我们使用随机支持Dirichlet-Categorical归纳逻辑概率模型，开发了一种原则性、可验证的语义通信方法，为Carnap和Hintikka的系统提供了现代统计重新解释。基于该模型，我们推导出一种目标导向的语义信息瓶颈公式，通过减少任务目标的不确定性来优先传输证据。利用从《加利福尼亚驾驶员手册》中提取的152条交通规则，我们在CARLA的MDrive模拟器上评估了该框架。在相同的通信预算下，语义证据选择在所有场景中均无安全危险地完成，而均匀证据选择则产生碰撞，展示了语义通信的优越性。

## Abstract
We consider First-Order Logic (FOL)-based semantic communication for neuro-symbolic decision-making in collaborative environments such as autonomous driving networks. Each connected autonomous vehicle (CAV) converts its partial sensor observations into a natural-language scene description and corresponding grounded FOL evidence. Under an uplink budget, a semantic encoder at each car selects the observations most informative for evaluating traffic rules and transmit to a Road Side Unit (RSU). The RSU fuses all received evidence, evaluates collaborative rules, performs logical deduction for vehicle-specific safety and right-of-way information for constrained downlink transmission. Each CAV combines the received deductions with its local description, enabling a local LLM agent to select a high-level driving action. We develop a principled, verifiable semantic communication method using a random-support Dirichlet--Categorical model of inductive logical probability, providing a modern statistical reinterpretation of Carnap's and Hintikka's systems. From this model, we derive a goal-oriented semantic information-bottleneck formulation that prioritizes evidence transmission by its reduction of uncertainty over task goals. Using 152 traffic rules extracted from the California Driver Handbook, we evaluate the framework on MDrive simulator in CARLA. Under identical communication budgets, semantic evidence selection completes every scenario without safety hazards, whereas uniform evidence selection produces collisions, showcasing semantic communication's superiority.

---

## 论文详细总结（自动生成）

# 中文总结

## 1. 论文的核心问题与整体含义

- **研究动机**：在协作自动驾驶场景中，每辆联网自动驾驶车辆（CAV）只能通过自身传感器获得部分环境观测，存在遮挡、感知范围有限等问题，容易引发碰撞风险。通过车与路侧单元（RSU）之间的信息共享可以弥补视野盲区，但完整传输传感器数据在带宽和时延约束下不可行。
- **核心问题**：如何在有限通信预算下，选择对下游驾驶决策（特别是交通规则评估与神经符号推理）最有价值的语义证据进行传输，而不是盲目传输原始数据或均匀采样。
- **整体含义**：论文提出一种基于一阶逻辑（FOL）的目标导向语义通信框架，将感知、通信、逻辑推理与 LLM 决策统一为可解释的神经符号流水线，强调“传什么”和“为什么传”都应服务于任务目标（交通安全与规则符合性）。

## 2. 论文提出的方法论

- **系统架构**：
  1. 每辆 CAV 将局部感知状态 `V_n` 通过字幕模型 `c_ψ` 转换为自然语言场景描述 `y_n`；
  2. 逻辑编码器 `f_ζ` 将 `y_n` 转换为带事实依据的 FOL 证据集 `e_n`；
  3. 上行语义编码器 `g_ul` 在预算 `B_ul^n` 下选择证据子集 `ê_n`；
  4. RSU 融合所有车辆的上行证据，结合交通规则集 `L_T` 计算演绎闭包 `d_R = Cn(e_R ∪ L_T)`，得到安全与路权结论；
  5. 下行语义编码器 `g_dl` 为每辆车选择其任务相关的演绎子集 `d̂_n`；
  6. CAV 将本地描述、接收到的演绎和本地推理结合，由 LLM `p_ω` 选择高层驾驶动作，再交由 PID 控制器执行。

- **核心理论模型**：
  - 使用 **Q-句法（Q-sentence）** 作为 FOL 语言的有限语义基元，每个 Q-句法对应一组谓词槽的完全真值指派，构成逻辑状态空间的一个划分。
  - 提出**随机支持 Dirichlet–Categorical 模型**，由“支持层”（哪些 Q-句法类型可能存在）和“频率层”（存在类型上的 Dirichlet–Categorical 分布）组成，从而对 Carnap 的 λ-连续统和 Hintikka 的归纳系统给出现代统计解释。
  - 基于归纳逻辑概率 `c(φ|e)` 定义 Carnap 式内容信息 `cont(φ)=1−c(φ)`、语义内容熵 `H_s(φ)=Σ c(φ_i)cont(φ_i)` 及其条件版本，进而定义证据 `e` 带来的语义熵变化 `γ_s(φ;e)=H_s(φ)−H_s(φ|e)`。
  - **目标导向语义信息瓶颈**：最优传输证据 `ê` 为最大化 `γ_s(φ;ê) − β I(e;ê)`，即在压缩传输量的同时最大程度降低关于交通规则假设空间 `φ` 的不确定性。
  - 对不完整观测，利用观测立方（observation cube）和软计数/EM 更新处理部分谓词未知的情况。

## 3. 实验设计

- **仿真平台与基准**：采用 CARLA 0.9.12 中的 **MDrive 基准**（MDrive-Interaction 场景），用于评估协作驾驶中的感知共享与决策协商。
- **交通规则**：从《California Driver Handbook》提取 **152 条 FOL 假设**，作为规则集和假设空间。
- **测试场景**：共 10 个匹配场景，包括：
  - 4 个两车 Pre-Crash 场景（变体 B、C、E、F，基于 NHTSA 预碰撞分类）；
  - 4 车无保护左转（变体 3）；
  - 3 车交叉口死锁（变体 3）；
  - 4 车高速匝道合流（变体 2）；
  - 4 车环岛（变体 2）；
  - 2 个支路进入主路的无信号交叉口场景（MMUJ 变体 9、10）。
- **对比方法**：
  - **Semantic selection**：按论文定理 1 的语义信息瓶颈标准排序并选择证据；
  - **Uniform selection**：在相同预算下均匀随机选择同样数量的候选原子。
  - 上行和下行预算相同：除 Pre-Crash C 为 `k=40` 外，其余均为 `k=25`。
- **LLM 组件**：统一使用 `gpt-5.4-nano` 进行 greedy decoding。
- **评价指标**：
  - 驾驶分数 `DS = RC × IP`（路线完成率 × 违规惩罚系数）；
  - 单独统计碰撞次数；
  - 通信质量使用**相关原子选择率** `SR`：上行按规则前件判定相关性，下行按规则后件判定。

## 4. 资源与算力

- 论文**未明确说明**使用的 GPU 型号、数量、训练/推理时长等算力信息。
- 仅提及使用 LLM（`gpt-5.4-nano`）作为决策模型，并使用 Claude Code 进行编码辅助、ChatGPT 进行校对；未报告这些模型的推理开销或硬件配置。
- 因此，在资源复现与算力成本方面信息不足。

## 5. 实验数量与充分性

- **实验数量**：10 个场景 × 2 种选择策略 = 20 个仿真试验；每个场景在相同预算下做一组语义 vs 均匀对比。
- **充分性**：
  - 场景覆盖较广，包括预碰撞、左转、死锁、合流、环岛、无信号路口等典型危险工况；
  - 有统一的驾驶分数和碰撞统计，并给出了失败案例分析；
  - 相同通信预算下的对比相对公平。
  - **不足**：未进行消融实验（如不同 β、预算层级、不同 LLM、规则数量变化）；均匀选择是随机策略，论文未报告多次随机种子重复实验的均值和方差，统计显著性不明确；也没有比较其他语义通信基线或端到端学习方法。

## 6. 论文的主要结论与发现

- 在相同通信预算下，**语义证据选择在所有 10 个场景中均无碰撞完成**，而均匀选择在每个场景中至少发生一次碰撞，总计 22 次碰撞。
- 语义选择的上行平均相关选择率为 92.7%，下行 97.1%，平均驾驶分数为 93.8；均匀选择对应为 44.5%、56.3% 和 46.0。
- 失败分析表明，均匀选择下的碰撞均源于**安全关键信息的遗漏**：要么上行缺少规则前件所需的事实，导致 RSU 无法推出安全警告；要么下行遗漏或错误分发关键演绎结论。
- 论文认为，安全协作驾驶不仅依赖形式化交通规则，还依赖于**可靠地传递应用规则所需的逻辑证据**；目标导向语义通信能够显著提高神经符号推理系统的安全性与通信效率。

## 7. 优点

- **理论创新**：将 Carnap 和 Hintikka 的归纳逻辑系统重新建模为随机支持 Dirichlet–Categorical 模型，统一了逻辑概率、语义内容和现代贝叶斯推断，理论推导较严谨。
- **可解释性**：通信选择的依据是“对任务目标的语义不确定性降低量”，而不是黑箱表示，每个传输证据都可以追溯其逻辑作用。
- **系统完整性**：将感知、形式化规则、上行/下行语义通信、RSU 演绎和 LLM 决策整合为一条可验证的神经符号流水线，符合人类驾驶“先理解场景、再依据规则推理”的认知方式。
- **实验设计**：基于真实交通规则和 CARLA 仿真，对比在相同预算下进行，且给出了通信指标与驾驶指标的双重验证，结果差异显著，具有较强的说服力。

## 8. 不足与局限

- **算力信息缺失**：未报告 GPU、训练时长、推理成本等资源细节，影响复现和实际部署评估。
- **感知层抽象化**：将感知模块抽象为理想的字幕接口，未考虑真实物体检测/视觉语言模型的误差对后续逻辑推理的影响。
- **对比方法简单**：仅对比均匀随机选择，未与现有深度语义通信方法、其他特征选择策略或不同信息瓶颈权重进行对比。
- **缺乏统计稳健性分析**：均匀选择具有随机性，但论文未报告多次重复实验的统计结果，单次结果可能存在随机偏差。
- **消融实验不足**：没有分析预算大小、规则数量、LLM 模型选择、β 参数等对性能的影响。
- **场景数量有限**：10 个场景虽多样，但仍无法覆盖真实道路的全部复杂情况；且所有场景来自单一仿真器，缺乏真实路测验证。
- **对规则集的依赖**：方法性能高度依赖形式化交通规则的完整性与准确性，如果规则覆盖不全，RSU 推理可能漏判风险。

（完）
