---
title: Uncertainty Reasoning with Large Language Models for Explainable Disease Diagnosis
title_zh: 基于大语言模型的可解释疾病诊断中的不确定性推理
authors: "Xiaoyang Fan, Yufan Cai, Zhe Hou, Jin Song Dong"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.25566v1"
tags: ["query:ns-xai"]
score: 9.0
evidence: 神经符号推理框架用于可解释疾病诊断
tldr: 临床决策需要处理不完整、不精确的语言化患者叙述。大语言模型擅长提取自然语言中的潜在信息，但缺乏可验证性和可解释性。本文提出一个神经符号推理框架，将大语言模型与形式逻辑对齐，通过两阶段推理（归纳符号泛化和推理验证）实现可解释且形式可验证的医学诊断。在公开基准上，该方法性能与最先进的大语言模型相当，同时提供可审计的推理路径和符合临床标准的诊断结论。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.25566v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1159, \"height\": 509, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25566v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1297, \"height\": 354, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.25566v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1123, \"height\": 442, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25566v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1440, \"height\": 325, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25566v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1415, \"height\": 332, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25566v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1410, \"height\": 333, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25566v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 938, \"height\": 204, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25566v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1018, \"height\": 167, \"label\": \"Table\"}]"
motivation: 大语言模型在医学诊断中缺乏可验证性和可解释性，而临床决策要求推理过程透明且可被医生审查。
method: 提取患者叙述和临床指南中的结构化实体与模糊症状模式，构建模糊逻辑和声明性规则的符号知识库，通过归纳泛化和逻辑推理引擎进行两阶段诊断推理。
result: 在公开基准上的诊断性能与最先进大语言模型持平，同时提供可解释的逐步推理链和形式化验证的诊断结论。
conclusion: 该神经符号框架结合了逻辑透明性、大语言模型适应性和概率鲁棒性，实现了与人类对齐的可验证健康推理。
---

## 摘要
临床决策需要在不完整、不精确且以语言表达的患者叙述中进行推理。虽然大语言模型在从自然语言中提取潜在信息方面表现出色，但它们缺乏可信医疗AI所必需的可验证性和可解释性。我们提出了一种神经符号推理框架，将大语言模型与形式逻辑对齐，以实现可解释且形式可验证的医疗诊断。患者描述和临床指南被嵌入到神经知识库中，大语言模型提取结构化的医疗实体、时间关系和模糊症状模式，这些被解码为用模糊逻辑和声明性规则表达的符号知识库。我们进行两阶段推理：(1) 归纳符号泛化，从编码的叙述中捕获诊断模式；(2) 通过逻辑编程引擎进行推理验证，以推导和验证符合临床标准的诊断。每个症状被视为具有概率权重的模糊谓词，推理路径是可审计、可调整且与医生反馈兼容的。与纯统计方法不同，我们的系统支持迭代细化：LLM生成的诊断与真实情况之间的不一致可以通过形式规则进行追踪、解释和纠正。通过结合基于逻辑的透明性、LLM的适应性和概率鲁棒性，该框架实现了与人类一致的医疗推理，具有强大的泛化能力和可验证的逐步推理链。我们在公共基准上验证了该框架，展示了符号推理与LLM在真实临床叙述中的有效调和。结果表明，性能与最先进的LLM相当，同时额外提供了可解释的推理路径和形式可验证的诊断结论。

## Abstract
Clinical decision-making requires reasoning over incomplete, imprecise, and linguistically expressed patient narratives. While large language models (LLMs) excel at extracting latent information from natural language, they lack the verifiability and interpretability essential for trustworthy medical AI. We propose a neuro-symbolic reasoning framework that aligns LLMs with formal logic to enable explainable and formally verifiable medical diagnosis. Patient descriptions and clinical guidelines are embedded into a neural knowledge base, where LLMs extract structured medical entities, temporal relations, and fuzzy symptom patterns, which are decoded into a symbolic knowledge base expressed in fuzzy logic and declarative rules. We perform two-stage reasoning: (1) inductive symbolic generalization to capture diagnostic patterns from encoded narratives, and (2) inference verification via a logic programming engine to derive and validate diagnoses consistent with clinical standards. Each symptom is treated as a fuzzy predicate with probabilistic weights, and inference paths are auditable, adjustable, and compatible with physician feedback. Unlike purely statistical methods, our system supports iterative refinement: misalignment between LLM-generated diagnoses and ground truth can be traced, explained, and corrected through formal rules. By combining logic-based transparency, LLM adaptability, and probabilistic robustness, the framework enables human-aligned healthcare inference with strong generalization and verifiable, step-by-step reasoning chains. We validate our framework on public benchmarks, demonstrating effective reconciliation of symbolic reasoning and LLMs with real-world clinical narratives. Results show performance comparable to state-of-the-art LLMs, while additionally providing interpretable reasoning paths and formally verifiable diagnostic conclusions.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将根据您提供的论文内容，为您生成一份结构化、深入且客观的中文总结。

---

# 论文结构化总结

### 1. 论文的核心问题与整体含义（研究动机和背景）

*   **核心问题**：临床诊断决策需要在信息不完整、语言模糊（如“轻度疲劳”）和概率性的情况下进行推理。大型语言模型（LLM）虽然能理解自然语言，但存在“幻觉”问题，其推理过程不透明、不可验证，难以在要求高可信度的医疗AI领域被医生信任。纯符号系统虽然透明，但难以处理现实世界中模糊、嘈杂的症状描述。
*   **整体含义**：论文旨在解决医疗AI的可信度问题。作者提出，为了实现既准确又可解释的临床诊断，需要一种“神经符号”方法。该方法结合了LLM在理解自然语言方面的灵活性与形式逻辑在推理上的透明性、可验证性，从而生成医生可以审计、理解和干预的诊断结果。

### 2. 论文提出的方法论：核心思想、关键技术细节

*   **核心思想**：构建一个“神经符号循环”（Neuro-symbolic Cycle）框架，将LLM作为“感知器”从病历中提取结构化信息，再将这些信息转化为符号化的、可验证的逻辑规则进行推理。

*   **关键技术细节**：
    1.  **两阶段推理流程**：
        *   **阶段一：归纳符号泛化 (Inductive Symbolic Generalization)**：LLM从患者叙述中提取结构化的三元组（实体、关系、值），如 `symptom(chest pain, exertional)`。
        *   **阶段二：推理验证 (Inference Verification)**：将提取的事实与临床指南规则输入到逻辑编程引擎（Prolog）中，通过模糊逻辑和概率推理进行验证，得出诊断假设。
    2.  **模糊符号化 (Fuzzy Symbolization)**：
        *   LLM提取的每个症状事实都被赋予一个模糊权重 (w ∈ [0, 1])，用于量化“轻度”、“间歇性”等自然语言中的模糊描述，形成 `fuzzy_symptom(chest_pain, 0.8)` 这样的模糊谓词。
    3.  **Prolog 形式推理**：
        *   将模糊事实和临床规则转换为 Prolog 程序，进行严格的符号推理。例如：
            ```prolog
            diagnosis(stable_angina) :- symptom(chest_pain), trigger(exertion), risk(_), not(lab(troponin_elevated)).
            ```
    4.  **混合症状加权 (Hybrid Symptom Weighting)**：
        *   每个症状的权重 `wi` 由两部分融合而成：
            *   **检索先验 (wretr_i)**：从类似病例库（FAISS索引）中检索到的相似度。
            *   **内在显著度 (wtext_i)**：LLM根据语言描述（如“严重”、“偶尔”）给出的权重。
            *   **校准**：通过一个可调节的软混合函数将两者融合。
    5.  **神经符号循环**：
        *   系统支持通过医生反馈和数据驱动的自动规则更新来迭代优化知识库。
            *   **医生反馈**：医生可以直接修改规则或调整症状权重，所有版本都有记录，便于审计。
            *   **自动更新**：采用在线被动-主动排序更新（Online PA Ranking Update）和基于统计的结构阈值来动态调整规则的结构（添加或剪裁症状-疾病边）。

### 3. 实验设计：数据集、Benchmark、对比方法

*   **数据集**：
    1.  **GretelAI/symptom_to_diagnosis**：单标签数据集，通过程序生成的患者画像与对应疾病。
    2.  **Lavita/ChatDoctor-iCliniq**：多标签数据集，源自真实医患对话（iCliniq平台）。
    3.  **MIMIC-IV (v3.1)**：多标签数据集，包含结构化实验室结果和临床笔记。来自PhysioNet。

*   **Benchmark**：任务的公正评价通过Top-1、Top-3和Top-5的准确率、精确率、召回率和F1分数来衡量。还使用GPT-4o评估了中间步骤的错误率和最终解释的可解释性分数。

*   **对比方法**：
    1.  **强基LLM Baselines**：GPT-4o, o4-mini, DeepSeek-R1。
    2.  **简单基线 (Simple Baseline)**：仅使用输入中第一个出现的症状进行预测。
    3.  **消融基线 (Ablation Baselines)**：
        *   **符号化-only (Symbolic-only)**：仅使用精确规则匹配。
        *   **符号化 + 概率 (Symbolic + Probabilistic)**：无模糊量化地诊断。
        *   **符号化 + 模糊 (Symbolic + Fuzzy)**：概率推理的诊断。

### 4. 资源与算力

*   **文中未明确提及**：论文没有明确说明训练LLM或运行模型所使用的具体GPU型号、数量、训练时长或计算量。实验部分只提到了调用GPT-4o等API，以及自己的推理框架。因此，算力细节缺失，无法做出具体评估。

### 5. 实验数量与充分性

*   **实验数量**：论文进行了较为全面的实验，包括：
    *   在3个数据集上的主对比实验。
    *   3组消融实验，分别验证了模糊模块和概率模块的必要性。
    *   在不合格数据剔除后的二次验证实验。
    *   一致性评估（中间步骤错误率、可解释性分数）。
    *   成本分析（Token用量、花费、运行时间）。
*   **充分性与客观性**：
    *   **充分**：实验覆盖了不同数据类型（单标签、多标签，程序生成、真实对话、结构化医疗记录），并进行了全面的消融分析，这对于验证所提模块的有效性是充分的。
    *   **客观/公平**：实验设计较为公平。对比了多个强LLM（GPT-4o等）和多个消融版本。尤其是在iCliniq数据集上，由于数据质量问题导致本方法性能略低于GPT-4o时，作者诚实地指出并分析了原因，并在清理数据后重新实验。不过，实验基准中略去了一些其他可能的神经符号基线或传统方法，可能略有偏颇。

### 6. 论文的主要结论与发现

1.  **性能与LLM相当**：所提出的神经符号框架在多个数据集上实现了与强大的纯LLM（如GPT-4o）相当甚至更优的诊断准确率，尤其在症状信号更清晰的MIMIC-IV数据集上表现更好。
2.  **可解释性优势**：该框架能够提供可审计、逐步、可验证的推理路径，并生成高分的可解释性报告，这是纯LLM无法做到的。
3.  **消融实验验证**：模糊量化和概率推理模块独立地且显著地提升了纯符号化基线的性能。
4.  **成本效益**：虽然在推理时间上更长，但该框架在Token使用量和货币成本上比直接使用GPT-4o更高效。
5.  **鲁棒性**：系统在症状提取方面有较低的误差率，推理链条具有良好的一致性。

### 7. 优点：方法或实验设计上的亮点

*   **创新的神经-符号整合**：巧妙地融合了LLM的感知能力与逻辑推理的透明度，通过“模糊化”和“概率化”解决了传统符号系统处理模糊语言困难的问题。
*   **可审计的推理链**：每一步推理（从提取到符号化到Prolog推导）都是公开的，不仅输出结果，还输出证据，满足了临床决策透明性的刚性需求。
*   **医生在回路中的迭代**：系统支持医生直接反馈修改规则，并结合数据自动调整，实现了模型的持续学习与进化，且所有版本都可追溯。
*   **统一不确定性建模**：将语言模糊性（通过模糊逻辑）和证据的不确定性（通过概率推理）统一在同一个框架中处理。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制

*   **依赖手工规则**：符号推理层依赖于手动策划的规则库。虽然可解释，但构建和维护的成本高，难以覆盖所有疾病的全部临床表现，限制了向新领域的可扩展性。
*   **模糊函数静态**：模糊逻辑模块依赖于专家设定的固定隶属度函数，缺乏对异质性患者群体的数据驱动校准能力。
*   **缺乏时间建模**：框架将症状建模为静态观测，未能显式地建模症状的出现和演变过程，这对于许多疾病的诊断至关重要。
*   **实验偏差与限制**：
    *   **数据偏差**：在iCliniq数据集上的性能受限，暴露了方法对症状描述质量及知识库覆盖的敏感性。
    *   **未说明算力**：缺乏对自身方法计算资源的简单报告或比较。
    *   **未对比多种符号基线**：可能未与最新、最复杂的神经符号基线进行对比。
*   **应用限制**：该框架依赖于一个可用的预训练LLM（GPT-4o，o4-mini）和一个已有的逻辑推理引擎（Prolog），形成一个混合系统。其鲁棒性受限于LLM的提取质量。在高度模糊、极度不常见的罕见病情况下，LLM提取的准确性可能下降，从而影响整个系统的表现。

（完）
