---
title: "DeepLog: A Software Framework for Modular Neurosymbolic AI"
title_zh: DeepLog：用于模块化神经符号人工智能的软件框架
authors: "Robin Manhaeve, Stefano Colamonaco, Vincent Derkinderen, Rik Adriaensen, Lucas Van Praet, Luc De Raedt, Giuseppe Marra"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10279v1"
tags: ["query:ns-xai"]
score: 9.0
evidence: DeepLog：统一逻辑与深度学习的模块化神经符号框架
tldr: 现有神经符号AI系统各具范式且不通用，缺乏统一后端。DeepLog作为模块化编译框架，将多种神经符号语言视为高层规范，自动编译为优化算术电路，并集成到标准PyTorch工作流中。该设计既降低了机器学习从业者使用逻辑模块的门槛，也为开发者提供了共享高性能基础，有望推动神经符号AI的标准化与普及。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.10279v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 869, \"height\": 588, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.10279v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 866, \"height\": 340, \"label\": \"Table\"}]"
motivation: 现有神经符号AI框架各异且不通用，缺乏统一后端，限制实际应用和开发效率。
method: 将不同神经符号语言视为高层规范，自动编译为优化的算术电路，集成到PyTorch中作为可组合模块。
result: 实现了多种神经符号系统的模拟，提供模块化、高性能的神经符号AI统一框架。
conclusion: DeepLog通过编译优化和模块化设计，降低了神经符号AI的入门门槛，并为研究者提供了共享开发基础。
---

## 摘要
DeepLog是一个可操作的神经符号框架，它在标准PyTorch工作流中统一了逻辑和深度学习。虽然现有的神经符号系统专注于特定的范式和语义，但DeepLog作为一个通用后端，可以模拟神经符号字母汤中的许多系统。通过将多种神经符号语言视为高级规范，DeepLog软件自动将它们编译为优化的算术电路。这种设计通过将逻辑视为可组合模块，降低了机器学习从业者的门槛，同时为神经符号开发者提供共享的高性能基础，用于原型化新的集成策略。代码地址：https://github.com/ML-KULeuven/deeplog

## Abstract
DeepLog is an operational neurosymbolic framework that unifies logic and deep learning within standard PyTorch workflows. While existing neurosymbolic systems focus on a particular paradigm and semantics, DeepLog serves as a universal backend that can emulate many systems in the neurosymbolic alphabet soup. By treating diverse neurosymbolic languages as high-level specifications, the DeepLog software automatically compiles them into optimized arithmetic circuits. This design lowers the barrier for machine learning practitioners by treating logic as composable modules, while providing neurosymbolic developers with a shared, high-performance basis for prototyping new integration strategies. The code is available here: https://github.com/ML-KULeuven/deeplog

---

## 论文详细总结（自动生成）

# 论文结构化总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：当前神经符号人工智能（NeSy）领域存在大量看似不兼容的框架和范式（即“字母汤”现象），这些系统在语义（模糊/概率）、集成方式（损失函数/架构）、前端语言（一阶逻辑/逻辑编程）等方面各有不同，但缺乏统一的底层平台，导致开发重复、比较困难、进展缓慢。
- **整体含义**：论文旨在提供一个模块化、高性能、可扩展的神经符号AI框架DeepLog，该框架能模拟多种现有NeSy系统，同时降低ML从业者的入门门槛，并为框架开发者提供共享后端，从而推动该领域的标准化和累积进步。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：DeepLog是一个可操作的神经符号抽象机器，它将不同NeSy语言视为高层规范，自动将其编译为优化的算术电路，并集成到标准PyTorch工作流中。通过将逻辑表示为可组合的模块，实现符号推理与深度学习的无缝结合。
- **关键技术细节**：
  - **符号接口（SymTensor）**：扩展PyTorch模块，为输入输出添加符号注解（如布尔值、概率、模糊分数），实现自动组合、验证和变换。
  - **GPU加速的算术电路**：利用Klay包在GPU上高效评估布尔公式、概率表达式等符号计算，避免CPU瓶颈（见表1的加速效果）。
  - **工厂模式（DeepLogModuleFactory）**：根据配置选择不同的语义（概率、模糊）和推理策略（电路/采样），可快速重构多种NeSy系统（如DeepProbLog、LTN）。
  - **编译器式编译**：将高级规范解析、映射到语义，然后编译为深度模块（DeepLogModule），支持多种电路后端（如SDD）。
- **流程**：用户给定高层规范（如DeepProbLog或LTN），DeepLog引擎解析并生成公式图，根据语义映射（概率或模糊）选择相应代数操作，最终编译为可GPU执行的算术电路模块。

## 3. 实验设计：数据集、场景、基准、对比方法

- **数据集/场景**：主要展示MNIST-Addition任务，即识别两个MNIST数字并求和的组合推理任务。
- **基准**：DeepProbLog（DPL）作为参考基线。
- **对比方法**：比较了DeepProbLog的CPU实现、DeepLog的CPU实现、DeepLog的GPU实现，记录每个查询的平均推理时间。
- **其他演示**：文中给出两个例子：①使用DIMACS格式的约束（如语义损失）加载到DeepLog模块；②用工厂模式实现逻辑张量网络（LTN）。但未报告完整实验，仅说明可行性。

## 4. 资源与算力

- **未明确说明具体GPU型号、数量、训练时长**。从表1可见DeepLog GPU加速取得了显著性能提升，但文中未提供使用的硬件细节（如GPU类型、内存、训练轮数等）。因此算力信息不充分。

## 5. 实验数量与充分性

- **实验数量少**：仅表1提供了MNIST-Addition任务的推理时间比较（不同位数1-4），以及少量示例说明。没有分类准确率、收敛速度、跨任务泛化等全面实验。
- **充分性与公平性**：
  - 表1数据来自先前论文Derkinderen et al. (2025)，非本论文独立实验。
  - 对比仅涉及DeepProbLog，未与其他NeSy系统（如LTN、NeurASP等）进行性能或准确性对比。
  - 缺少消融实验（如不同语义、不同电路优化策略的影响）。
  - 无任何表格展示训练/测试精度，也未报告多个随机种子下的标准差。因此实验不够充分，客观性和公平性有局限。

## 6. 论文的主要结论与发现

- **主要结论**：DeepLog作为一个模块化后端，能够以少量代码重建多种NeSy系统（如DeepProbLog、LTN），并实现GPU加速的符号推理，显著提升效率。
- **发现**：通过分离语义规范与操作实现，可以统一看似不同的NeSy范式；GPU加速的算术电路比CPU实现快几个数量级（见表1：4位数字查询从0.196秒降至0.054秒）。
- **意义**：为NeSy社区提供了可复用的共享基础设施，降低了开发者重复造轮子的成本，同时保持与PyTorch生态兼容。

## 7. 优点：方法或实验设计上的亮点

- **模块化与兼容性**：DeepLog与标准PyTorch工作流无缝集成，无需替换现有框架，只需添加符号注解，便于ML从业者采用。
- **GPU全流程加速**：不仅神经网络部分，符号推理也完全运行在GPU上，避免CPU-GPU数据传输瓶颈，这是与其他多数NeSy系统相比的显著优势。
- **抽象工厂模式**：允许通过配置切换不同语义（概率/模糊）和推理策略，易于比较和原型设计，论文中成功复现了LTN。
- **自动编译与验证**：符号接口实现运行时类型检查，提升易用性和调试性。

## 8. 不足与局限

- **实验验证薄弱**：仅在一个简单任务（MNIST-Addition）上展示了推理时间，缺乏更复杂、更大规模任务的评估；未报告准确率、学习曲线、收敛性等关键指标。
- **未提供标准基准对比**：没有与主流NeSy框架（如LTN、Pylon、NeurASP）在相同任务上比较性能或准确性，说服力不足。
- **缺失消融实验**：未分析不同编译策略、不同电路表示、不同语义选择对结果的影响。
- **算力信息不透明**：未说明实验硬件配置，影响实验结果的可复现性。
- **应用限制**：论文强调框架的通用性，但仅展示了逻辑约束和简单概率推理，复杂一阶逻辑、递归、非单调推理等场景的兼容性未验证。
- **尚未开源（投稿时）**：代码链接虽已提供，但论文表示“代码可用”，实际评估时尚未公开稳定版本，可能影响社区信任。

（完）
